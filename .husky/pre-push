#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running CI Health Check before push..."
bash .github/scripts/ci-health-check.sh
if [ $? -ne 0 ]; then
  echo "Push aborted due to CI health check failure."
  exit 1
fi
