#!/bin/sh
# Entire CLI hooks
if [ -f 'C:\Users\Johnny Cage\.local\bin\entire.exe' ]; then 'C:\Users\Johnny Cage\.local\bin\entire.exe' hooks git prepare-commit-msg "$1" "$2" 2>/dev/null || true; else :; fi
# Chain: run pre-existing hook
_entire_hook_dir="$(dirname "$0")"
if [ -x "$_entire_hook_dir/prepare-commit-msg.pre-entire" ]; then
    "$_entire_hook_dir/prepare-commit-msg.pre-entire" "$@"
fi
