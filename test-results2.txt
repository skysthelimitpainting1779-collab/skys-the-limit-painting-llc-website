TAP version 13
# ACTIVE PREVENTION (mandatory ΓÇö not optional reading): Do not repeat recorded failures. PreToolUse hooks DENY known-bad patterns. Policy: iron law 12 ROOT_CAUSE + iron law 14 active prevention.
# > Generated: 2026-07-13T17:51:32.852Z ┬╖ > **This is not a log.** Agents must OBEY these lessons. Hooks may DENY tool calls that repeat them. ┬╖ \#\# Iron laws (always) ┬╖ 1. **Root cause only** ΓÇö never soft-skip, `|| true`, or disable checks as the fix (`.agents/governance/ROOT_CAUSE.md`). ┬╖ 2. **Windows bash** ΓÇö Git\\bin before System32; fix: `npm run hooks:fix-bash-path`; verify `where bash`. ┬╖ 3. **PowerShell** ΓÇö prefer `node scripts/...` or `.ps1`; no nested `powershell -Command` soup. ┬╖ 4. **Next.js** ΓÇö never `next/dynamic` + `{ ssr: false }` in Server Components. ┬╖ 5. **Learn ΓåÆ enforce** ΓÇö after a failure: record, then hard-guard if machine-detectable. ┬╖ \#\# Top lessons (obey) ┬╖ | ID | Status | Lesson | ┬╖ |----|--------|--------| ┬╖ | ERR-20260710-87d7 | resolved | Semgrep Guardian blocks Write/Edit when not logged in. Log into guardian MCP for real scans, or accept non-blocking policy with explicit prΓÇª | ┬╖ | ERR-20260710-1770 | resolved | After Machine PATH fix, fully restart IDE/agent shells. agentos:health uses registry PATH for bash.ok. Stale process PATH is expected untilΓÇª | ┬╖ | ERR-20260710-28e9 | resolved | ROOT CAUSE: bare bash was WSL System32 before Git. Fix: Admin scripts/fix-windows-bash-path.ps1 prepends Git\\bin to Machine PATH. Verify whΓÇª | ┬╖ | ERR-20260710-4c3b | open | Use npm run learn:prevent:test or JSON file + Get-Content -Raw; never rely on pwsh-escaped JSON pipes for prevention dry-runs | ┬╖ | ERR-20260709-2e26 | resolved | Never use next/dynamic with { ssr: false } inside Server Components. Import client leaves statically when browser work is already behind usΓÇª | ┬╖ | ERR-20260709-070a | resolved | On Windows, prefer `node scripts/...` over nested powershell -Command. Never assign values to switch parameters. Use .ps1 files for multi-sΓÇª | ┬╖ \#\# Hard-blocked patterns (PreToolUse ΓÇö cannot soft-skip) ┬╖ - `next/dynamic` + `ssr: false` in TS/JS ΓåÆ **deny** ┬╖ - Nested / broken `powershell -Command` ΓåÆ **deny** ┬╖ - System32-bash soft-skip ΓÇ£fixesΓÇ¥ ΓåÆ **deny** ┬╖ - Emoji in `src/**` product source ΓåÆ **deny** ┬╖ - Read/write `GRAPH_REPORT.md` or bulk `graphify-out/wiki` ΓåÆ **deny** ┬╖ - Recreate purged `.agents` bloat (plan/status/dashboard/ΓÇª) ΓåÆ **deny** ┬╖ - `|| true` / soft-skip as the ΓÇ£fixΓÇ¥ in hooks ΓåÆ **deny** ┬╖ - Disable prevention without break-glass ΓåÆ **deny** ┬╖ \#\# Commands ┬╖ ```bash ┬╖ npm run learn:prevent \# print inject text ┬╖ npm run learn:prevent:rebuild \# refresh this file ┬╖ npm run ship:improve \# purge + health + eval loop ┬╖ node scripts/active-prevention.mjs check < payload.json ┬╖ ```
# OBEY NOW: ERR-20260710-87d7: Semgrep Guardian blocks Write/Edit when not logged in. Log into guardian MCP for real scans, or accΓÇª | ERR-20260710-1770: After Machine PATH fix, fully restart IDE/agent shells. agentos:health uses registry PATH for bash.ΓÇª | ERR-20260710-28e9: ROOT CAUSE: bare bash was WSL System32 before Git. Fix: Admin scripts/fix-windows-bash-path.ps1 preΓÇª | ERR-20260710-4c3b: Use npm run learn:prevent:test or JSON file + Get-Content -Raw; never rely on pwsh-escaped JSON pipΓÇª | ERR-20260709-2e26: Never use next/dynamic with { ssr: false } inside Server Components. Import client leaves staticallΓÇª
# Subtest: selfTest suite passes (hard guards)
ok 1 - selfTest suite passes (hard guards)
  ---
  duration_ms: 22.0818
  type: 'test'
  ...
# Subtest: denies next/dynamic ssr:false writes
ok 2 - denies next/dynamic ssr:false writes
  ---
  duration_ms: 2.0593
  type: 'test'
  ...
# Subtest: denies nested powershell -Command
ok 3 - denies nested powershell -Command
  ---
  duration_ms: 1.1622
  type: 'test'
  ...
# Subtest: allows clean node commands
ok 4 - allows clean node commands
  ---
  duration_ms: 7.2346
  type: 'test'
  ...
# Subtest: denies emoji in src product files
ok 5 - denies emoji in src product files
  ---
  duration_ms: 1.1527
  type: 'test'
  ...
# Subtest: denies GRAPH_REPORT read dumps
ok 6 - denies GRAPH_REPORT read dumps
  ---
  duration_ms: 0.8568
  type: 'test'
  ...
# Subtest: denies recreating purged bloat
ok 7 - denies recreating purged bloat
  ---
  duration_ms: 0.9838
  type: 'test'
  ...
# Subtest: SOFT env cannot override deny
ok 8 - SOFT env cannot override deny
  ---
  duration_ms: 1.1164
  type: 'test'
  ...
# Subtest: preToolHookOutput returns permissionDecision deny JSON
ok 9 - preToolHookOutput returns permissionDecision deny JSON
  ---
  duration_ms: 3.2068
  type: 'test'
  ...
# Subtest: rebuild + inject produce non-empty active context
ok 10 - rebuild + inject produce non-empty active context
  ---
  duration_ms: 160.6027
  type: 'test'
  ...
# Subtest: loadLessons filters synthetic incidents
ok 11 - loadLessons filters synthetic incidents
  ---
  duration_ms: 1.6274
  type: 'test'
  ...
# Subtest: exports a versioned bulletproof core
ok 12 - exports a versioned bulletproof core
  ---
  duration_ms: 2.0705
  type: 'test'
  ...
# Subtest: normalizeTask fills safe defaults
ok 13 - normalizeTask fills safe defaults
  ---
  duration_ms: 1.3873
  type: 'test'
  ...
# Subtest: isTaskRunnable blocks verified/blocked and over-attempted failed
ok 14 - isTaskRunnable blocks verified/blocked and over-attempted failed
  ---
  duration_ms: 0.5174
  type: 'test'
  ...
# Subtest: pickNextTask prefers high priority and ignores blocked
ok 15 - pickNextTask prefers high priority and ignores blocked
  ---
  duration_ms: 0.5027
  type: 'test'
  ...
# Subtest: safePhaseResumeIndex recovers from unknown phase
ok 16 - safePhaseResumeIndex recovers from unknown phase
  ---
  duration_ms: 0.4148
  type: 'test'
  ...
# Subtest: shouldQuarantineTask at max attempts
ok 17 - shouldQuarantineTask at max attempts
  ---
  duration_ms: 0.3022
  type: 'test'
  ...
# Subtest: assertPhaseEntry auto-seeds when checkpoint missing
ok 18 - assertPhaseEntry auto-seeds when checkpoint missing
  ---
  duration_ms: 8.2649
  type: 'test'
  ...
# Subtest: idempotencyKey is stable
ok 19 - idempotencyKey is stable
  ---
  duration_ms: 1.4129
  type: 'test'
  ...
# Subtest: bumpMetric never NaNs
ok 20 - bumpMetric never NaNs
  ---
  duration_ms: 0.7256
  type: 'test'
  ...
# Subtest: agent-os.js is a zero-theater kernel wired to core only
ok 21 - agent-os.js is a zero-theater kernel wired to core only
  ---
  duration_ms: 1.3586
  type: 'test'
  ...
# [agent-os-store] backend=json (.agents/hub_db.json)
# Subtest: json backend round-trips hub document
ok 22 - json backend round-trips hub document
  ---
  duration_ms: 589.8019
  type: 'test'
  ...
# [agent-os-store] backend=turso url=file:C:/Users/JOHNNY~1/AppData/Local/Temp/agent-ΓÇª
# [agent-os-store] backend=turso url=file:C:/Users/JOHNNY~1/AppData/Local/Temp/agent-ΓÇª
# Subtest: file: libsql backend works offline as Turso-compatible store
ok 23 - file: libsql backend works offline as Turso-compatible store
  ---
  duration_ms: 53.6924
  type: 'test'
  ...
# Subtest: agent-os.js CLI wires store command (no turso theater CLI)
ok 24 - agent-os.js CLI wires store command (no turso theater CLI)
  ---
  duration_ms: 1.1074
  type: 'test'
  ...
# Subtest: agent OS keeps quarantine self-heal wiring (no git checkout rollback)
ok 25 - agent OS keeps quarantine self-heal wiring (no git checkout rollback)
  ---
  duration_ms: 3.314
  type: 'test'
  ...
# Subtest: agent OS self-healing quarantines failures instead of reverting user files
ok 26 - agent OS self-healing quarantines failures instead of reverting user files
  ---
  duration_ms: 0.6481
  type: 'test'
  ...
# Subtest: zero-theater kernel: only real host-native paths
ok 27 - zero-theater kernel: only real host-native paths
  ---
  duration_ms: 2.998
  type: 'test'
  ...
# Subtest: host-native specialists compiled for Claude Cursor Codex
ok 28 - host-native specialists compiled for Claude Cursor Codex
  ---
  duration_ms: 12.8705
  type: 'test'
  ...
# Subtest: api/leads - asText utility
    # Subtest: returns trimmed string for string input
    ok 1 - returns trimmed string for string input
      ---
      duration_ms: 6.0003
      type: 'test'
      ...
    # Subtest: returns empty string for null
    ok 2 - returns empty string for null
      ---
      duration_ms: 0.2827
      type: 'test'
      ...
    # Subtest: returns empty string for undefined
    ok 3 - returns empty string for undefined
      ---
      duration_ms: 0.212
      type: 'test'
      ...
    # Subtest: returns empty string for number
    ok 4 - returns empty string for number
      ---
      duration_ms: 1.0083
      type: 'test'
      ...
    # Subtest: returns empty string for boolean
    ok 5 - returns empty string for boolean
      ---
      duration_ms: 0.2397
      type: 'test'
      ...
    # Subtest: returns empty string for object
    ok 6 - returns empty string for object
      ---
      duration_ms: 0.3762
      type: 'test'
      ...
    # Subtest: returns empty string for array
    ok 7 - returns empty string for array
      ---
      duration_ms: 0.2264
      type: 'test'
      ...
    # Subtest: preserves internal whitespace
    ok 8 - preserves internal whitespace
      ---
      duration_ms: 0.3235
      type: 'test'
      ...
    # Subtest: handles empty string
    ok 9 - handles empty string
      ---
      duration_ms: 0.4336
      type: 'test'
      ...
    # Subtest: handles whitespace-only string
    ok 10 - handles whitespace-only string
      ---
      duration_ms: 0.5575
      type: 'test'
      ...
    1..10
ok 29 - api/leads - asText utility
  ---
  duration_ms: 12.5726
  type: 'suite'
  ...
# Subtest: api/leads - isPayload guard
    # Subtest: returns true for plain object
    ok 1 - returns true for plain object
      ---
      duration_ms: 0.5604
      type: 'test'
      ...
    # Subtest: returns true for empty object
    ok 2 - returns true for empty object
      ---
      duration_ms: 0.3869
      type: 'test'
      ...
    # Subtest: returns false for null
    ok 3 - returns false for null
      ---
      duration_ms: 0.1929
      type: 'test'
      ...
    # Subtest: returns false for undefined
    ok 4 - returns false for undefined
      ---
      duration_ms: 0.1648
      type: 'test'
      ...
    # Subtest: returns false for array
    ok 5 - returns false for array
      ---
      duration_ms: 0.1741
      type: 'test'
      ...
    # Subtest: returns false for string
    ok 6 - returns false for string
      ---
      duration_ms: 0.4068
      type: 'test'
      ...
    # Subtest: returns false for number
    ok 7 - returns false for number
      ---
      duration_ms: 0.1794
      type: 'test'
      ...
    # Subtest: returns false for boolean false
    ok 8 - returns false for boolean false
      ---
      duration_ms: 0.248
      type: 'test'
      ...
    # Subtest: returns false for empty string
    ok 9 - returns false for empty string
      ---
      duration_ms: 0.1965
      type: 'test'
      ...
    # Subtest: returns false for zero
    ok 10 - returns false for zero
      ---
      duration_ms: 0.2021
      type: 'test'
      ...
    1..10
ok 30 - api/leads - isPayload guard
  ---
  duration_ms: 3.4657
  type: 'suite'
  ...
# Subtest: api/leads - escapeHtml sanitization
    # Subtest: escapes ampersand
    ok 1 - escapes ampersand
      ---
      duration_ms: 0.4781
      type: 'test'
      ...
    # Subtest: escapes less-than
    ok 2 - escapes less-than
      ---
      duration_ms: 0.3756
      type: 'test'
      ...
    # Subtest: escapes greater-than
    ok 3 - escapes greater-than
      ---
      duration_ms: 0.5155
      type: 'test'
      ...
    # Subtest: escapes double quotes
    ok 4 - escapes double quotes
      ---
      duration_ms: 0.1821
      type: 'test'
      ...
    # Subtest: escapes single quotes
    ok 5 - escapes single quotes
      ---
      duration_ms: 1.2837
      type: 'test'
      ...
    # Subtest: escapes all special characters in combination
    ok 6 - escapes all special characters in combination
      ---
      duration_ms: 0.2076
      type: 'test'
      ...
    # Subtest: passes through safe strings unchanged
    ok 7 - passes through safe strings unchanged
      ---
      duration_ms: 0.1529
      type: 'test'
      ...
    # Subtest: converts non-string values to string first
    ok 8 - converts non-string values to string first
      ---
      duration_ms: 2.0854
      type: 'test'
      ...
    # Subtest: handles empty string
    ok 9 - handles empty string
      ---
      duration_ms: 0.4204
      type: 'test'
      ...
    # Subtest: handles null (converts to string)
    ok 10 - handles null (converts to string)
      ---
      duration_ms: 0.172
      type: 'test'
      ...
    # Subtest: handles undefined (converts to string)
    ok 11 - handles undefined (converts to string)
      ---
      duration_ms: 0.2243
      type: 'test'
      ...
    1..11
ok 31 - api/leads - escapeHtml sanitization
  ---
  duration_ms: 7.2117
  type: 'suite'
  ...
# Subtest: api/leads - getSafeValue prototype pollution guard
    # Subtest: returns value for normal key
    ok 1 - returns value for normal key
      ---
      duration_ms: 0.3686
      type: 'test'
      ...
    # Subtest: returns undefined for __proto__
    ok 2 - returns undefined for __proto__
      ---
      duration_ms: 0.1633
      type: 'test'
      ...
    # Subtest: returns undefined for constructor
    ok 3 - returns undefined for constructor
      ---
      duration_ms: 0.1681
      type: 'test'
      ...
    # Subtest: returns undefined for prototype
    ok 4 - returns undefined for prototype
      ---
      duration_ms: 0.1568
      type: 'test'
      ...
    # Subtest: returns undefined for missing key
    ok 5 - returns undefined for missing key
      ---
      duration_ms: 0.1479
      type: 'test'
      ...
    # Subtest: returns falsy values correctly
    ok 6 - returns falsy values correctly
      ---
      duration_ms: 0.1735
      type: 'test'
      ...
    1..6
ok 32 - api/leads - getSafeValue prototype pollution guard
  ---
  duration_ms: 1.5867
  type: 'suite'
  ...
# Subtest: api/leads - buildLeadId generation
    # Subtest: starts with SKY- prefix
    ok 1 - starts with SKY- prefix
      ---
      duration_ms: 1.584
      type: 'test'
      ...
    # Subtest: matches expected format SKY-TIMESTAMP-RANDOM
    ok 2 - matches expected format SKY-TIMESTAMP-RANDOM
      ---
      duration_ms: 0.4916
      type: 'test'
      ...
    # Subtest: generates unique IDs
    ok 3 - generates unique IDs
      ---
      duration_ms: 1.0194
      type: 'test'
      ...
    # Subtest: timestamp portion is 14 digits
    ok 4 - timestamp portion is 14 digits
      ---
      duration_ms: 0.2586
      type: 'test'
      ...
    # Subtest: ManyChat lead ID starts with SKY-MC-
    ok 5 - ManyChat lead ID starts with SKY-MC-
      ---
      duration_ms: 0.2875
      type: 'test'
      ...
    # Subtest: ManyChat lead ID matches expected format
    ok 6 - ManyChat lead ID matches expected format
      ---
      duration_ms: 0.2419
      type: 'test'
      ...
    1..6
ok 33 - api/leads - buildLeadId generation
  ---
  duration_ms: 4.4579
  type: 'suite'
  ...
# Subtest: api/leads - validate lead payload
    # Subtest: returns empty string for valid payload
    ok 1 - returns empty string for valid payload
      ---
      duration_ms: 0.6383
      type: 'test'
      ...
    # Subtest: returns error when name is missing
    ok 2 - returns error when name is missing
      ---
      duration_ms: 0.2487
      type: 'test'
      ...
    # Subtest: returns error when email is missing
    ok 3 - returns error when email is missing
      ---
      duration_ms: 0.2197
      type: 'test'
      ...
    # Subtest: returns error for invalid email format
    ok 4 - returns error for invalid email format
      ---
      duration_ms: 0.2248
      type: 'test'
      ...
    # Subtest: returns error for email without domain
    ok 5 - returns error for email without domain
      ---
      duration_ms: 0.1787
      type: 'test'
      ...
    # Subtest: returns error for email with spaces
    ok 6 - returns error for email with spaces
      ---
      duration_ms: 0.1625
      type: 'test'
      ...
    # Subtest: accepts valid email formats
    ok 7 - accepts valid email formats
      ---
      duration_ms: 0.2071
      type: 'test'
      ...
    # Subtest: returns spam error when website honeypot is filled
    ok 8 - returns spam error when website honeypot is filled
      ---
      duration_ms: 0.1686
      type: 'test'
      ...
    # Subtest: returns error for multiple missing fields
    ok 9 - returns error for multiple missing fields
      ---
      duration_ms: 0.2029
      type: 'test'
      ...
    # Subtest: accepts payload with valid photosUrl
    ok 10 - accepts payload with valid photosUrl
      ---
      duration_ms: 0.1987
      type: 'test'
      ...
    # Subtest: returns error for photosUrl with invalid protocol
    ok 11 - returns error for photosUrl with invalid protocol
      ---
      duration_ms: 0.2894
      type: 'test'
      ...
    # Subtest: returns error for photosUrl that is not a URL
    ok 12 - returns error for photosUrl that is not a URL
      ---
      duration_ms: 0.2307
      type: 'test'
      ...
    # Subtest: returns error for photosUrl without valid hostname
    ok 13 - returns error for photosUrl without valid hostname
      ---
      duration_ms: 0.2002
      type: 'test'
      ...
    # Subtest: allows payload without photosUrl
    ok 14 - allows payload without photosUrl
      ---
      duration_ms: 0.1598
      type: 'test'
      ...
    # Subtest: trims whitespace from field values during validation
    ok 15 - trims whitespace from field values during validation
      ---
      duration_ms: 0.164
      type: 'test'
      ...
    1..15
ok 34 - api/leads - validate lead payload
  ---
  duration_ms: 4.2211
  type: 'suite'
  ...
# Subtest: api/leads - buildLeadHtml email template
    # Subtest: generates HTML with header
    ok 1 - generates HTML with header
      ---
      duration_ms: 0.5367
      type: 'test'
      ...
    # Subtest: generates table rows for non-empty fields
    ok 2 - generates table rows for non-empty fields
      ---
      duration_ms: 0.2157
      type: 'test'
      ...
    # Subtest: excludes website honeypot field
    ok 3 - excludes website honeypot field
      ---
      duration_ms: 0.1774
      type: 'test'
      ...
    # Subtest: excludes empty fields
    ok 4 - excludes empty fields
      ---
      duration_ms: 0.1864
      type: 'test'
      ...
    # Subtest: escapes HTML in values to prevent XSS
    ok 5 - escapes HTML in values to prevent XSS
      ---
      duration_ms: 0.275
      type: 'test'
      ...
    # Subtest: escapes HTML in keys
    ok 6 - escapes HTML in keys
      ---
      duration_ms: 0.2103
      type: 'test'
      ...
    1..6
ok 35 - api/leads - buildLeadHtml email template
  ---
  duration_ms: 1.9434
  type: 'suite'
  ...
# Subtest: api/leads - Rate limiter
    # Subtest: allows first request from an IP
    ok 1 - allows first request from an IP
      ---
      duration_ms: 0.4044
      type: 'test'
      ...
    # Subtest: allows up to max requests
    ok 2 - allows up to max requests
      ---
      duration_ms: 0.1899
      type: 'test'
      ...
    # Subtest: blocks requests beyond max
    ok 3 - blocks requests beyond max
      ---
      duration_ms: 2.5203
      type: 'test'
      ...
    # Subtest: tracks IPs independently
    ok 4 - tracks IPs independently
      ---
      duration_ms: 0.2803
      type: 'test'
      ...
    # Subtest: resets counter after window expires
    ok 5 - resets counter after window expires
      ---
      duration_ms: 4.3637
      type: 'test'
      ...
    1..5
ok 36 - api/leads - Rate limiter
  ---
  duration_ms: 8.1185
  type: 'suite'
  ...
# Subtest: data/markets - Market definitions
    # Subtest: exports exactly three markets
    ok 1 - exports exactly three markets
      ---
      duration_ms: 2.2697
      type: 'test'
      ...
    # Subtest: MarketSlug type includes all three market slugs
    ok 2 - MarketSlug type includes all three market slugs
      ---
      duration_ms: 5.276
      type: 'test'
      ...
    # Subtest: each market has required navigation label
    ok 3 - each market has required navigation label
      ---
      duration_ms: 2.6827
      type: 'test'
      ...
    # Subtest: markets have sequential numbering
    ok 4 - markets have sequential numbering
      ---
      duration_ms: 0.5452
      type: 'test'
      ...
    # Subtest: Market interface declares all required fields
    ok 5 - Market interface declares all required fields
      ---
      duration_ms: 0.8091
      type: 'test'
      ...
    # Subtest: each market has capabilities array with title and body
    ok 6 - each market has capabilities array with title and body
      ---
      duration_ms: 0.2941
      type: 'test'
      ...
    # Subtest: each market has process array with title and body
    ok 7 - each market has process array with title and body
      ---
      duration_ms: 0.3867
      type: 'test'
      ...
    # Subtest: residential market has correct CTA
    ok 8 - residential market has correct CTA
      ---
      duration_ms: 0.3114
      type: 'test'
      ...
    # Subtest: commercial market has correct CTA
    ok 9 - commercial market has correct CTA
      ---
      duration_ms: 0.3808
      type: 'test'
      ...
    # Subtest: public-sector market has correct CTA
    ok 10 - public-sector market has correct CTA
      ---
      duration_ms: 0.6513
      type: 'test'
      ...
    # Subtest: commercial market references NAICS Code 238320
    ok 11 - commercial market references NAICS Code 238320
      ---
      duration_ms: 0.2355
      type: 'test'
      ...
    # Subtest: public-sector market references contractor registration
    ok 12 - public-sector market references contractor registration
      ---
      duration_ms: 0.1749
      type: 'test'
      ...
    # Subtest: marketBySlug is derived from markets array via reduce
    ok 13 - marketBySlug is derived from markets array via reduce
      ---
      duration_ms: 0.2037
      type: 'test'
      ...
    # Subtest: trustPillars array exports four items
    ok 14 - trustPillars array exports four items
      ---
      duration_ms: 0.3297
      type: 'test'
      ...
    # Subtest: trustPillars includes contractor registration pillar
    ok 15 - trustPillars includes contractor registration pillar
      ---
      duration_ms: 0.1726
      type: 'test'
      ...
    # Subtest: trustPillars includes owner-operated pillar
    ok 16 - trustPillars includes owner-operated pillar
      ---
      duration_ms: 0.1638
      type: 'test'
      ...
    # Subtest: trustPillars includes insurance coverage pillar
    ok 17 - trustPillars includes insurance coverage pillar
      ---
      duration_ms: 0.1743
      type: 'test'
      ...
    # Subtest: trustPillars includes scope clarity pillar
    ok 18 - trustPillars includes scope clarity pillar
      ---
      duration_ms: 0.1676
      type: 'test'
      ...
    # Subtest: supportingImages exports correct image paths
    ok 19 - supportingImages exports correct image paths
      ---
      duration_ms: 0.3582
      type: 'test'
      ...
    # Subtest: all market images use .webp extension
    ok 20 - all market images use .webp extension
      ---
      duration_ms: 0.3401
      type: 'test'
      ...
    # Subtest: all market heroImages use .webp extension
    ok 21 - all market heroImages use .webp extension
      ---
      duration_ms: 0.2511
      type: 'test'
      ...
    # Subtest: each market has exactly 3 capabilities
    ok 22 - each market has exactly 3 capabilities
      ---
      duration_ms: 0.3562
      type: 'test'
      ...
    # Subtest: each market has exactly 4 process steps
    ok 23 - each market has exactly 4 process steps
      ---
      duration_ms: 0.3567
      type: 'test'
      ...
    # Subtest: each market has at least 3 proof points
    ok 24 - each market has at least 3 proof points
      ---
      duration_ms: 0.3993
      type: 'test'
      ...
    # Subtest: metaTitle fields include company name
    ok 25 - metaTitle fields include company name
      ---
      duration_ms: 0.2662
      type: 'test'
      ...
    1..25
ok 37 - data/markets - Market definitions
  ---
  duration_ms: 20.9899
  type: 'suite'
  ...
# Subtest: data/landingPages - Area landing pages
    # Subtest: LandingPageKind type covers service and area
    ok 1 - LandingPageKind type covers service and area
      ---
      duration_ms: 0.5946
      type: 'test'
      ...
    # Subtest: LandingPage interface declares all required fields
    ok 2 - LandingPage interface declares all required fields
      ---
      duration_ms: 0.6888
      type: 'test'
      ...
    # Subtest: neighborhoods field is optional
    ok 3 - neighborhoods field is optional
      ---
      duration_ms: 0.1727
      type: 'test'
      ...
    # Subtest: area landing pages cover all 7 expected service areas
    ok 4 - area landing pages cover all 7 expected service areas
      ---
      duration_ms: 0.5163
      type: 'test'
      ...
    # Subtest: all area pages have kind set to area
    ok 5 - all area pages have kind set to area
      ---
      duration_ms: 0.3581
      type: 'test'
      ...
    # Subtest: inver-grove-heights has neighborhoods
    ok 6 - inver-grove-heights has neighborhoods
      ---
      duration_ms: 0.1721
      type: 'test'
      ...
    # Subtest: each area page has at least 3 proof points
    ok 7 - each area page has at least 3 proof points
      ---
      duration_ms: 0.2673
      type: 'test'
      ...
    # Subtest: each area page has at least 3 process steps
    ok 8 - each area page has at least 3 process steps
      ---
      duration_ms: 0.2624
      type: 'test'
      ...
    # Subtest: each area page has related slugs array
    ok 9 - each area page has related slugs array
      ---
      duration_ms: 0.2567
      type: 'test'
      ...
    1..9
ok 38 - data/landingPages - Area landing pages
  ---
  duration_ms: 4.3038
  type: 'suite'
  ...
# Subtest: data/landingPages - Service landing pages
    # Subtest: service landing pages cover all expected services
    ok 1 - service landing pages cover all expected services
      ---
      duration_ms: 0.6921
      type: 'test'
      ...
    # Subtest: all service pages have kind set to service
    ok 2 - all service pages have kind set to service
      ---
      duration_ms: 0.2461
      type: 'test'
      ...
    # Subtest: market field uses only valid market labels
    ok 3 - market field uses only valid market labels
      ---
      duration_ms: 0.505
      type: 'test'
      ...
    # Subtest: all landing page images use .webp extension
    ok 4 - all landing page images use .webp extension
      ---
      duration_ms: 0.2334
      type: 'test'
      ...
    # Subtest: no duplicate slugs across all landing pages
    ok 5 - no duplicate slugs across all landing pages
      ---
      duration_ms: 0.3179
      type: 'test'
      ...
    1..5
ok 39 - data/landingPages - Service landing pages
  ---
  duration_ms: 2.3172
  type: 'suite'
  ...
# Subtest: data/landingPages - Utility functions
    # Subtest: landingPagePath generates area paths correctly
    ok 1 - landingPagePath generates area paths correctly
      ---
      duration_ms: 0.2934
      type: 'test'
      ...
    # Subtest: landingPagePath generates service paths correctly
    ok 2 - landingPagePath generates service paths correctly
      ---
      duration_ms: 0.1636
      type: 'test'
      ...
    # Subtest: landingPageByKindAndSlug returns undefined for missing slug
    ok 3 - landingPageByKindAndSlug returns undefined for missing slug
      ---
      duration_ms: 0.1797
      type: 'test'
      ...
    # Subtest: landingPageBySlug searches combined landingPages array
    ok 4 - landingPageBySlug searches combined landingPages array
      ---
      duration_ms: 0.1563
      type: 'test'
      ...
    # Subtest: landingPages combines area and service arrays
    ok 5 - landingPages combines area and service arrays
      ---
      duration_ms: 0.3799
      type: 'test'
      ...
    # Subtest: landingPageByKindAndSlug selects correct collection by kind
    ok 6 - landingPageByKindAndSlug selects correct collection by kind
      ---
      duration_ms: 0.1828
      type: 'test'
      ...
    1..6
ok 40 - data/landingPages - Utility functions
  ---
  duration_ms: 1.7155
  type: 'suite'
  ...
# Subtest: Cross-module data consistency
    # Subtest: all market slugs from markets.ts are referenced in landing page related arrays
    ok 1 - all market slugs from markets.ts are referenced in landing page related arrays
      ---
      duration_ms: 0.3098
      type: 'test'
      ...
    # Subtest: area landing page slugs match cities in seo.ts areaServed
    ok 2 - area landing page slugs match cities in seo.ts areaServed
      ---
      duration_ms: 0.7039
      type: 'test'
      ...
    # Subtest: settings service_areas match seo.ts areaServed cities
    ok 3 - settings service_areas match seo.ts areaServed cities
      ---
      duration_ms: 0.6818
      type: 'test'
      ...
    # Subtest: contact phone matches seo phone
    ok 4 - contact phone matches seo phone
      ---
      duration_ms: 0.9337
      type: 'test'
      ...
    # Subtest: contact email matches seo email
    ok 5 - contact email matches seo email
      ---
      duration_ms: 4.5708
      type: 'test'
      ...
    1..5
ok 41 - Cross-module data consistency
  ---
  duration_ms: 7.6612
  type: 'suite'
  ...
# Subtest: filterPublishedCaseStudies drops drafts
ok 42 - filterPublishedCaseStudies drops drafts
  ---
  duration_ms: 4.7327
  type: 'test'
  ...
# Subtest: directusAssetUrl builds asset path
ok 43 - directusAssetUrl builds asset path
  ---
  duration_ms: 0.4886
  type: 'test'
  ...
# [CMS] case_studies fetch failed, returning empty: fetch failed
# [CMS] markets fetch failed, returning empty: fetch failed
# [CMS] site_config fetch failed, returning null: fetch failed
# Subtest: getCaseStudies uses real SDK against stub Directus REST
ok 44 - getCaseStudies uses real SDK against stub Directus REST
  ---
  duration_ms: 120.8937
  type: 'test'
  ...
# Subtest: getCaseStudies returns empty on CMS down (graceful fallback)
ok 45 - getCaseStudies returns empty on CMS down (graceful fallback)
  ---
  duration_ms: 2.5082
  type: 'test'
  ...
# Subtest: getMarkets empty when unreachable
ok 46 - getMarkets empty when unreachable
  ---
  duration_ms: 2.1376
  type: 'test'
  ...
# Subtest: getSiteConfig null when unreachable
ok 47 - getSiteConfig null when unreachable
  ---
  duration_ms: 5.6983
  type: 'test'
  ...
# Subtest: probeDirectusHealth hits stub /server/health
ok 48 - probeDirectusHealth hits stub /server/health
  ---
  duration_ms: 22.8865
  type: 'test'
  ...
# Subtest: Tier 1: Feature Coverage
    # Subtest: T1.1 Routing & Navigation - Core layout components render successfully
    ok 1 - T1.1 Routing & Navigation - Core layout components render successfully
      ---
      duration_ms: 7.3898
      type: 'test'
      ...
    # Subtest: T1.2 Routing & Navigation - Header navigation links exist
    not ok 2 - T1.2 Routing & Navigation - Header navigation links exist
      ---
      duration_ms: 5.0309
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:35:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /to="\/residential"/. Input:
        
        "'use client';\r\n" +
          '\r\n' +
          "import React, { useState, useEffect } from 'react';\n" +
          "import Link from 'next/link';\n" +
          "import Image from 'next/image';\n" +
          "import { usePathname } from 'next/navigation';\r\n" +
          "import { Menu, X, Phone, PaintRoller, ChevronDown, Brush, Warehouse } from 'lucide-react';\r\n" +
          'import { \r\n' +
          '  Sheet, \r\n' +
          '  SheetContent, \r\n' +
          '  SheetTrigger, \r\n' +
          '  SheetHeader,\r\n' +
          '  SheetTitle,\r\n' +
          '  SheetDescription\r\n' +
          "} from '@/components/ui/sheet';\r\n" +
          "import { motion, AnimatePresence } from 'motion/react';\r\n" +
          "import { cn } from '@/lib/utils';\r\n" +
          '\r\n' +
          'export default function ConversionHeader() {\r\n' +
          '  const [isScrolled, setIsScrolled] = useState(false);\r\n' +
          '  const [isServicesOpen, setIsServicesOpen] = useState(false);\r\n' +
          '  const pathname = usePathname();\r\n' +
          '\r\n' +
          '  useEffect(() => {\r\n' +
          '    const handleScroll = () => {\r\n' +
          '      setIsScrolled(window.scrollY > 20);\r\n' +
          '    };\r\n' +
          "    window.addEventListener('scroll', handleScroll);\r\n" +
          "    return () => window.removeEventListener('scroll', handleScroll);\r\n" +
          '  }, []);\r\n' +
          '\r\n' +
          '  // Close dropdown on route change\r\n' +
          '  useEffect(() => {\r\n' +
          '    setIsServicesOpen(false);\r\n' +
          '  }, [pathname]);\r\n' +
          '\r\n' +
          '  const navClass = cn(\r\n' +
          '    "fixed top-0 left-0 right-0 z-50 transition-all duration-300",\r\n' +
          '    isScrolled \r\n' +
          '      ? "bg-[#0A0A0A]/95 backdrop-blur-xl border-b border-zinc-800 py-3" \n' +
          '      : "bg-transparent py-5"\r\n' +
          '  );\r\n' +
          '\r\n' +
          '  const containerVariants = {\r\n' +
          '    hidden: { opacity: 0, y: -5 },\r\n' +
          '    show: {\r\n' +
          '      opacity: 1,\r\n' +
          '      y: 0,\r\n' +
          '      transition: {\r\n' +
          '        staggerChildren: 0.05,\r\n' +
          '        duration: 0.2,\r\n' +
          '      }\r\n' +
          '    },\r\n' +
          '    exit: {\r\n' +
          '      opacity: 0,\r\n' +
          '      y: -5,\r\n' +
          '      transition: { duration: 0.15 }\r\n' +
          '    }\r\n' +
          '  };\r\n' +
          '\r\n' +
          '  const itemVariants = {\r\n' +
          '    hidden: { opacity: 0, x: -10 },\r\n' +
          '    show: { opacity: 1, x: 0, transition: { type: "spring", stiffness: 300, damping: 24 } as any }\r\n' +
          '  };\r\n' +
          '\r\n' +
          '  return (\r\n' +
          '    <header className={navClass}>\r\n' +
          '      <div className="container-page mx-auto">\r\n' +
          '        <div className="flex items-center justify-between">\r\n' +
          '          \r\n' +
          '          {/* Logo */}\r\n' +
          '          <Link href="/" className="flex items-center gap-3 group">\r\n' +
          '            <div className="flex h-10 w-10 shrink-0 items-center justify-center border border-zinc-700 bg-[#0A0A0A] u-transition group-hover:border-[#FF5A00]">\n' +
          `              <Image src="/brand/SkyLLP_BrandLogo.svg" alt="Sky's the Limit Painting" width={24} height={24} priority />\n` +
          '            </div>\r\n' +
          '            <div className="hidden sm:block">\r\n' +
          `              <span className="font-display font-bold text-lg leading-none tracking-tight block text-white">SKY'S THE LIMIT</span>\r\n` +
          '              <span className="eyebrow block mt-0.5 text-[#FF5A00]">PAINTING</span>\r\n' +
          '            </div>\r\n' +
          '          </Link>\r\n' +
          '\r\n' +
          '          {/* Desktop Nav */}\r\n' +
          '          <nav className="hidden lg:flex items-center gap-8">\r\n' +
          '            <div className="relative" onMouseLeave={() => setIsServicesOpen(false)}>\r\n' +
          '              <button \r\n' +
          '                className={cn(\r\n' +
          '                  "flex items-center gap-1 text-sm font-bold tracking-wider uppercase transition-colors py-2",\r\n' +
          `                  isServicesOpen || pathname.includes('/painting-services') ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]"\r\n` +
          '                )}\r\n' +
          '                onMouseEnter={() => setIsServicesOpen(true)}\r\n' +
          '                onClick={() => setIsServicesOpen(!isServicesOpen)}\r\n' +
          '              >\r\n' +
          '                Services <ChevronDown size={14} className={cn("transition-transform duration-200", isServicesOpen && "rotate-180")} />\r\n' +
          '              </button>\r\n' +
          '\r\n' +
          '              <AnimatePresence>\r\n' +
          '                {isServicesOpen && (\r\n' +
          '                  <motion.div \r\n' +
          '                    variants={containerVariants}\r\n' +
          '                    initial="hidden"\r\n' +
          '                    animate="show"\r\n' +
          '                    exit="exit"\r\n' +
          '                    className="absolute top-full left-1/2 -translate-x-1/2 mt-2 w-[480px] bg-[#0B0B0D] border border-white/10 shadow-2xl p-4 grid grid-cols-2 gap-2"\r\n' +
          '                  >\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/interior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Interior Painting</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Walls, trim, ceilings, & prep.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/exterior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><Brush size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Exterior Painting</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Siding, brick, stucco, & decks.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/cabinet-refinishing" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Cabinet Refinishing</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Factory-finish transformations.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/commercial" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><Warehouse size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Commercial</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">HOA, property mgmt, retail.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '                  </motion.div>\r\n' +
          '                )}\r\n' +
          '              </AnimatePresence>\r\n' +
          '            </div>\r\n' +
          '\r\n' +
          '            <Link href="/projects" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/projects" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              Projects\r\n' +
          '            </Link>\r\n' +
          '            <Link href="/service-area" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/service-area" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              Service Area\r\n' +
          '            </Link>\r\n' +
          '            <Link href="/about" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/about" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              About Us\r\n' +
          '            </Link>\r\n' +
          '          </nav>\r\n' +
          '\r\n' +
          '          {/* CTA & Mobile Toggle */}\r\n' +
          '          <div className="flex items-center gap-4">\r\n' +
          '            <a href="tel:+16514104196" className="hidden lg:flex items-center gap-2 text-sm font-bold text-white hover:text-[#FF5A00] transition-colors">\n' +
          '              <Phone size={16} />\r\n' +
          '              (651) 410-4196\n' +
          '            </a>\r\n' +
          '            \r\n' +
          '            <Link \r\n' +
          '              href="/estimate" \r\n' +
          '              className="hidden bg-[#FF5A00] px-6 py-2.5 text-sm font-bold uppercase tracking-wider text-white transition-colors hover:bg-[#d94d00] md:inline-flex"\n' +
          '            >\r\n' +
          '              Get Estimate\r\n' +
          '            </Link>\r\n' +
          '\r\n' +
          '            {/* Mobile Menu */}\r\n' +
          '            <Sheet>\r\n' +
          '              <SheetTrigger>\r\n' +
          '                <button className="lg:hidden text-white p-2" aria-label="Open Menu">\r\n' +
          '                  <Menu size={24} />\r\n' +
          '                </button>\r\n' +
          '              </SheetTrigger>\r\n' +
          '              <SheetContent side="right" className="w-full sm:w-[400px] bg-[#050505] border-l border-white/10 p-0">\r\n' +
          '                <SheetHeader className="p-6 border-b border-white/10 text-left">\r\n' +
          '                  <SheetTitle className="text-white font-display text-xl uppercase">Menu</SheetTitle>\r\n' +
          `                  <SheetDescription className="sr-only">Navigate around the Sky's the Limit Painting website.</SheetDescription>\r\n` +
          '                </SheetHeader>\r\n' +
          '                \r\n' +
          '                <div className="p-6 overflow-y-auto h-[calc(100vh-80px)]">\r\n' +
          '                  <nav className="flex flex-col gap-6 text-xl">\r\n' +
          '                    <Link href="/">Home</Link>\r\n' +
          '                    <div className="pt-4 pb-2">\r\n' +
          '                      <p className="eyebrow mb-4">Painting Services</p>\r\n' +
          '                      <div className="flex flex-col gap-4 pl-4 border-l border-white/10">\r\n' +
          '                        <Link href="/painting-services/interior-painting" className="text-lg">Interior Painting</Link>\r\n' +
          '                        <Link href="/painting-services/exterior-painting" className="text-lg">Exterior Painting</Link>\r\n' +
          '                        <Link href="/painting-services/cabinet-refinishing" className="text-lg">Cabinet Refinishing</Link>\r\n' +
          '                        <Link href="/commercial" className="text-lg">Commercial & HOA</Link>\r\n' +
          '                      </div>\r\n' +
          '                    </div>\r\n' +
          '                    <Link href="/projects">Projects</Link>\r\n' +
          '                    <Link href="/service-area">Service Area</Link>\r\n' +
          '                    <Link href="/about">About Us</Link>\r\n' +
          '                    <Link href="/contact">Contact</Link>\r\n' +
          '   '... 755 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import React, { useState, useEffect } from 'react';
        import Link from 'next/link';
        import Image from 'next/image';
        import { usePathname } from 'next/navigation';
        import { Menu, X, Phone, PaintRoller, ChevronDown, Brush, Warehouse } from 'lucide-react';
        import { 
          Sheet, 
          SheetContent, 
          SheetTrigger, 
          SheetHeader,
          SheetTitle,
          SheetDescription
        } from '@/components/ui/sheet';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '@/lib/utils';
        
        export default function ConversionHeader() {
          const [isScrolled, setIsScrolled] = useState(false);
          const [isServicesOpen, setIsServicesOpen] = useState(false);
          const pathname = usePathname();
        
          useEffect(() => {
            const handleScroll = () => {
              setIsScrolled(window.scrollY > 20);
            };
            window.addEventListener('scroll', handleScroll);
            return () => window.removeEventListener('scroll', handleScroll);
          }, []);
        
          // Close dropdown on route change
          useEffect(() => {
            setIsServicesOpen(false);
          }, [pathname]);
        
          const navClass = cn(
            "fixed top-0 left-0 right-0 z-50 transition-all duration-300",
            isScrolled 
              ? "bg-[#0A0A0A]/95 backdrop-blur-xl border-b border-zinc-800 py-3" 
              : "bg-transparent py-5"
          );
        
          const containerVariants = {
            hidden: { opacity: 0, y: -5 },
            show: {
              opacity: 1,
              y: 0,
              transition: {
                staggerChildren: 0.05,
                duration: 0.2,
              }
            },
            exit: {
              opacity: 0,
              y: -5,
              transition: { duration: 0.15 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, x: -10 },
            show: { opacity: 1, x: 0, transition: { type: "spring", stiffness: 300, damping: 24 } as any }
          };
        
          return (
            <header className={navClass}>
              <div className="container-page mx-auto">
                <div className="flex items-center justify-between">
                  
                  {/* Logo */}
                  <Link href="/" className="flex items-center gap-3 group">
                    <div className="flex h-10 w-10 shrink-0 items-center justify-center border border-zinc-700 bg-[#0A0A0A] u-transition group-hover:border-[#FF5A00]">
                      <Image src="/brand/SkyLLP_BrandLogo.svg" alt="Sky's the Limit Painting" width={24} height={24} priority />
                    </div>
                    <div className="hidden sm:block">
                      <span className="font-display font-bold text-lg leading-none tracking-tight block text-white">SKY'S THE LIMIT</span>
                      <span className="eyebrow block mt-0.5 text-[#FF5A00]">PAINTING</span>
                    </div>
                  </Link>
        
                  {/* Desktop Nav */}
                  <nav className="hidden lg:flex items-center gap-8">
                    <div className="relative" onMouseLeave={() => setIsServicesOpen(false)}>
                      <button 
                        className={cn(
                          "flex items-center gap-1 text-sm font-bold tracking-wider uppercase transition-colors py-2",
                          isServicesOpen || pathname.includes('/painting-services') ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]"
                        )}
                        onMouseEnter={() => setIsServicesOpen(true)}
                        onClick={() => setIsServicesOpen(!isServicesOpen)}
                      >
                        Services <ChevronDown size={14} className={cn("transition-transform duration-200", isServicesOpen && "rotate-180")} />
                      </button>
        
                      <AnimatePresence>
                        {isServicesOpen && (
                          <motion.div 
                            variants={containerVariants}
                            initial="hidden"
                            animate="show"
                            exit="exit"
                            className="absolute top-full left-1/2 -translate-x-1/2 mt-2 w-[480px] bg-[#0B0B0D] border border-white/10 shadow-2xl p-4 grid grid-cols-2 gap-2"
                          >
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/interior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Interior Painting</div>
                                  <div className="text-xs text-gray-400 mt-1">Walls, trim, ceilings, & prep.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/exterior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><Brush size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Exterior Painting</div>
                                  <div className="text-xs text-gray-400 mt-1">Siding, brick, stucco, & decks.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/cabinet-refinishing" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Cabinet Refinishing</div>
                                  <div className="text-xs text-gray-400 mt-1">Factory-finish transformations.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/commercial" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><Warehouse size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Commercial</div>
                                  <div className="text-xs text-gray-400 mt-1">HOA, property mgmt, retail.</div>
                                </div>
                              </Link>
                            </motion.div>
                          </motion.div>
                        )}
                      </AnimatePresence>
                    </div>
        
                    <Link href="/projects" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/projects" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      Projects
                    </Link>
                    <Link href="/service-area" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/service-area" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      Service Area
                    </Link>
                    <Link href="/about" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/about" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      About Us
                    </Link>
                  </nav>
        
                  {/* CTA & Mobile Toggle */}
                  <div className="flex items-center gap-4">
                    <a href="tel:+16514104196" className="hidden lg:flex items-center gap-2 text-sm font-bold text-white hover:text-[#FF5A00] transition-colors">
                      <Phone size={16} />
                      (651) 410-4196
                    </a>
                    
                    <Link 
                      href="/estimate" 
                      className="hidden bg-[#FF5A00] px-6 py-2.5 text-sm font-bold uppercase tracking-wider text-white transition-colors hover:bg-[#d94d00] md:inline-flex"
                    >
                      Get Estimate
                    </Link>
        
                    {/* Mobile Menu */}
                    <Sheet>
                      <SheetTrigger>
                        <button className="lg:hidden text-white p-2" aria-label="Open Menu">
                          <Menu size={24} />
                        </button>
                      </SheetTrigger>
                      <SheetContent side="right" className="w-full sm:w-[400px] bg-[#050505] border-l border-white/10 p-0">
                        <SheetHeader className="p-6 border-b border-white/10 text-left">
                          <SheetTitle className="text-white font-display text-xl uppercase">Menu</SheetTitle>
                          <SheetDescription className="sr-only">Navigate around the Sky's the Limit Painting website.</SheetDescription>
                        </SheetHeader>
                        
                        <div className="p-6 overflow-y-auto h-[calc(100vh-80px)]">
                          <nav className="flex flex-col gap-6 text-xl">
                            <Link href="/">Home</Link>
                            <div className="pt-4 pb-2">
                              <p className="eyebrow mb-4">Painting Services</p>
                              <div className="flex flex-col gap-4 pl-4 border-l border-white/10">
                                <Link href="/painting-services/interior-painting" className="text-lg">Interior Painting</Link>
                                <Link href="/painting-services/exterior-painting" className="text-lg">Exterior Painting</Link>
                                <Link href="/painting-services/cabinet-refinishing" className="text-lg">Cabinet Refinishing</Link>
                                <Link href="/commercial" className="text-lg">Commercial & HOA</Link>
                              </div>
                            </div>
                            <Link href="/projects">Projects</Link>
                            <Link href="/service-area">Service Area</Link>
                            <Link href="/about">About Us</Link>
                            <Link href="/contact">Contact</Link>
                          </nav>
                          
                          <div className="mt-12 flex flex-col gap-4">
                            <Link href="/estimate" className="bg-[#FF5A00] text-white text-center py-4 font-bold tracking-wider uppercase">
                              Get Your Free Estimate
                            </Link>
                            <a href="tel:+16514104196" className="border border-white/10 text-white text-center py-4 font-bold flex items-center justify-center gap-2">
                              <Phone size={18} />
                              (651) 410-4196
                            </a>
                          </div>
                        </div>
                      </SheetContent>
                    </Sheet>
                  </div>
                </div>
              </div>
            </header>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:37:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
      ...
    # Subtest: T1.3 Routing & Navigation - Micro-utility header bar displays warning/intake message
    not ok 3 - T1.3 Routing & Navigation - Micro-utility header bar displays warning/intake message
      ---
      duration_ms: 1.8694
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:42:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /Prep-first painting across the Twin Cities/. Input:
        
        "'use client';\r\n" +
          '\r\n' +
          "import React, { useState, useEffect } from 'react';\n" +
          "import Link from 'next/link';\n" +
          "import Image from 'next/image';\n" +
          "import { usePathname } from 'next/navigation';\r\n" +
          "import { Menu, X, Phone, PaintRoller, ChevronDown, Brush, Warehouse } from 'lucide-react';\r\n" +
          'import { \r\n' +
          '  Sheet, \r\n' +
          '  SheetContent, \r\n' +
          '  SheetTrigger, \r\n' +
          '  SheetHeader,\r\n' +
          '  SheetTitle,\r\n' +
          '  SheetDescription\r\n' +
          "} from '@/components/ui/sheet';\r\n" +
          "import { motion, AnimatePresence } from 'motion/react';\r\n" +
          "import { cn } from '@/lib/utils';\r\n" +
          '\r\n' +
          'export default function ConversionHeader() {\r\n' +
          '  const [isScrolled, setIsScrolled] = useState(false);\r\n' +
          '  const [isServicesOpen, setIsServicesOpen] = useState(false);\r\n' +
          '  const pathname = usePathname();\r\n' +
          '\r\n' +
          '  useEffect(() => {\r\n' +
          '    const handleScroll = () => {\r\n' +
          '      setIsScrolled(window.scrollY > 20);\r\n' +
          '    };\r\n' +
          "    window.addEventListener('scroll', handleScroll);\r\n" +
          "    return () => window.removeEventListener('scroll', handleScroll);\r\n" +
          '  }, []);\r\n' +
          '\r\n' +
          '  // Close dropdown on route change\r\n' +
          '  useEffect(() => {\r\n' +
          '    setIsServicesOpen(false);\r\n' +
          '  }, [pathname]);\r\n' +
          '\r\n' +
          '  const navClass = cn(\r\n' +
          '    "fixed top-0 left-0 right-0 z-50 transition-all duration-300",\r\n' +
          '    isScrolled \r\n' +
          '      ? "bg-[#0A0A0A]/95 backdrop-blur-xl border-b border-zinc-800 py-3" \n' +
          '      : "bg-transparent py-5"\r\n' +
          '  );\r\n' +
          '\r\n' +
          '  const containerVariants = {\r\n' +
          '    hidden: { opacity: 0, y: -5 },\r\n' +
          '    show: {\r\n' +
          '      opacity: 1,\r\n' +
          '      y: 0,\r\n' +
          '      transition: {\r\n' +
          '        staggerChildren: 0.05,\r\n' +
          '        duration: 0.2,\r\n' +
          '      }\r\n' +
          '    },\r\n' +
          '    exit: {\r\n' +
          '      opacity: 0,\r\n' +
          '      y: -5,\r\n' +
          '      transition: { duration: 0.15 }\r\n' +
          '    }\r\n' +
          '  };\r\n' +
          '\r\n' +
          '  const itemVariants = {\r\n' +
          '    hidden: { opacity: 0, x: -10 },\r\n' +
          '    show: { opacity: 1, x: 0, transition: { type: "spring", stiffness: 300, damping: 24 } as any }\r\n' +
          '  };\r\n' +
          '\r\n' +
          '  return (\r\n' +
          '    <header className={navClass}>\r\n' +
          '      <div className="container-page mx-auto">\r\n' +
          '        <div className="flex items-center justify-between">\r\n' +
          '          \r\n' +
          '          {/* Logo */}\r\n' +
          '          <Link href="/" className="flex items-center gap-3 group">\r\n' +
          '            <div className="flex h-10 w-10 shrink-0 items-center justify-center border border-zinc-700 bg-[#0A0A0A] u-transition group-hover:border-[#FF5A00]">\n' +
          `              <Image src="/brand/SkyLLP_BrandLogo.svg" alt="Sky's the Limit Painting" width={24} height={24} priority />\n` +
          '            </div>\r\n' +
          '            <div className="hidden sm:block">\r\n' +
          `              <span className="font-display font-bold text-lg leading-none tracking-tight block text-white">SKY'S THE LIMIT</span>\r\n` +
          '              <span className="eyebrow block mt-0.5 text-[#FF5A00]">PAINTING</span>\r\n' +
          '            </div>\r\n' +
          '          </Link>\r\n' +
          '\r\n' +
          '          {/* Desktop Nav */}\r\n' +
          '          <nav className="hidden lg:flex items-center gap-8">\r\n' +
          '            <div className="relative" onMouseLeave={() => setIsServicesOpen(false)}>\r\n' +
          '              <button \r\n' +
          '                className={cn(\r\n' +
          '                  "flex items-center gap-1 text-sm font-bold tracking-wider uppercase transition-colors py-2",\r\n' +
          `                  isServicesOpen || pathname.includes('/painting-services') ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]"\r\n` +
          '                )}\r\n' +
          '                onMouseEnter={() => setIsServicesOpen(true)}\r\n' +
          '                onClick={() => setIsServicesOpen(!isServicesOpen)}\r\n' +
          '              >\r\n' +
          '                Services <ChevronDown size={14} className={cn("transition-transform duration-200", isServicesOpen && "rotate-180")} />\r\n' +
          '              </button>\r\n' +
          '\r\n' +
          '              <AnimatePresence>\r\n' +
          '                {isServicesOpen && (\r\n' +
          '                  <motion.div \r\n' +
          '                    variants={containerVariants}\r\n' +
          '                    initial="hidden"\r\n' +
          '                    animate="show"\r\n' +
          '                    exit="exit"\r\n' +
          '                    className="absolute top-full left-1/2 -translate-x-1/2 mt-2 w-[480px] bg-[#0B0B0D] border border-white/10 shadow-2xl p-4 grid grid-cols-2 gap-2"\r\n' +
          '                  >\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/interior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Interior Painting</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Walls, trim, ceilings, & prep.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/exterior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><Brush size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Exterior Painting</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Siding, brick, stucco, & decks.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/cabinet-refinishing" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Cabinet Refinishing</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Factory-finish transformations.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/commercial" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><Warehouse size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Commercial</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">HOA, property mgmt, retail.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '                  </motion.div>\r\n' +
          '                )}\r\n' +
          '              </AnimatePresence>\r\n' +
          '            </div>\r\n' +
          '\r\n' +
          '            <Link href="/projects" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/projects" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              Projects\r\n' +
          '            </Link>\r\n' +
          '            <Link href="/service-area" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/service-area" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              Service Area\r\n' +
          '            </Link>\r\n' +
          '            <Link href="/about" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/about" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              About Us\r\n' +
          '            </Link>\r\n' +
          '          </nav>\r\n' +
          '\r\n' +
          '          {/* CTA & Mobile Toggle */}\r\n' +
          '          <div className="flex items-center gap-4">\r\n' +
          '            <a href="tel:+16514104196" className="hidden lg:flex items-center gap-2 text-sm font-bold text-white hover:text-[#FF5A00] transition-colors">\n' +
          '              <Phone size={16} />\r\n' +
          '              (651) 410-4196\n' +
          '            </a>\r\n' +
          '            \r\n' +
          '            <Link \r\n' +
          '              href="/estimate" \r\n' +
          '              className="hidden bg-[#FF5A00] px-6 py-2.5 text-sm font-bold uppercase tracking-wider text-white transition-colors hover:bg-[#d94d00] md:inline-flex"\n' +
          '            >\r\n' +
          '              Get Estimate\r\n' +
          '            </Link>\r\n' +
          '\r\n' +
          '            {/* Mobile Menu */}\r\n' +
          '            <Sheet>\r\n' +
          '              <SheetTrigger>\r\n' +
          '                <button className="lg:hidden text-white p-2" aria-label="Open Menu">\r\n' +
          '                  <Menu size={24} />\r\n' +
          '                </button>\r\n' +
          '              </SheetTrigger>\r\n' +
          '              <SheetContent side="right" className="w-full sm:w-[400px] bg-[#050505] border-l border-white/10 p-0">\r\n' +
          '                <SheetHeader className="p-6 border-b border-white/10 text-left">\r\n' +
          '                  <SheetTitle className="text-white font-display text-xl uppercase">Menu</SheetTitle>\r\n' +
          `                  <SheetDescription className="sr-only">Navigate around the Sky's the Limit Painting website.</SheetDescription>\r\n` +
          '                </SheetHeader>\r\n' +
          '                \r\n' +
          '                <div className="p-6 overflow-y-auto h-[calc(100vh-80px)]">\r\n' +
          '                  <nav className="flex flex-col gap-6 text-xl">\r\n' +
          '                    <Link href="/">Home</Link>\r\n' +
          '                    <div className="pt-4 pb-2">\r\n' +
          '                      <p className="eyebrow mb-4">Painting Services</p>\r\n' +
          '                      <div className="flex flex-col gap-4 pl-4 border-l border-white/10">\r\n' +
          '                        <Link href="/painting-services/interior-painting" className="text-lg">Interior Painting</Link>\r\n' +
          '                        <Link href="/painting-services/exterior-painting" className="text-lg">Exterior Painting</Link>\r\n' +
          '                        <Link href="/painting-services/cabinet-refinishing" className="text-lg">Cabinet Refinishing</Link>\r\n' +
          '                        <Link href="/commercial" className="text-lg">Commercial & HOA</Link>\r\n' +
          '                      </div>\r\n' +
          '                    </div>\r\n' +
          '                    <Link href="/projects">Projects</Link>\r\n' +
          '                    <Link href="/service-area">Service Area</Link>\r\n' +
          '                    <Link href="/about">About Us</Link>\r\n' +
          '                    <Link href="/contact">Contact</Link>\r\n' +
          '   '... 755 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import React, { useState, useEffect } from 'react';
        import Link from 'next/link';
        import Image from 'next/image';
        import { usePathname } from 'next/navigation';
        import { Menu, X, Phone, PaintRoller, ChevronDown, Brush, Warehouse } from 'lucide-react';
        import { 
          Sheet, 
          SheetContent, 
          SheetTrigger, 
          SheetHeader,
          SheetTitle,
          SheetDescription
        } from '@/components/ui/sheet';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '@/lib/utils';
        
        export default function ConversionHeader() {
          const [isScrolled, setIsScrolled] = useState(false);
          const [isServicesOpen, setIsServicesOpen] = useState(false);
          const pathname = usePathname();
        
          useEffect(() => {
            const handleScroll = () => {
              setIsScrolled(window.scrollY > 20);
            };
            window.addEventListener('scroll', handleScroll);
            return () => window.removeEventListener('scroll', handleScroll);
          }, []);
        
          // Close dropdown on route change
          useEffect(() => {
            setIsServicesOpen(false);
          }, [pathname]);
        
          const navClass = cn(
            "fixed top-0 left-0 right-0 z-50 transition-all duration-300",
            isScrolled 
              ? "bg-[#0A0A0A]/95 backdrop-blur-xl border-b border-zinc-800 py-3" 
              : "bg-transparent py-5"
          );
        
          const containerVariants = {
            hidden: { opacity: 0, y: -5 },
            show: {
              opacity: 1,
              y: 0,
              transition: {
                staggerChildren: 0.05,
                duration: 0.2,
              }
            },
            exit: {
              opacity: 0,
              y: -5,
              transition: { duration: 0.15 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, x: -10 },
            show: { opacity: 1, x: 0, transition: { type: "spring", stiffness: 300, damping: 24 } as any }
          };
        
          return (
            <header className={navClass}>
              <div className="container-page mx-auto">
                <div className="flex items-center justify-between">
                  
                  {/* Logo */}
                  <Link href="/" className="flex items-center gap-3 group">
                    <div className="flex h-10 w-10 shrink-0 items-center justify-center border border-zinc-700 bg-[#0A0A0A] u-transition group-hover:border-[#FF5A00]">
                      <Image src="/brand/SkyLLP_BrandLogo.svg" alt="Sky's the Limit Painting" width={24} height={24} priority />
                    </div>
                    <div className="hidden sm:block">
                      <span className="font-display font-bold text-lg leading-none tracking-tight block text-white">SKY'S THE LIMIT</span>
                      <span className="eyebrow block mt-0.5 text-[#FF5A00]">PAINTING</span>
                    </div>
                  </Link>
        
                  {/* Desktop Nav */}
                  <nav className="hidden lg:flex items-center gap-8">
                    <div className="relative" onMouseLeave={() => setIsServicesOpen(false)}>
                      <button 
                        className={cn(
                          "flex items-center gap-1 text-sm font-bold tracking-wider uppercase transition-colors py-2",
                          isServicesOpen || pathname.includes('/painting-services') ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]"
                        )}
                        onMouseEnter={() => setIsServicesOpen(true)}
                        onClick={() => setIsServicesOpen(!isServicesOpen)}
                      >
                        Services <ChevronDown size={14} className={cn("transition-transform duration-200", isServicesOpen && "rotate-180")} />
                      </button>
        
                      <AnimatePresence>
                        {isServicesOpen && (
                          <motion.div 
                            variants={containerVariants}
                            initial="hidden"
                            animate="show"
                            exit="exit"
                            className="absolute top-full left-1/2 -translate-x-1/2 mt-2 w-[480px] bg-[#0B0B0D] border border-white/10 shadow-2xl p-4 grid grid-cols-2 gap-2"
                          >
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/interior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Interior Painting</div>
                                  <div className="text-xs text-gray-400 mt-1">Walls, trim, ceilings, & prep.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/exterior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><Brush size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Exterior Painting</div>
                                  <div className="text-xs text-gray-400 mt-1">Siding, brick, stucco, & decks.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/cabinet-refinishing" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Cabinet Refinishing</div>
                                  <div className="text-xs text-gray-400 mt-1">Factory-finish transformations.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/commercial" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><Warehouse size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Commercial</div>
                                  <div className="text-xs text-gray-400 mt-1">HOA, property mgmt, retail.</div>
                                </div>
                              </Link>
                            </motion.div>
                          </motion.div>
                        )}
                      </AnimatePresence>
                    </div>
        
                    <Link href="/projects" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/projects" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      Projects
                    </Link>
                    <Link href="/service-area" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/service-area" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      Service Area
                    </Link>
                    <Link href="/about" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/about" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      About Us
                    </Link>
                  </nav>
        
                  {/* CTA & Mobile Toggle */}
                  <div className="flex items-center gap-4">
                    <a href="tel:+16514104196" className="hidden lg:flex items-center gap-2 text-sm font-bold text-white hover:text-[#FF5A00] transition-colors">
                      <Phone size={16} />
                      (651) 410-4196
                    </a>
                    
                    <Link 
                      href="/estimate" 
                      className="hidden bg-[#FF5A00] px-6 py-2.5 text-sm font-bold uppercase tracking-wider text-white transition-colors hover:bg-[#d94d00] md:inline-flex"
                    >
                      Get Estimate
                    </Link>
        
                    {/* Mobile Menu */}
                    <Sheet>
                      <SheetTrigger>
                        <button className="lg:hidden text-white p-2" aria-label="Open Menu">
                          <Menu size={24} />
                        </button>
                      </SheetTrigger>
                      <SheetContent side="right" className="w-full sm:w-[400px] bg-[#050505] border-l border-white/10 p-0">
                        <SheetHeader className="p-6 border-b border-white/10 text-left">
                          <SheetTitle className="text-white font-display text-xl uppercase">Menu</SheetTitle>
                          <SheetDescription className="sr-only">Navigate around the Sky's the Limit Painting website.</SheetDescription>
                        </SheetHeader>
                        
                        <div className="p-6 overflow-y-auto h-[calc(100vh-80px)]">
                          <nav className="flex flex-col gap-6 text-xl">
                            <Link href="/">Home</Link>
                            <div className="pt-4 pb-2">
                              <p className="eyebrow mb-4">Painting Services</p>
                              <div className="flex flex-col gap-4 pl-4 border-l border-white/10">
                                <Link href="/painting-services/interior-painting" className="text-lg">Interior Painting</Link>
                                <Link href="/painting-services/exterior-painting" className="text-lg">Exterior Painting</Link>
                                <Link href="/painting-services/cabinet-refinishing" className="text-lg">Cabinet Refinishing</Link>
                                <Link href="/commercial" className="text-lg">Commercial & HOA</Link>
                              </div>
                            </div>
                            <Link href="/projects">Projects</Link>
                            <Link href="/service-area">Service Area</Link>
                            <Link href="/about">About Us</Link>
                            <Link href="/contact">Contact</Link>
                          </nav>
                          
                          <div className="mt-12 flex flex-col gap-4">
                            <Link href="/estimate" className="bg-[#FF5A00] text-white text-center py-4 font-bold tracking-wider uppercase">
                              Get Your Free Estimate
                            </Link>
                            <a href="tel:+16514104196" className="border border-white/10 text-white text-center py-4 font-bold flex items-center justify-center gap-2">
                              <Phone size={18} />
                              (651) 410-4196
                            </a>
                          </div>
                        </div>
                      </SheetContent>
                    </Sheet>
                  </div>
                </div>
              </div>
            </header>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:44:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: T1.4 Routing & Navigation - Redirects for legacy routes redirect to new pages
    ok 4 - T1.4 Routing & Navigation - Redirects for legacy routes redirect to new pages
      ---
      duration_ms: 0.6834
      type: 'test'
      ...
    # Subtest: T1.5 Routing & Navigation - Invalid paths serve the customized 404 page
    ok 5 - T1.5 Routing & Navigation - Invalid paths serve the customized 404 page
      ---
      duration_ms: 4.1071
      type: 'test'
      ...
    # Subtest: T1.6 Three-Market Content - Home page renders the approved positioning statement
    not ok 6 - T1.6 Three-Market Content - Home page renders the approved positioning statement
      ---
      duration_ms: 1.8758
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:61:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /Residential detail\. Commercial discipline\. Public-sector ready\./. Input:
        
        "import Link from 'next/link';\n" +
          "import { ArrowRight, Check, ClipboardCheck, HardHat, Ruler, ShieldCheck } from 'lucide-react';\n" +
          "import LeadForm from '../components/LeadForm';\n" +
          "import PrepProtocolStage from '../components/PrepProtocolStage';\n" +
          "import ResponsiveImage from '../components/ResponsiveImage';\n" +
          "import { faqSchema } from '../lib/seo';\n" +
          '\n' +
          'const scopeRows = [\n' +
          "  ['INTERIOR', 'Walls ┬╖ ceilings ┬╖ trim ┬╖ cabinets', 'Residential'],\n" +
          "  ['EXTERIOR', 'Siding ┬╖ trim ┬╖ decks ┬╖ fences', 'Residential'],\n" +
          "  ['PROPERTY', 'Repaints ┬╖ turnovers ┬╖ common areas', 'Commercial'],\n" +
          "  ['MARKING', 'Parking lots ┬╖ curbs ┬╖ safety striping', 'Public / commercial'],\n" +
          '];\n' +
          '\n' +
          'const faqItems = [\n' +
          '  {\n' +
          "    question: 'How is a painting project scoped?',\n" +
          "    answer: 'The scope begins with surfaces, condition, access, preparation requirements, photos, and timeline. The result is a written estimate before work is scheduled.',\n" +
          '  },\n' +
          '  {\n' +
          "    question: 'Who performs the walkthrough?',\n" +
          "    answer: 'Anthony Briseno handles the project conversation and walkthrough directly, so the scope is not passed between a sales team and a crew.',\n" +
          '  },\n' +
          '  {\n' +
          "    question: 'Can documentation be provided for property or public work?',\n" +
          "    answer: 'A certificate of insurance is available for qualified commercial and public-sector opportunities. Ask about documentation during the scope review.',\n" +
          '  },\n' +
          '];\n' +
          '\n' +
          'export default function HomeClient() {\n' +
          '  return (\n' +
          '    <article className="bg-[#0A0A0A] text-zinc-100">\n' +
          '      <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqSchema(faqItems)) }} />\n' +
          '\n' +
          '      <section aria-labelledby="home-title" className="border-b border-zinc-800">\n' +
          '        <div className="mx-auto grid min-h-[calc(100svh-117px)] max-w-7xl lg:grid-cols-[minmax(0,1.1fr)_minmax(24rem,0.9fr)]">\n' +
          '          <div className="flex flex-col justify-between px-6 py-10 sm:px-8 lg:px-12 lg:py-14">\n' +
          '            <div>\n' +
          '              <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">Twin Cities / Residential painting scope desk</p>\n' +
          '              <h1 id="home-title" className="mt-6 max-w-4xl text-5xl font-black uppercase leading-[0.9] tracking-[-0.06em] text-white sm:text-7xl lg:text-8xl">\n' +
          '                Paint work with the prep exposed.\n' +
          '              </h1>\n' +
          '              <p className="mt-8 max-w-2xl text-base leading-7 text-zinc-300 sm:text-lg">\n' +
          '                Sky&apos;s the Limit Painting LLC scopes the protection, correction, coating system, and final walkthrough before a project reaches your home.\n' +
          '              </p>\n' +
          '            </div>\n' +
          '\n' +
          '            <div className="mt-10">\n' +
          '              <Link href="/estimate" className="inline-flex min-h-14 items-center gap-4 bg-[#FF5A00] px-6 py-4 text-sm font-black uppercase tracking-[0.12em] text-white transition-colors hover:bg-[#d94d00] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-[#FF5A00]">\n' +
          '                Calculate room scope <ArrowRight aria-hidden="true" size={18} />\n' +
          '              </Link>\n' +
          '              <div className="mt-5 grid max-w-2xl border border-zinc-700 sm:grid-cols-3">\n' +
          `                {[['MN ID', 'IR816596'], ['WORKERS COMP', '176.041 EXEMPT'], ['COVERAGE', '100% INSURED']].map(([label, value]) => <div key={label} className="border-b border-zinc-800 px-4 py-3 last:border-b-0 sm:border-b-0 sm:border-r sm:last:border-r-0"><p className="font-mono text-[9px] uppercase tracking-[0.14em] text-zinc-500">{label}</p><p className="mt-1 font-mono text-[10px] font-bold uppercase tracking-[0.08em] text-white">{value}</p></div>)}\n` +
          '              </div>\n' +
          '              <p className="mt-4 font-mono text-[10px] uppercase tracking-[0.16em] text-zinc-500">Start with surfaces, condition, and dimensions. Contact details come last.</p>\n' +
          '            </div>\n' +
          '          </div>\n' +
          '\n' +
          '          <div className="relative min-h-[24rem] border-t border-zinc-800 lg:min-h-0 lg:border-l lg:border-t-0">\n' +
          '            <ResponsiveImage src="/images/site/marketing-hero-exterior-painting.webp" alt="Painter preparing an exterior trim surface with windows and landscaping protected" width={1600} height={900} sizes="(min-width: 1024px) 45vw, 100vw" priority fetchPriority="high" className="absolute inset-0 h-full w-full object-cover" />\n' +
          '            <div className="absolute inset-x-0 bottom-0 border-t border-zinc-700 bg-[#0A0A0A]/95 p-5 backdrop-blur-sm">\n' +
          '              <p className="font-mono text-[10px] uppercase tracking-[0.18em] text-zinc-400">Field image / exterior preparation</p>\n' +
          '              <p className="mt-2 text-sm font-semibold text-white">Protection and surface correction are part of the work orderΓÇönot an upgrade.</p>\n' +
          '            </div>\n' +
          '          </div>\n' +
          '        </div>\n' +
          '      </section>\n' +
          '\n' +
          '      <section aria-labelledby="scope-ledger" className="border-b border-zinc-800">\n' +
          '        <div className="mx-auto max-w-7xl px-6 py-16 sm:px-8 lg:px-12">\n' +
          '          <div className="grid gap-8 lg:grid-cols-[0.75fr_1.25fr] lg:gap-16">\n' +
          '            <div>\n' +
          '              <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">01 / Scope ledger</p>\n' +
          '              <h2 id="scope-ledger" className="mt-4 text-4xl font-black uppercase leading-none tracking-[-0.05em] text-white sm:text-5xl">What gets priced.</h2>\n' +
          '              <p className="mt-6 max-w-md text-sm leading-7 text-zinc-400">The estimate follows the actual site conditions. Each line item exists to make preparation and execution visible before scheduling.</p>\n' +
          '            </div>\n' +
          '            <dl className="border-t border-zinc-700">\n' +
          '              {scopeRows.map(([code, work, market]) => (\n' +
          '                <div key={code} className="grid grid-cols-[4rem_1fr] gap-4 border-b border-zinc-800 py-5 sm:grid-cols-[5rem_1fr_auto] sm:items-center">\n' +
          '                  <dt className="font-mono text-xs font-bold tracking-widest text-[#FF5A00]">{code}</dt>\n' +
          '                  <dd className="text-sm font-bold uppercase tracking-wide text-white">{work}</dd>\n' +
          '                  <dd className="col-start-2 font-mono text-[10px] uppercase tracking-[0.16em] text-zinc-500 sm:col-start-auto">{market}</dd>\n' +
          '                </div>\n' +
          '              ))}\n' +
          '            </dl>\n' +
          '          </div>\n' +
          '        </div>\n' +
          '      </section>\n' +
          '\n' +
          '      <PrepProtocolStage />\n' +
          '\n' +
          '      <section aria-labelledby="proof" className="border-b border-zinc-800">\n' +
          '        <div className="mx-auto grid max-w-7xl lg:grid-cols-2">\n' +
          '          <div className="relative aspect-[4/3] border-b border-zinc-800 lg:aspect-auto lg:border-b-0 lg:border-r">\n' +
          '            <ResponsiveImage src="/images/site/iphone-interior-painting-progress.webp" alt="Interior painting work in progress with room surfaces prepared for coating" width={1200} height={900} sizes="(min-width: 1024px) 50vw, 100vw" className="absolute inset-0 h-full w-full object-cover" />\n' +
          '          </div>\n' +
          '          <div className="px-6 py-16 sm:px-8 lg:px-12">\n' +
          '            <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">03 / Operational controls</p>\n' +
          '            <h2 id="proof" className="mt-4 text-4xl font-black uppercase leading-none tracking-[-0.05em] text-white sm:text-5xl">A contractor record you can inspect.</h2>\n' +
          '            <ul className="mt-10 divide-y divide-zinc-800 border-y border-zinc-800">\n' +
          '              {[\n' +
          "                [ShieldCheck, 'MN ID: IR816596', 'Minnesota specialty contractor registration'],\n" +
          "                [HardHat, '176.041 Workers Comp Exempt', 'Owner-operator exemption record'],\n" +
          "                [ClipboardCheck, '100% Insured', 'Certificate of insurance available for qualified opportunities'],\n" +
          "                [Ruler, 'Direct scope review', 'Owner-led walkthrough through final detail'],\n" +
          '              ].map(([Icon, title, detail]) => {\n' +
          '                const Mark = Icon as typeof ShieldCheck;\n' +
          '                return <li key={title as string} className="flex gap-4 py-5"><Mark aria-hidden="true" className="mt-1 shrink-0 text-[#FF5A00]" size={18} /><div><p className="text-sm font-bold uppercase tracking-wide text-white">{title as string}</p><p className="mt-1 font-mono text-[10px] uppercase tracking-[0.12em] text-zinc-500">{detail as string}</p></div></li>;\n' +
          '              })}\n' +
          '            </ul>\n' +
          '          </div>\n' +
          '        </div>\n' +
          '      </section>\n' +
          '\n' +
          '      <section aria-labelledby="request-scope" className="bg-[#0A0A0A]">\n' +
          '        <div className="mx-auto grid max-w-7xl gap-10 px-6 py-16 sm:px-8 lg:grid-cols-[0.8fr_1.2fr] lg:px-12 lg:py-20">\n' +
          '          <aside className="border-t-2 border-[#FF5A00] pt-6">\n' +
          '            <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">04 / Request a written scope</p>\n' +
          '            <h2 id="request-scope" className="mt-4 text-4xl font-black uppercase leading-none tracking-[-0.05em] text-white sm:text-5xl">Send the job data.</h2>\n' +
          '            <p className="mt-6 max-w-md text-sm leading-7 text-zinc-400">Tell us the property type, city, surfaces, timeline, and preparation needs. The final step asks how Anthony should reach you.</p>\n' +
          '            <p className="mt-8 font-mono text-[10px] uppercase tracking-[0.15em] text-zinc-500">Service area / Minneapolis ┬╖ St. Paul ┬╖ Twin Cities metro</p>\n' +
          '          </aside>\n' +
          '          <div className="border border-zinc-700 bg-[#111111] p-5 sm:p-8"><LeadForm source="homepage_scope_desk" defaultMarket="Residential" /></div>\n' +
          '        </div>\n' +
          '      </section>\n' +
          '    </article>\n' +
          '  );\n' +
          '}\n'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        import Link from 'next/link';
        import { ArrowRight, Check, ClipboardCheck, HardHat, Ruler, ShieldCheck } from 'lucide-react';
        import LeadForm from '../components/LeadForm';
        import PrepProtocolStage from '../components/PrepProtocolStage';
        import ResponsiveImage from '../components/ResponsiveImage';
        import { faqSchema } from '../lib/seo';
        
        const scopeRows = [
          ['INTERIOR', 'Walls ┬╖ ceilings ┬╖ trim ┬╖ cabinets', 'Residential'],
          ['EXTERIOR', 'Siding ┬╖ trim ┬╖ decks ┬╖ fences', 'Residential'],
          ['PROPERTY', 'Repaints ┬╖ turnovers ┬╖ common areas', 'Commercial'],
          ['MARKING', 'Parking lots ┬╖ curbs ┬╖ safety striping', 'Public / commercial'],
        ];
        
        const faqItems = [
          {
            question: 'How is a painting project scoped?',
            answer: 'The scope begins with surfaces, condition, access, preparation requirements, photos, and timeline. The result is a written estimate before work is scheduled.',
          },
          {
            question: 'Who performs the walkthrough?',
            answer: 'Anthony Briseno handles the project conversation and walkthrough directly, so the scope is not passed between a sales team and a crew.',
          },
          {
            question: 'Can documentation be provided for property or public work?',
            answer: 'A certificate of insurance is available for qualified commercial and public-sector opportunities. Ask about documentation during the scope review.',
          },
        ];
        
        export default function HomeClient() {
          return (
            <article className="bg-[#0A0A0A] text-zinc-100">
              <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqSchema(faqItems)) }} />
        
              <section aria-labelledby="home-title" className="border-b border-zinc-800">
                <div className="mx-auto grid min-h-[calc(100svh-117px)] max-w-7xl lg:grid-cols-[minmax(0,1.1fr)_minmax(24rem,0.9fr)]">
                  <div className="flex flex-col justify-between px-6 py-10 sm:px-8 lg:px-12 lg:py-14">
                    <div>
                      <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">Twin Cities / Residential painting scope desk</p>
                      <h1 id="home-title" className="mt-6 max-w-4xl text-5xl font-black uppercase leading-[0.9] tracking-[-0.06em] text-white sm:text-7xl lg:text-8xl">
                        Paint work with the prep exposed.
                      </h1>
                      <p className="mt-8 max-w-2xl text-base leading-7 text-zinc-300 sm:text-lg">
                        Sky&apos;s the Limit Painting LLC scopes the protection, correction, coating system, and final walkthrough before a project reaches your home.
                      </p>
                    </div>
        
                    <div className="mt-10">
                      <Link href="/estimate" className="inline-flex min-h-14 items-center gap-4 bg-[#FF5A00] px-6 py-4 text-sm font-black uppercase tracking-[0.12em] text-white transition-colors hover:bg-[#d94d00] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-[#FF5A00]">
                        Calculate room scope <ArrowRight aria-hidden="true" size={18} />
                      </Link>
                      <div className="mt-5 grid max-w-2xl border border-zinc-700 sm:grid-cols-3">
                        {[['MN ID', 'IR816596'], ['WORKERS COMP', '176.041 EXEMPT'], ['COVERAGE', '100% INSURED']].map(([label, value]) => <div key={label} className="border-b border-zinc-800 px-4 py-3 last:border-b-0 sm:border-b-0 sm:border-r sm:last:border-r-0"><p className="font-mono text-[9px] uppercase tracking-[0.14em] text-zinc-500">{label}</p><p className="mt-1 font-mono text-[10px] font-bold uppercase tracking-[0.08em] text-white">{value}</p></div>)}
                      </div>
                      <p className="mt-4 font-mono text-[10px] uppercase tracking-[0.16em] text-zinc-500">Start with surfaces, condition, and dimensions. Contact details come last.</p>
                    </div>
                  </div>
        
                  <div className="relative min-h-[24rem] border-t border-zinc-800 lg:min-h-0 lg:border-l lg:border-t-0">
                    <ResponsiveImage src="/images/site/marketing-hero-exterior-painting.webp" alt="Painter preparing an exterior trim surface with windows and landscaping protected" width={1600} height={900} sizes="(min-width: 1024px) 45vw, 100vw" priority fetchPriority="high" className="absolute inset-0 h-full w-full object-cover" />
                    <div className="absolute inset-x-0 bottom-0 border-t border-zinc-700 bg-[#0A0A0A]/95 p-5 backdrop-blur-sm">
                      <p className="font-mono text-[10px] uppercase tracking-[0.18em] text-zinc-400">Field image / exterior preparation</p>
                      <p className="mt-2 text-sm font-semibold text-white">Protection and surface correction are part of the work orderΓÇönot an upgrade.</p>
                    </div>
                  </div>
                </div>
              </section>
        
              <section aria-labelledby="scope-ledger" className="border-b border-zinc-800">
                <div className="mx-auto max-w-7xl px-6 py-16 sm:px-8 lg:px-12">
                  <div className="grid gap-8 lg:grid-cols-[0.75fr_1.25fr] lg:gap-16">
                    <div>
                      <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">01 / Scope ledger</p>
                      <h2 id="scope-ledger" className="mt-4 text-4xl font-black uppercase leading-none tracking-[-0.05em] text-white sm:text-5xl">What gets priced.</h2>
                      <p className="mt-6 max-w-md text-sm leading-7 text-zinc-400">The estimate follows the actual site conditions. Each line item exists to make preparation and execution visible before scheduling.</p>
                    </div>
                    <dl className="border-t border-zinc-700">
                      {scopeRows.map(([code, work, market]) => (
                        <div key={code} className="grid grid-cols-[4rem_1fr] gap-4 border-b border-zinc-800 py-5 sm:grid-cols-[5rem_1fr_auto] sm:items-center">
                          <dt className="font-mono text-xs font-bold tracking-widest text-[#FF5A00]">{code}</dt>
                          <dd className="text-sm font-bold uppercase tracking-wide text-white">{work}</dd>
                          <dd className="col-start-2 font-mono text-[10px] uppercase tracking-[0.16em] text-zinc-500 sm:col-start-auto">{market}</dd>
                        </div>
                      ))}
                    </dl>
                  </div>
                </div>
              </section>
        
              <PrepProtocolStage />
        
              <section aria-labelledby="proof" className="border-b border-zinc-800">
                <div className="mx-auto grid max-w-7xl lg:grid-cols-2">
                  <div className="relative aspect-[4/3] border-b border-zinc-800 lg:aspect-auto lg:border-b-0 lg:border-r">
                    <ResponsiveImage src="/images/site/iphone-interior-painting-progress.webp" alt="Interior painting work in progress with room surfaces prepared for coating" width={1200} height={900} sizes="(min-width: 1024px) 50vw, 100vw" className="absolute inset-0 h-full w-full object-cover" />
                  </div>
                  <div className="px-6 py-16 sm:px-8 lg:px-12">
                    <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">03 / Operational controls</p>
                    <h2 id="proof" className="mt-4 text-4xl font-black uppercase leading-none tracking-[-0.05em] text-white sm:text-5xl">A contractor record you can inspect.</h2>
                    <ul className="mt-10 divide-y divide-zinc-800 border-y border-zinc-800">
                      {[
                        [ShieldCheck, 'MN ID: IR816596', 'Minnesota specialty contractor registration'],
                        [HardHat, '176.041 Workers Comp Exempt', 'Owner-operator exemption record'],
                        [ClipboardCheck, '100% Insured', 'Certificate of insurance available for qualified opportunities'],
                        [Ruler, 'Direct scope review', 'Owner-led walkthrough through final detail'],
                      ].map(([Icon, title, detail]) => {
                        const Mark = Icon as typeof ShieldCheck;
                        return <li key={title as string} className="flex gap-4 py-5"><Mark aria-hidden="true" className="mt-1 shrink-0 text-[#FF5A00]" size={18} /><div><p className="text-sm font-bold uppercase tracking-wide text-white">{title as string}</p><p className="mt-1 font-mono text-[10px] uppercase tracking-[0.12em] text-zinc-500">{detail as string}</p></div></li>;
                      })}
                    </ul>
                  </div>
                </div>
              </section>
        
              <section aria-labelledby="request-scope" className="bg-[#0A0A0A]">
                <div className="mx-auto grid max-w-7xl gap-10 px-6 py-16 sm:px-8 lg:grid-cols-[0.8fr_1.2fr] lg:px-12 lg:py-20">
                  <aside className="border-t-2 border-[#FF5A00] pt-6">
                    <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">04 / Request a written scope</p>
                    <h2 id="request-scope" className="mt-4 text-4xl font-black uppercase leading-none tracking-[-0.05em] text-white sm:text-5xl">Send the job data.</h2>
                    <p className="mt-6 max-w-md text-sm leading-7 text-zinc-400">Tell us the property type, city, surfaces, timeline, and preparation needs. The final step asks how Anthony should reach you.</p>
                    <p className="mt-8 font-mono text-[10px] uppercase tracking-[0.15em] text-zinc-500">Service area / Minneapolis ┬╖ St. Paul ┬╖ Twin Cities metro</p>
                  </aside>
                  <div className="border border-zinc-700 bg-[#111111] p-5 sm:p-8"><LeadForm source="homepage_scope_desk" defaultMarket="Residential" /></div>
                </div>
              </section>
            </article>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:63:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: T1.7 Three-Market Content - Residential page loads specific data fields
    ok 7 - T1.7 Three-Market Content - Residential page loads specific data fields
      ---
      duration_ms: 0.5876
      type: 'test'
      ...
    # Subtest: T1.8 Three-Market Content - Commercial page loads specific data fields
    ok 8 - T1.8 Three-Market Content - Commercial page loads specific data fields
      ---
      duration_ms: 0.7569
      type: 'test'
      ...
    # Subtest: T1.9 Three-Market Content - Public Sector page loads specific data fields
    ok 9 - T1.9 Three-Market Content - Public Sector page loads specific data fields
      ---
      duration_ms: 2.2005
      type: 'test'
      ...
    # Subtest: T1.10 Three-Market Content - Capabilities statement page displays NAICS and SWIFT details
    ok 10 - T1.10 Three-Market Content - Capabilities statement page displays NAICS and SWIFT details
      ---
      duration_ms: 2.4959
      type: 'test'
      ...
    # Subtest: T1.11 Local SEO Pages - Service Areas landing pages load data
    ok 11 - T1.11 Local SEO Pages - Service Areas landing pages load data
      ---
      duration_ms: 0.6403
      type: 'test'
      ...
    # Subtest: T1.12 Local SEO Pages - Painting Services landing pages load data
    ok 12 - T1.12 Local SEO Pages - Painting Services landing pages load data
      ---
      duration_ms: 0.5333
      type: 'test'
      ...
    # Subtest: T1.13 Local SEO Pages - XML Sitemap contains SEO slugs
    ok 13 - T1.13 Local SEO Pages - XML Sitemap contains SEO slugs
      ---
      duration_ms: 0.4761
      type: 'test'
      ...
    # Subtest: T1.14 Local SEO Pages - App Router pages exist for market and SEO routes
    ok 14 - T1.14 Local SEO Pages - App Router pages exist for market and SEO routes
      ---
      duration_ms: 1.1108
      type: 'test'
      ...
    # Subtest: T1.15 Local SEO Pages - Breadcrumb schemas contain valid JSON-LD metadata
    ok 15 - T1.15 Local SEO Pages - Breadcrumb schemas contain valid JSON-LD metadata
      ---
      duration_ms: 0.3877
      type: 'test'
      ...
    # Subtest: T1.16 Interactive Components - BeforeAfterSlider renders before and after images
    ok 16 - T1.16 Interactive Components - BeforeAfterSlider renders before and after images
      ---
      duration_ms: 1.5
      type: 'test'
      ...
    # Subtest: T1.17 Interactive Components - ServiceAreaMap renders SVG map with city pins
    ok 17 - T1.17 Interactive Components - ServiceAreaMap renders SVG map with city pins
      ---
      duration_ms: 0.6059
      type: 'test'
      ...
    # Subtest: T1.18 Interactive Components - Clicking service area pins navigates user correctly
    ok 18 - T1.18 Interactive Components - Clicking service area pins navigates user correctly
      ---
      duration_ms: 0.4023
      type: 'test'
      ...
    # Subtest: T1.19 Interactive Components - dead SPA cursor theater is deleted
    ok 19 - T1.19 Interactive Components - dead SPA cursor theater is deleted
      ---
      duration_ms: 0.8886
      type: 'test'
      ...
    # Subtest: T1.20 Interactive Components - MagneticButtons trigger hover states and scale
    ok 20 - T1.20 Interactive Components - MagneticButtons trigger hover states and scale
      ---
      duration_ms: 0.9443
      type: 'test'
      ...
    # Subtest: T1.21 Room Calculator - Preset selections apply correct room dimensions
    not ok 21 - T1.21 Room Calculator - Preset selections apply correct room dimensions
      ---
      duration_ms: 1.8396
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:156:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /Bedroom.*12.*14.*8/. Input:
        
        "'use client';\n" +
          '\n' +
          "import { useState, FormEvent, useEffect, useRef } from 'react';\n" +
          "import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';\n" +
          "import PageTransition from '../components/PageTransition';\n" +
          "import RangeSlider from '../components/RangeSlider';\n" +
          "import { trackEvent } from '../lib/analytics';\n" +
          "import { buildEstimateMailto } from '../lib/contact';\n" +
          "import { motion, AnimatePresence } from 'motion/react';\n" +
          "import { cn } from '../lib/utils';\n" +
          '\n' +
          "type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;\n" +
          "type PrepLevel = 'standard' | 'premium' | null;\n" +
          '\n' +
          'interface ChatMessage {\n' +
          '  id: string;\n' +
          "  sender: 'bot' | 'user';\n" +
          '  text: React.ReactNode;\n' +
          '  isComponent?: boolean;\n' +
          '}\n' +
          '\n' +
          'const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };\n' +
          '\n' +
          'export default function EstimatePage() {\n' +
          '  const [messages, setMessages] = useState<ChatMessage[]>([]);\n' +
          '  const [isTyping, setIsTyping] = useState(false);\n' +
          '  const scrollRef = useRef<HTMLDivElement>(null);\n' +
          '\n' +
          '  // State\n' +
          '  const [step, setStep] = useState(0);\n' +
          '  const [projectType, setProjectType] = useState<ProjectType>(null);\n' +
          '  \n' +
          '  // Interior State\n' +
          "  const [roomType, setRoomType] = useState('Bedroom');\n" +
          '  const [width, setWidth] = useState(12);\n' +
          '  const [length, setLength] = useState(14);\n' +
          '  const [height, setHeight] = useState(8);\n' +
          '\n' +
          '  // Exterior State\n' +
          "  const [stories, setStories] = useState('1 Story');\n" +
          "  const [siding, setSiding] = useState('Wood / LP SmartSide');\n" +
          '\n' +
          '  // Cabinet State\n' +
          '  const [cabinetCount, setCabinetCount] = useState(20);\n' +
          '\n' +
          '  // Common State\n' +
          '  const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);\n' +
          '\n' +
          '  // Lead State\n' +
          "  const [name, setName] = useState('');\n" +
          "  const [phone, setPhone] = useState('');\n" +
          "  const [email, setEmail] = useState('');\n" +
          "  const [city, setCity] = useState('');\n" +
          "  const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');\n" +
          "  const [submitMessage, setSubmitMessage] = useState('');\n" +
          '\n' +
          '  const scrollToBottom = () => {\n' +
          '    if (scrollRef.current) {\n' +
          '      scrollRef.current.scrollTo({\n' +
          '        top: scrollRef.current.scrollHeight,\n' +
          "        behavior: 'smooth'\n" +
          '      });\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  useEffect(() => {\n' +
          '    scrollToBottom();\n' +
          '  }, [messages, isTyping, step]);\n' +
          '\n' +
          '  const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {\n' +
          '    setIsTyping(true);\n' +
          '    setTimeout(() => {\n' +
          '      setIsTyping(false);\n' +
          "      setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);\n" +
          '    }, delay);\n' +
          '  };\n' +
          '\n' +
          '  const addUserMessage = (text: string) => {\n' +
          "    setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);\n" +
          '  };\n' +
          '\n' +
          '  // Initialize\n' +
          '  useEffect(() => {\n' +
          '    if (step === 0) {\n' +
          `      addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);\n` +
          '      setTimeout(() => setStep(1), 1200);\n' +
          '    }\n' +
          '  }, []);\n' +
          '\n' +
          '  const handleProjectType = (type: ProjectType, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setProjectType(type);\n' +
          '    \n' +
          "    if (type === 'interior') {\n" +
          '      addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");\n' +
          '      setStep(2);\n' +
          "    } else if (type === 'exterior') {\n" +
          '      addBotMessage("Exterior painting. How many stories is the home?");\n' +
          '      setStep(3);\n' +
          "    } else if (type === 'cabinets') {\n" +
          '      addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");\n' +
          '      setStep(4);\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const handleInteriorRoom = (type: string) => {\n' +
          '    addUserMessage(type);\n' +
          '    setRoomType(type);\n' +
          '    addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");\n' +
          '    setStep(2.1);\n' +
          '  };\n' +
          '\n' +
          '  const submitInteriorDimensions = () => {\n' +
          '    addUserMessage(`${width}x${length}x${height} ft`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorStories = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setStories(val);\n' +
          '    addBotMessage("What type of siding do you have?");\n' +
          '    setStep(3.1);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorSiding = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setSiding(val);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const submitCabinets = () => {\n' +
          '    addUserMessage(`${cabinetCount} doors/drawers`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handlePrep = (prep: PrepLevel, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setPrepLevel(prep);\n' +
          '    addBotMessage("Calculating your estimate...", 400);\n' +
          '    setTimeout(() => {\n' +
          '      calculateFinal();\n' +
          '    }, 1500);\n' +
          '    setStep(6);\n' +
          '  };\n' +
          '\n' +
          '  const calculateFinal = () => {\n' +
          '    let low = 0;\n' +
          '    let high = 0;\n' +
          '\n' +
          "    if (projectType === 'interior') {\n" +
          '      const wallArea = 2 * (width + length) * height;\n' +
          '      const base = wallArea * 3.50;\n' +
          "      const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;\n" +
          '      const total = base + prepVal + 250;\n' +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          "    } else if (projectType === 'exterior') {\n" +
          '      let base = 3500;\n' +
          "      if (stories === '2 Story') base = 5500;\n" +
          "      if (stories === '3+ Story') base = 8500;\n" +
          '      \n' +
          '      let mult = 1.0;\n' +
          "      if (siding === 'Stucco') mult = 1.3;\n" +
          "      if (siding === 'Brick / Masonry') mult = 1.4;\n" +
          '\n' +
          "      const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);\n" +
          '      low = total * 0.85;\n' +
          '      high = total * 1.20;\n' +
          "    } else if (projectType === 'cabinets') {\n" +
          "      const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);\n" +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          '    }\n' +
          '\n' +
          '    low = Math.round(low / 100) * 100;\n' +
          '    high = Math.round(high / 100) * 100;\n' +
          '\n' +
          '    addBotMessage(\n' +
          '      <div className="space-y-4 reveal-up">\n' +
          '        <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>\n' +
          '        <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">\n' +
          '          <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>\n' +
          '          <p className="eyebrow mb-2">Estimated Range</p>\n' +
          '          <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>\n' +
          '        </div>\n' +
          '        <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>\n' +
          '      </div>\n' +
          '    , 200, true);\n' +
          '\n' +
          '    setStep(7);\n' +
          '  };\n' +
          '\n' +
          '  const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {\n' +
          '    e.preventDefault();\n' +
          '    const payload = {\n' +
          "      source: 'Chatbot Estimate',\n" +
          "      page: '/estimate',\n" +
          '      name, phone, email, city,\n' +
          '      projectType, prepLevel,\n' +
          '      notes: `Project: ${projectType} \\nPrep: ${prepLevel}`\n' +
          '    };\n' +
          '\n' +
          "    setStatus('submitting');\n" +
          '    try {\n' +
          "      const response = await fetch('/api/leads', {\n" +
          "        method: 'POST',\n" +
          "        headers: { 'Content-Type': 'application/json' },\n" +
          '        body: JSON.stringify(payload),\n' +
          '      });\n' +
          '      if (response.ok) {\n' +
          "        setStatus('sent');\n" +
          '        addUserMessage("Submitted my info.");\n' +
          `        addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);\n` +
          '      } else {\n' +
          "        setStatus('fallback');\n" +
          '      }\n' +
          '    } catch {\n' +
          "      setStatus('fallback');\n" +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const containerVariants = {\n' +
          '    hidden: { opacity: 0 },\n' +
          '    show: {\n' +
          '      opacity: 1,\n' +
          '      transition: { staggerChildren: 0.1 }\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const itemVariants = {\n' +
          '    hidden: { opacity: 0, y: 15, scale: 0.95 },\n' +
          '    show: { opacity: 1, y: 0, scale: 1, transition: springConfig }\n' +
          '  };\n' +
          '\n' +
          '  return (\n' +
          '    <PageTransition>\n' +
          '      <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">\n' +
          '        \n' +
          '        <div className="noise-overlay"></div>\n' +
          '\n' +
          '        <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">\n' +
          '          \n' +
          '          {/* Header */}\n' +
          '          <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">\n' +
          '            <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">\n' +
          '               <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />\n' +
          '            </div>\n' +
          '            <div>\n' +
          '              <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>\n' +
          '              <p className="eyebrow flex items-center gap-2 mt-1">\n' +
          '                <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online\n' +
          '              </p>\n' +
          '            </div>\n' +
          '          </div>\n' +
          '\n' +
          '          {/* Chat Area */}\n' +
          '          <div \n' +
          '            ref={scrollRef}\n' +
          '            className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"\n' +
          '          >\n' +
          '            <AnimatePresence initial={false}>\n' +
          '              {messages.map((msg) => (\n' +
          '                <motion.div\n' +
          '                  key={msg.id}\n' +
          '                  initial={{ opacity: 0, y: 20, scale: 0.95 }}\n' +
          '                  animate={{ opacity: 1, y: 0, scale: 1 }}\n' +
          '                  transition={springConfig}\n' +
          "                  className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}\n" +
          '                >\n' +
          "                  <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>\n" +
          '                    \n' +
          "                    <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>\n" +
          "                      {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}\n" +
          '                    </div>\n' +
          '\n' +
          '                    <div className={cn(\n' +
          '                      "p-5 text-sm l'... 10894 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import { useState, FormEvent, useEffect, useRef } from 'react';
        import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';
        import PageTransition from '../components/PageTransition';
        import RangeSlider from '../components/RangeSlider';
        import { trackEvent } from '../lib/analytics';
        import { buildEstimateMailto } from '../lib/contact';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '../lib/utils';
        
        type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;
        type PrepLevel = 'standard' | 'premium' | null;
        
        interface ChatMessage {
          id: string;
          sender: 'bot' | 'user';
          text: React.ReactNode;
          isComponent?: boolean;
        }
        
        const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };
        
        export default function EstimatePage() {
          const [messages, setMessages] = useState<ChatMessage[]>([]);
          const [isTyping, setIsTyping] = useState(false);
          const scrollRef = useRef<HTMLDivElement>(null);
        
          // State
          const [step, setStep] = useState(0);
          const [projectType, setProjectType] = useState<ProjectType>(null);
          
          // Interior State
          const [roomType, setRoomType] = useState('Bedroom');
          const [width, setWidth] = useState(12);
          const [length, setLength] = useState(14);
          const [height, setHeight] = useState(8);
        
          // Exterior State
          const [stories, setStories] = useState('1 Story');
          const [siding, setSiding] = useState('Wood / LP SmartSide');
        
          // Cabinet State
          const [cabinetCount, setCabinetCount] = useState(20);
        
          // Common State
          const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);
        
          // Lead State
          const [name, setName] = useState('');
          const [phone, setPhone] = useState('');
          const [email, setEmail] = useState('');
          const [city, setCity] = useState('');
          const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');
          const [submitMessage, setSubmitMessage] = useState('');
        
          const scrollToBottom = () => {
            if (scrollRef.current) {
              scrollRef.current.scrollTo({
                top: scrollRef.current.scrollHeight,
                behavior: 'smooth'
              });
            }
          };
        
          useEffect(() => {
            scrollToBottom();
          }, [messages, isTyping, step]);
        
          const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {
            setIsTyping(true);
            setTimeout(() => {
              setIsTyping(false);
              setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);
            }, delay);
          };
        
          const addUserMessage = (text: string) => {
            setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);
          };
        
          // Initialize
          useEffect(() => {
            if (step === 0) {
              addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);
              setTimeout(() => setStep(1), 1200);
            }
          }, []);
        
          const handleProjectType = (type: ProjectType, label: string) => {
            addUserMessage(label);
            setProjectType(type);
            
            if (type === 'interior') {
              addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");
              setStep(2);
            } else if (type === 'exterior') {
              addBotMessage("Exterior painting. How many stories is the home?");
              setStep(3);
            } else if (type === 'cabinets') {
              addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");
              setStep(4);
            }
          };
        
          const handleInteriorRoom = (type: string) => {
            addUserMessage(type);
            setRoomType(type);
            addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");
            setStep(2.1);
          };
        
          const submitInteriorDimensions = () => {
            addUserMessage(`${width}x${length}x${height} ft`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handleExteriorStories = (val: string) => {
            addUserMessage(val);
            setStories(val);
            addBotMessage("What type of siding do you have?");
            setStep(3.1);
          };
        
          const handleExteriorSiding = (val: string) => {
            addUserMessage(val);
            setSiding(val);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const submitCabinets = () => {
            addUserMessage(`${cabinetCount} doors/drawers`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handlePrep = (prep: PrepLevel, label: string) => {
            addUserMessage(label);
            setPrepLevel(prep);
            addBotMessage("Calculating your estimate...", 400);
            setTimeout(() => {
              calculateFinal();
            }, 1500);
            setStep(6);
          };
        
          const calculateFinal = () => {
            let low = 0;
            let high = 0;
        
            if (projectType === 'interior') {
              const wallArea = 2 * (width + length) * height;
              const base = wallArea * 3.50;
              const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;
              const total = base + prepVal + 250;
              low = total * 0.90;
              high = total * 1.15;
            } else if (projectType === 'exterior') {
              let base = 3500;
              if (stories === '2 Story') base = 5500;
              if (stories === '3+ Story') base = 8500;
              
              let mult = 1.0;
              if (siding === 'Stucco') mult = 1.3;
              if (siding === 'Brick / Masonry') mult = 1.4;
        
              const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);
              low = total * 0.85;
              high = total * 1.20;
            } else if (projectType === 'cabinets') {
              const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);
              low = total * 0.90;
              high = total * 1.15;
            }
        
            low = Math.round(low / 100) * 100;
            high = Math.round(high / 100) * 100;
        
            addBotMessage(
              <div className="space-y-4 reveal-up">
                <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>
                <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>
                  <p className="eyebrow mb-2">Estimated Range</p>
                  <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>
                </div>
                <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>
              </div>
            , 200, true);
        
            setStep(7);
          };
        
          const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {
            e.preventDefault();
            const payload = {
              source: 'Chatbot Estimate',
              page: '/estimate',
              name, phone, email, city,
              projectType, prepLevel,
              notes: `Project: ${projectType} \nPrep: ${prepLevel}`
            };
        
            setStatus('submitting');
            try {
              const response = await fetch('/api/leads', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload),
              });
              if (response.ok) {
                setStatus('sent');
                addUserMessage("Submitted my info.");
                addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);
              } else {
                setStatus('fallback');
              }
            } catch {
              setStatus('fallback');
            }
          };
        
          const containerVariants = {
            hidden: { opacity: 0 },
            show: {
              opacity: 1,
              transition: { staggerChildren: 0.1 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, y: 15, scale: 0.95 },
            show: { opacity: 1, y: 0, scale: 1, transition: springConfig }
          };
        
          return (
            <PageTransition>
              <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">
                
                <div className="noise-overlay"></div>
        
                <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">
                  
                  {/* Header */}
                  <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">
                    <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">
                       <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />
                    </div>
                    <div>
                      <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>
                      <p className="eyebrow flex items-center gap-2 mt-1">
                        <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online
                      </p>
                    </div>
                  </div>
        
                  {/* Chat Area */}
                  <div 
                    ref={scrollRef}
                    className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"
                  >
                    <AnimatePresence initial={false}>
                      {messages.map((msg) => (
                        <motion.div
                          key={msg.id}
                          initial={{ opacity: 0, y: 20, scale: 0.95 }}
                          animate={{ opacity: 1, y: 0, scale: 1 }}
                          transition={springConfig}
                          className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}
                        >
                          <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>
                            
                            <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>
                              {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}
                            </div>
        
                            <div className={cn(
                              "p-5 text-sm leading-relaxed",
                              msg.sender === 'user' 
                                ? "bg-[#FF5A00] text-white" 
                                : msg.isComponent 
                                  ? "bg-transparent p-0 w-full"
                                  : "bg-[#11100d] border border-white/10 text-[#c9c1b4]"
                            )}>
                              {msg.text}
                            </div>
                          </div>
                        </motion.div>
                      ))}
        
                      {isTyping && (
                        <motion.div
                          initial={{ opacity: 0, y: 10 }}
                          animate={{ opacity: 1, y: 0 }}
                          exit={{ opacity: 0, scale: 0.95 }}
                          transition={{ duration: 0.2 }}
                          className="flex justify-start"
                        >
                          <div className="flex gap-4">
                            <div className="shrink-0 w-10 h-10 flex items-center justify-center bg-[#182023] text-white border border-white/10">
                              <Bot size={18} strokeWidth={1.5} />
                            </div>
                            <div className="bg-[#11100d] border border-white/10 p-5 flex items-center gap-2 h-[60px]">
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.3s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.15s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce"></div>
                            </div>
                          </div>
                        </motion.div>
                      )}
                    </AnimatePresence>
                  </div>
        
                  {/* Controls Footer */}
                  <div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-[#050505] via-[#050505]/95 to-transparent pt-12 pb-6 px-4 sm:px-8 border-t border-white/5 z-20">
                     <AnimatePresence mode="wait">
                       {step === 1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col sm:flex-row gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('interior', 'Interior Rooms')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Interior Rooms</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('exterior', 'Exterior Painting')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Exterior Painting</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('cabinets', 'Cabinet Refinishing')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Cabinet Refinishing</motion.button>
                         </motion.div>
                       )}
        
                       {step === 2 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Bedroom', 'Living Room', 'Kitchen', 'Bathroom', 'Hallway', 'Other'].map(r => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={r} onClick={() => handleInteriorRoom(r)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">{r}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 2.1 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="w" label="Width" value={width} min={5} max={40} suffix="FT" onChange={setWidth} />
                            <RangeSlider id="l" label="Length" value={length} min={5} max={40} suffix="FT" onChange={setLength} />
                            <RangeSlider id="h" label="Ceiling Height" value={height} min={7} max={20} suffix="FT" onChange={setHeight} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitInteriorDimensions} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Dimensions</motion.button>
                         </motion.div>
                       )}
        
                       {step === 3 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           {['1 Story', '2 Story', '3+ Story'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorStories(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-left">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 3.1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Wood / LP SmartSide', 'Stucco', 'Vinyl / Aluminum', 'Brick / Masonry'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorSiding(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-center flex items-center justify-center h-full">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 4 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="c" label="Total Doors & Drawers" value={cabinetCount} min={5} max={60} onChange={setCabinetCount} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitCabinets} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Count</motion.button>
                         </motion.div>
                       )}
        
                       {step === 5 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('standard', 'Standard Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-white">Standard Prep</p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Light αª╕αªéαªòαªƒ, minor caulk, 1 coat primer & topcoat. Great for minor refreshes.</p>
                           </motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('premium', 'Premium Detail Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-[#FF5A00] flex items-center gap-2">Premium Detail Prep <span className="bg-[#FF5A00]/20 text-[#FF5A00] text-[10px] uppercase px-2 py-0.5 tracking-wider font-bold">Recommended</span></p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Elite multi-stage sanding, deep caulking, wood stabilization, premium coats.</p>
                           </motion.button>
                         </motion.div>
                       )}
        
                       {step === 7 && !isTyping && status !== 'sent' && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={springConfig} className="bg-[#11100d] border border-[#FF5A00]/30 p-6 shadow-2xl relative overflow-hidden">
                           <div className="absolute top-0 left-0 right-0 h-1 bg-[#FF5A00]"></div>
                           <form onSubmit={handleFinalSubmit} className="space-y-4">
                             <p className="eyebrow mb-4">Request Walkthrough</p>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="text" placeholder="Name" value={name} onChange={e => setName(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="tel" placeholder="Phone" value={phone} onChange={e => setPhone(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="email" placeholder="Email" value={email} onChange={e => setEmail(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="text" placeholder="City" value={city} onChange={e => setCity(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} disabled={status === 'submitting'} type="submit" className="w-full bg-[#FF5A00] text-white p-4 text-sm font-bold flex items-center justify-center gap-2 hover:bg-[#ff7a33] transition-colors disabled:opacity-50 mt-2 shimmer-cta">
                               {status === 'submitting' ? <Loader2 className="animate-spin" size={16} /> : 'Lock In Estimate'}
                             </motion.button>
                           </form>
                         </motion.div>
                       )}
                     </AnimatePresence>
                  </div>
        
                </div>
                <div className="text-center text-xs text-gray-600 mt-2 pb-4">
                  reg: ir816596 | painting
                </div>
              </section>
            </PageTransition>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:158:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: T1.22 Room Calculator - Room dimensions width/length changes update wall surface area calculations
    not ok 22 - T1.22 Room Calculator - Room dimensions width/length changes update wall surface area calculations
      ---
      duration_ms: 2.9434
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:162:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /2 \* \(dimensions\.width \+ dimensions\.length\) \* dimensions\.ceilingHeight/. Input:
        
        "'use client';\n" +
          '\n' +
          "import { useState, FormEvent, useEffect, useRef } from 'react';\n" +
          "import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';\n" +
          "import PageTransition from '../components/PageTransition';\n" +
          "import RangeSlider from '../components/RangeSlider';\n" +
          "import { trackEvent } from '../lib/analytics';\n" +
          "import { buildEstimateMailto } from '../lib/contact';\n" +
          "import { motion, AnimatePresence } from 'motion/react';\n" +
          "import { cn } from '../lib/utils';\n" +
          '\n' +
          "type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;\n" +
          "type PrepLevel = 'standard' | 'premium' | null;\n" +
          '\n' +
          'interface ChatMessage {\n' +
          '  id: string;\n' +
          "  sender: 'bot' | 'user';\n" +
          '  text: React.ReactNode;\n' +
          '  isComponent?: boolean;\n' +
          '}\n' +
          '\n' +
          'const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };\n' +
          '\n' +
          'export default function EstimatePage() {\n' +
          '  const [messages, setMessages] = useState<ChatMessage[]>([]);\n' +
          '  const [isTyping, setIsTyping] = useState(false);\n' +
          '  const scrollRef = useRef<HTMLDivElement>(null);\n' +
          '\n' +
          '  // State\n' +
          '  const [step, setStep] = useState(0);\n' +
          '  const [projectType, setProjectType] = useState<ProjectType>(null);\n' +
          '  \n' +
          '  // Interior State\n' +
          "  const [roomType, setRoomType] = useState('Bedroom');\n" +
          '  const [width, setWidth] = useState(12);\n' +
          '  const [length, setLength] = useState(14);\n' +
          '  const [height, setHeight] = useState(8);\n' +
          '\n' +
          '  // Exterior State\n' +
          "  const [stories, setStories] = useState('1 Story');\n" +
          "  const [siding, setSiding] = useState('Wood / LP SmartSide');\n" +
          '\n' +
          '  // Cabinet State\n' +
          '  const [cabinetCount, setCabinetCount] = useState(20);\n' +
          '\n' +
          '  // Common State\n' +
          '  const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);\n' +
          '\n' +
          '  // Lead State\n' +
          "  const [name, setName] = useState('');\n" +
          "  const [phone, setPhone] = useState('');\n" +
          "  const [email, setEmail] = useState('');\n" +
          "  const [city, setCity] = useState('');\n" +
          "  const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');\n" +
          "  const [submitMessage, setSubmitMessage] = useState('');\n" +
          '\n' +
          '  const scrollToBottom = () => {\n' +
          '    if (scrollRef.current) {\n' +
          '      scrollRef.current.scrollTo({\n' +
          '        top: scrollRef.current.scrollHeight,\n' +
          "        behavior: 'smooth'\n" +
          '      });\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  useEffect(() => {\n' +
          '    scrollToBottom();\n' +
          '  }, [messages, isTyping, step]);\n' +
          '\n' +
          '  const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {\n' +
          '    setIsTyping(true);\n' +
          '    setTimeout(() => {\n' +
          '      setIsTyping(false);\n' +
          "      setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);\n" +
          '    }, delay);\n' +
          '  };\n' +
          '\n' +
          '  const addUserMessage = (text: string) => {\n' +
          "    setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);\n" +
          '  };\n' +
          '\n' +
          '  // Initialize\n' +
          '  useEffect(() => {\n' +
          '    if (step === 0) {\n' +
          `      addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);\n` +
          '      setTimeout(() => setStep(1), 1200);\n' +
          '    }\n' +
          '  }, []);\n' +
          '\n' +
          '  const handleProjectType = (type: ProjectType, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setProjectType(type);\n' +
          '    \n' +
          "    if (type === 'interior') {\n" +
          '      addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");\n' +
          '      setStep(2);\n' +
          "    } else if (type === 'exterior') {\n" +
          '      addBotMessage("Exterior painting. How many stories is the home?");\n' +
          '      setStep(3);\n' +
          "    } else if (type === 'cabinets') {\n" +
          '      addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");\n' +
          '      setStep(4);\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const handleInteriorRoom = (type: string) => {\n' +
          '    addUserMessage(type);\n' +
          '    setRoomType(type);\n' +
          '    addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");\n' +
          '    setStep(2.1);\n' +
          '  };\n' +
          '\n' +
          '  const submitInteriorDimensions = () => {\n' +
          '    addUserMessage(`${width}x${length}x${height} ft`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorStories = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setStories(val);\n' +
          '    addBotMessage("What type of siding do you have?");\n' +
          '    setStep(3.1);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorSiding = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setSiding(val);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const submitCabinets = () => {\n' +
          '    addUserMessage(`${cabinetCount} doors/drawers`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handlePrep = (prep: PrepLevel, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setPrepLevel(prep);\n' +
          '    addBotMessage("Calculating your estimate...", 400);\n' +
          '    setTimeout(() => {\n' +
          '      calculateFinal();\n' +
          '    }, 1500);\n' +
          '    setStep(6);\n' +
          '  };\n' +
          '\n' +
          '  const calculateFinal = () => {\n' +
          '    let low = 0;\n' +
          '    let high = 0;\n' +
          '\n' +
          "    if (projectType === 'interior') {\n" +
          '      const wallArea = 2 * (width + length) * height;\n' +
          '      const base = wallArea * 3.50;\n' +
          "      const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;\n" +
          '      const total = base + prepVal + 250;\n' +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          "    } else if (projectType === 'exterior') {\n" +
          '      let base = 3500;\n' +
          "      if (stories === '2 Story') base = 5500;\n" +
          "      if (stories === '3+ Story') base = 8500;\n" +
          '      \n' +
          '      let mult = 1.0;\n' +
          "      if (siding === 'Stucco') mult = 1.3;\n" +
          "      if (siding === 'Brick / Masonry') mult = 1.4;\n" +
          '\n' +
          "      const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);\n" +
          '      low = total * 0.85;\n' +
          '      high = total * 1.20;\n' +
          "    } else if (projectType === 'cabinets') {\n" +
          "      const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);\n" +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          '    }\n' +
          '\n' +
          '    low = Math.round(low / 100) * 100;\n' +
          '    high = Math.round(high / 100) * 100;\n' +
          '\n' +
          '    addBotMessage(\n' +
          '      <div className="space-y-4 reveal-up">\n' +
          '        <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>\n' +
          '        <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">\n' +
          '          <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>\n' +
          '          <p className="eyebrow mb-2">Estimated Range</p>\n' +
          '          <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>\n' +
          '        </div>\n' +
          '        <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>\n' +
          '      </div>\n' +
          '    , 200, true);\n' +
          '\n' +
          '    setStep(7);\n' +
          '  };\n' +
          '\n' +
          '  const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {\n' +
          '    e.preventDefault();\n' +
          '    const payload = {\n' +
          "      source: 'Chatbot Estimate',\n" +
          "      page: '/estimate',\n" +
          '      name, phone, email, city,\n' +
          '      projectType, prepLevel,\n' +
          '      notes: `Project: ${projectType} \\nPrep: ${prepLevel}`\n' +
          '    };\n' +
          '\n' +
          "    setStatus('submitting');\n" +
          '    try {\n' +
          "      const response = await fetch('/api/leads', {\n" +
          "        method: 'POST',\n" +
          "        headers: { 'Content-Type': 'application/json' },\n" +
          '        body: JSON.stringify(payload),\n' +
          '      });\n' +
          '      if (response.ok) {\n' +
          "        setStatus('sent');\n" +
          '        addUserMessage("Submitted my info.");\n' +
          `        addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);\n` +
          '      } else {\n' +
          "        setStatus('fallback');\n" +
          '      }\n' +
          '    } catch {\n' +
          "      setStatus('fallback');\n" +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const containerVariants = {\n' +
          '    hidden: { opacity: 0 },\n' +
          '    show: {\n' +
          '      opacity: 1,\n' +
          '      transition: { staggerChildren: 0.1 }\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const itemVariants = {\n' +
          '    hidden: { opacity: 0, y: 15, scale: 0.95 },\n' +
          '    show: { opacity: 1, y: 0, scale: 1, transition: springConfig }\n' +
          '  };\n' +
          '\n' +
          '  return (\n' +
          '    <PageTransition>\n' +
          '      <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">\n' +
          '        \n' +
          '        <div className="noise-overlay"></div>\n' +
          '\n' +
          '        <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">\n' +
          '          \n' +
          '          {/* Header */}\n' +
          '          <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">\n' +
          '            <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">\n' +
          '               <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />\n' +
          '            </div>\n' +
          '            <div>\n' +
          '              <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>\n' +
          '              <p className="eyebrow flex items-center gap-2 mt-1">\n' +
          '                <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online\n' +
          '              </p>\n' +
          '            </div>\n' +
          '          </div>\n' +
          '\n' +
          '          {/* Chat Area */}\n' +
          '          <div \n' +
          '            ref={scrollRef}\n' +
          '            className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"\n' +
          '          >\n' +
          '            <AnimatePresence initial={false}>\n' +
          '              {messages.map((msg) => (\n' +
          '                <motion.div\n' +
          '                  key={msg.id}\n' +
          '                  initial={{ opacity: 0, y: 20, scale: 0.95 }}\n' +
          '                  animate={{ opacity: 1, y: 0, scale: 1 }}\n' +
          '                  transition={springConfig}\n' +
          "                  className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}\n" +
          '                >\n' +
          "                  <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>\n" +
          '                    \n' +
          "                    <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>\n" +
          "                      {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}\n" +
          '                    </div>\n' +
          '\n' +
          '                    <div className={cn(\n' +
          '                      "p-5 text-sm l'... 10894 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import { useState, FormEvent, useEffect, useRef } from 'react';
        import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';
        import PageTransition from '../components/PageTransition';
        import RangeSlider from '../components/RangeSlider';
        import { trackEvent } from '../lib/analytics';
        import { buildEstimateMailto } from '../lib/contact';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '../lib/utils';
        
        type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;
        type PrepLevel = 'standard' | 'premium' | null;
        
        interface ChatMessage {
          id: string;
          sender: 'bot' | 'user';
          text: React.ReactNode;
          isComponent?: boolean;
        }
        
        const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };
        
        export default function EstimatePage() {
          const [messages, setMessages] = useState<ChatMessage[]>([]);
          const [isTyping, setIsTyping] = useState(false);
          const scrollRef = useRef<HTMLDivElement>(null);
        
          // State
          const [step, setStep] = useState(0);
          const [projectType, setProjectType] = useState<ProjectType>(null);
          
          // Interior State
          const [roomType, setRoomType] = useState('Bedroom');
          const [width, setWidth] = useState(12);
          const [length, setLength] = useState(14);
          const [height, setHeight] = useState(8);
        
          // Exterior State
          const [stories, setStories] = useState('1 Story');
          const [siding, setSiding] = useState('Wood / LP SmartSide');
        
          // Cabinet State
          const [cabinetCount, setCabinetCount] = useState(20);
        
          // Common State
          const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);
        
          // Lead State
          const [name, setName] = useState('');
          const [phone, setPhone] = useState('');
          const [email, setEmail] = useState('');
          const [city, setCity] = useState('');
          const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');
          const [submitMessage, setSubmitMessage] = useState('');
        
          const scrollToBottom = () => {
            if (scrollRef.current) {
              scrollRef.current.scrollTo({
                top: scrollRef.current.scrollHeight,
                behavior: 'smooth'
              });
            }
          };
        
          useEffect(() => {
            scrollToBottom();
          }, [messages, isTyping, step]);
        
          const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {
            setIsTyping(true);
            setTimeout(() => {
              setIsTyping(false);
              setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);
            }, delay);
          };
        
          const addUserMessage = (text: string) => {
            setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);
          };
        
          // Initialize
          useEffect(() => {
            if (step === 0) {
              addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);
              setTimeout(() => setStep(1), 1200);
            }
          }, []);
        
          const handleProjectType = (type: ProjectType, label: string) => {
            addUserMessage(label);
            setProjectType(type);
            
            if (type === 'interior') {
              addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");
              setStep(2);
            } else if (type === 'exterior') {
              addBotMessage("Exterior painting. How many stories is the home?");
              setStep(3);
            } else if (type === 'cabinets') {
              addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");
              setStep(4);
            }
          };
        
          const handleInteriorRoom = (type: string) => {
            addUserMessage(type);
            setRoomType(type);
            addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");
            setStep(2.1);
          };
        
          const submitInteriorDimensions = () => {
            addUserMessage(`${width}x${length}x${height} ft`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handleExteriorStories = (val: string) => {
            addUserMessage(val);
            setStories(val);
            addBotMessage("What type of siding do you have?");
            setStep(3.1);
          };
        
          const handleExteriorSiding = (val: string) => {
            addUserMessage(val);
            setSiding(val);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const submitCabinets = () => {
            addUserMessage(`${cabinetCount} doors/drawers`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handlePrep = (prep: PrepLevel, label: string) => {
            addUserMessage(label);
            setPrepLevel(prep);
            addBotMessage("Calculating your estimate...", 400);
            setTimeout(() => {
              calculateFinal();
            }, 1500);
            setStep(6);
          };
        
          const calculateFinal = () => {
            let low = 0;
            let high = 0;
        
            if (projectType === 'interior') {
              const wallArea = 2 * (width + length) * height;
              const base = wallArea * 3.50;
              const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;
              const total = base + prepVal + 250;
              low = total * 0.90;
              high = total * 1.15;
            } else if (projectType === 'exterior') {
              let base = 3500;
              if (stories === '2 Story') base = 5500;
              if (stories === '3+ Story') base = 8500;
              
              let mult = 1.0;
              if (siding === 'Stucco') mult = 1.3;
              if (siding === 'Brick / Masonry') mult = 1.4;
        
              const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);
              low = total * 0.85;
              high = total * 1.20;
            } else if (projectType === 'cabinets') {
              const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);
              low = total * 0.90;
              high = total * 1.15;
            }
        
            low = Math.round(low / 100) * 100;
            high = Math.round(high / 100) * 100;
        
            addBotMessage(
              <div className="space-y-4 reveal-up">
                <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>
                <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>
                  <p className="eyebrow mb-2">Estimated Range</p>
                  <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>
                </div>
                <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>
              </div>
            , 200, true);
        
            setStep(7);
          };
        
          const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {
            e.preventDefault();
            const payload = {
              source: 'Chatbot Estimate',
              page: '/estimate',
              name, phone, email, city,
              projectType, prepLevel,
              notes: `Project: ${projectType} \nPrep: ${prepLevel}`
            };
        
            setStatus('submitting');
            try {
              const response = await fetch('/api/leads', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload),
              });
              if (response.ok) {
                setStatus('sent');
                addUserMessage("Submitted my info.");
                addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);
              } else {
                setStatus('fallback');
              }
            } catch {
              setStatus('fallback');
            }
          };
        
          const containerVariants = {
            hidden: { opacity: 0 },
            show: {
              opacity: 1,
              transition: { staggerChildren: 0.1 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, y: 15, scale: 0.95 },
            show: { opacity: 1, y: 0, scale: 1, transition: springConfig }
          };
        
          return (
            <PageTransition>
              <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">
                
                <div className="noise-overlay"></div>
        
                <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">
                  
                  {/* Header */}
                  <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">
                    <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">
                       <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />
                    </div>
                    <div>
                      <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>
                      <p className="eyebrow flex items-center gap-2 mt-1">
                        <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online
                      </p>
                    </div>
                  </div>
        
                  {/* Chat Area */}
                  <div 
                    ref={scrollRef}
                    className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"
                  >
                    <AnimatePresence initial={false}>
                      {messages.map((msg) => (
                        <motion.div
                          key={msg.id}
                          initial={{ opacity: 0, y: 20, scale: 0.95 }}
                          animate={{ opacity: 1, y: 0, scale: 1 }}
                          transition={springConfig}
                          className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}
                        >
                          <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>
                            
                            <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>
                              {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}
                            </div>
        
                            <div className={cn(
                              "p-5 text-sm leading-relaxed",
                              msg.sender === 'user' 
                                ? "bg-[#FF5A00] text-white" 
                                : msg.isComponent 
                                  ? "bg-transparent p-0 w-full"
                                  : "bg-[#11100d] border border-white/10 text-[#c9c1b4]"
                            )}>
                              {msg.text}
                            </div>
                          </div>
                        </motion.div>
                      ))}
        
                      {isTyping && (
                        <motion.div
                          initial={{ opacity: 0, y: 10 }}
                          animate={{ opacity: 1, y: 0 }}
                          exit={{ opacity: 0, scale: 0.95 }}
                          transition={{ duration: 0.2 }}
                          className="flex justify-start"
                        >
                          <div className="flex gap-4">
                            <div className="shrink-0 w-10 h-10 flex items-center justify-center bg-[#182023] text-white border border-white/10">
                              <Bot size={18} strokeWidth={1.5} />
                            </div>
                            <div className="bg-[#11100d] border border-white/10 p-5 flex items-center gap-2 h-[60px]">
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.3s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.15s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce"></div>
                            </div>
                          </div>
                        </motion.div>
                      )}
                    </AnimatePresence>
                  </div>
        
                  {/* Controls Footer */}
                  <div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-[#050505] via-[#050505]/95 to-transparent pt-12 pb-6 px-4 sm:px-8 border-t border-white/5 z-20">
                     <AnimatePresence mode="wait">
                       {step === 1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col sm:flex-row gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('interior', 'Interior Rooms')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Interior Rooms</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('exterior', 'Exterior Painting')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Exterior Painting</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('cabinets', 'Cabinet Refinishing')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Cabinet Refinishing</motion.button>
                         </motion.div>
                       )}
        
                       {step === 2 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Bedroom', 'Living Room', 'Kitchen', 'Bathroom', 'Hallway', 'Other'].map(r => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={r} onClick={() => handleInteriorRoom(r)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">{r}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 2.1 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="w" label="Width" value={width} min={5} max={40} suffix="FT" onChange={setWidth} />
                            <RangeSlider id="l" label="Length" value={length} min={5} max={40} suffix="FT" onChange={setLength} />
                            <RangeSlider id="h" label="Ceiling Height" value={height} min={7} max={20} suffix="FT" onChange={setHeight} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitInteriorDimensions} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Dimensions</motion.button>
                         </motion.div>
                       )}
        
                       {step === 3 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           {['1 Story', '2 Story', '3+ Story'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorStories(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-left">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 3.1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Wood / LP SmartSide', 'Stucco', 'Vinyl / Aluminum', 'Brick / Masonry'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorSiding(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-center flex items-center justify-center h-full">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 4 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="c" label="Total Doors & Drawers" value={cabinetCount} min={5} max={60} onChange={setCabinetCount} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitCabinets} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Count</motion.button>
                         </motion.div>
                       )}
        
                       {step === 5 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('standard', 'Standard Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-white">Standard Prep</p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Light αª╕αªéαªòαªƒ, minor caulk, 1 coat primer & topcoat. Great for minor refreshes.</p>
                           </motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('premium', 'Premium Detail Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-[#FF5A00] flex items-center gap-2">Premium Detail Prep <span className="bg-[#FF5A00]/20 text-[#FF5A00] text-[10px] uppercase px-2 py-0.5 tracking-wider font-bold">Recommended</span></p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Elite multi-stage sanding, deep caulking, wood stabilization, premium coats.</p>
                           </motion.button>
                         </motion.div>
                       )}
        
                       {step === 7 && !isTyping && status !== 'sent' && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={springConfig} className="bg-[#11100d] border border-[#FF5A00]/30 p-6 shadow-2xl relative overflow-hidden">
                           <div className="absolute top-0 left-0 right-0 h-1 bg-[#FF5A00]"></div>
                           <form onSubmit={handleFinalSubmit} className="space-y-4">
                             <p className="eyebrow mb-4">Request Walkthrough</p>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="text" placeholder="Name" value={name} onChange={e => setName(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="tel" placeholder="Phone" value={phone} onChange={e => setPhone(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="email" placeholder="Email" value={email} onChange={e => setEmail(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="text" placeholder="City" value={city} onChange={e => setCity(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} disabled={status === 'submitting'} type="submit" className="w-full bg-[#FF5A00] text-white p-4 text-sm font-bold flex items-center justify-center gap-2 hover:bg-[#ff7a33] transition-colors disabled:opacity-50 mt-2 shimmer-cta">
                               {status === 'submitting' ? <Loader2 className="animate-spin" size={16} /> : 'Lock In Estimate'}
                             </motion.button>
                           </form>
                         </motion.div>
                       )}
                     </AnimatePresence>
                  </div>
        
                </div>
                <div className="text-center text-xs text-gray-600 mt-2 pb-4">
                  reg: ir816596 | painting
                </div>
              </section>
            </PageTransition>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:164:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: T1.23 Room Calculator - Adjusting doors and windows counts updates openings cost
    not ok 23 - T1.23 Room Calculator - Adjusting doors and windows counts updates openings cost
      ---
      duration_ms: 1.9936
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:183:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /doorsCount \* 150 \+ .*windowsCount \* 100/. Input:
        
        "'use client';\n" +
          '\n' +
          "import { useState, FormEvent, useEffect, useRef } from 'react';\n" +
          "import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';\n" +
          "import PageTransition from '../components/PageTransition';\n" +
          "import RangeSlider from '../components/RangeSlider';\n" +
          "import { trackEvent } from '../lib/analytics';\n" +
          "import { buildEstimateMailto } from '../lib/contact';\n" +
          "import { motion, AnimatePresence } from 'motion/react';\n" +
          "import { cn } from '../lib/utils';\n" +
          '\n' +
          "type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;\n" +
          "type PrepLevel = 'standard' | 'premium' | null;\n" +
          '\n' +
          'interface ChatMessage {\n' +
          '  id: string;\n' +
          "  sender: 'bot' | 'user';\n" +
          '  text: React.ReactNode;\n' +
          '  isComponent?: boolean;\n' +
          '}\n' +
          '\n' +
          'const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };\n' +
          '\n' +
          'export default function EstimatePage() {\n' +
          '  const [messages, setMessages] = useState<ChatMessage[]>([]);\n' +
          '  const [isTyping, setIsTyping] = useState(false);\n' +
          '  const scrollRef = useRef<HTMLDivElement>(null);\n' +
          '\n' +
          '  // State\n' +
          '  const [step, setStep] = useState(0);\n' +
          '  const [projectType, setProjectType] = useState<ProjectType>(null);\n' +
          '  \n' +
          '  // Interior State\n' +
          "  const [roomType, setRoomType] = useState('Bedroom');\n" +
          '  const [width, setWidth] = useState(12);\n' +
          '  const [length, setLength] = useState(14);\n' +
          '  const [height, setHeight] = useState(8);\n' +
          '\n' +
          '  // Exterior State\n' +
          "  const [stories, setStories] = useState('1 Story');\n" +
          "  const [siding, setSiding] = useState('Wood / LP SmartSide');\n" +
          '\n' +
          '  // Cabinet State\n' +
          '  const [cabinetCount, setCabinetCount] = useState(20);\n' +
          '\n' +
          '  // Common State\n' +
          '  const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);\n' +
          '\n' +
          '  // Lead State\n' +
          "  const [name, setName] = useState('');\n" +
          "  const [phone, setPhone] = useState('');\n" +
          "  const [email, setEmail] = useState('');\n" +
          "  const [city, setCity] = useState('');\n" +
          "  const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');\n" +
          "  const [submitMessage, setSubmitMessage] = useState('');\n" +
          '\n' +
          '  const scrollToBottom = () => {\n' +
          '    if (scrollRef.current) {\n' +
          '      scrollRef.current.scrollTo({\n' +
          '        top: scrollRef.current.scrollHeight,\n' +
          "        behavior: 'smooth'\n" +
          '      });\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  useEffect(() => {\n' +
          '    scrollToBottom();\n' +
          '  }, [messages, isTyping, step]);\n' +
          '\n' +
          '  const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {\n' +
          '    setIsTyping(true);\n' +
          '    setTimeout(() => {\n' +
          '      setIsTyping(false);\n' +
          "      setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);\n" +
          '    }, delay);\n' +
          '  };\n' +
          '\n' +
          '  const addUserMessage = (text: string) => {\n' +
          "    setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);\n" +
          '  };\n' +
          '\n' +
          '  // Initialize\n' +
          '  useEffect(() => {\n' +
          '    if (step === 0) {\n' +
          `      addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);\n` +
          '      setTimeout(() => setStep(1), 1200);\n' +
          '    }\n' +
          '  }, []);\n' +
          '\n' +
          '  const handleProjectType = (type: ProjectType, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setProjectType(type);\n' +
          '    \n' +
          "    if (type === 'interior') {\n" +
          '      addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");\n' +
          '      setStep(2);\n' +
          "    } else if (type === 'exterior') {\n" +
          '      addBotMessage("Exterior painting. How many stories is the home?");\n' +
          '      setStep(3);\n' +
          "    } else if (type === 'cabinets') {\n" +
          '      addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");\n' +
          '      setStep(4);\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const handleInteriorRoom = (type: string) => {\n' +
          '    addUserMessage(type);\n' +
          '    setRoomType(type);\n' +
          '    addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");\n' +
          '    setStep(2.1);\n' +
          '  };\n' +
          '\n' +
          '  const submitInteriorDimensions = () => {\n' +
          '    addUserMessage(`${width}x${length}x${height} ft`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorStories = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setStories(val);\n' +
          '    addBotMessage("What type of siding do you have?");\n' +
          '    setStep(3.1);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorSiding = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setSiding(val);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const submitCabinets = () => {\n' +
          '    addUserMessage(`${cabinetCount} doors/drawers`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handlePrep = (prep: PrepLevel, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setPrepLevel(prep);\n' +
          '    addBotMessage("Calculating your estimate...", 400);\n' +
          '    setTimeout(() => {\n' +
          '      calculateFinal();\n' +
          '    }, 1500);\n' +
          '    setStep(6);\n' +
          '  };\n' +
          '\n' +
          '  const calculateFinal = () => {\n' +
          '    let low = 0;\n' +
          '    let high = 0;\n' +
          '\n' +
          "    if (projectType === 'interior') {\n" +
          '      const wallArea = 2 * (width + length) * height;\n' +
          '      const base = wallArea * 3.50;\n' +
          "      const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;\n" +
          '      const total = base + prepVal + 250;\n' +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          "    } else if (projectType === 'exterior') {\n" +
          '      let base = 3500;\n' +
          "      if (stories === '2 Story') base = 5500;\n" +
          "      if (stories === '3+ Story') base = 8500;\n" +
          '      \n' +
          '      let mult = 1.0;\n' +
          "      if (siding === 'Stucco') mult = 1.3;\n" +
          "      if (siding === 'Brick / Masonry') mult = 1.4;\n" +
          '\n' +
          "      const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);\n" +
          '      low = total * 0.85;\n' +
          '      high = total * 1.20;\n' +
          "    } else if (projectType === 'cabinets') {\n" +
          "      const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);\n" +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          '    }\n' +
          '\n' +
          '    low = Math.round(low / 100) * 100;\n' +
          '    high = Math.round(high / 100) * 100;\n' +
          '\n' +
          '    addBotMessage(\n' +
          '      <div className="space-y-4 reveal-up">\n' +
          '        <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>\n' +
          '        <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">\n' +
          '          <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>\n' +
          '          <p className="eyebrow mb-2">Estimated Range</p>\n' +
          '          <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>\n' +
          '        </div>\n' +
          '        <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>\n' +
          '      </div>\n' +
          '    , 200, true);\n' +
          '\n' +
          '    setStep(7);\n' +
          '  };\n' +
          '\n' +
          '  const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {\n' +
          '    e.preventDefault();\n' +
          '    const payload = {\n' +
          "      source: 'Chatbot Estimate',\n" +
          "      page: '/estimate',\n" +
          '      name, phone, email, city,\n' +
          '      projectType, prepLevel,\n' +
          '      notes: `Project: ${projectType} \\nPrep: ${prepLevel}`\n' +
          '    };\n' +
          '\n' +
          "    setStatus('submitting');\n" +
          '    try {\n' +
          "      const response = await fetch('/api/leads', {\n" +
          "        method: 'POST',\n" +
          "        headers: { 'Content-Type': 'application/json' },\n" +
          '        body: JSON.stringify(payload),\n' +
          '      });\n' +
          '      if (response.ok) {\n' +
          "        setStatus('sent');\n" +
          '        addUserMessage("Submitted my info.");\n' +
          `        addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);\n` +
          '      } else {\n' +
          "        setStatus('fallback');\n" +
          '      }\n' +
          '    } catch {\n' +
          "      setStatus('fallback');\n" +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const containerVariants = {\n' +
          '    hidden: { opacity: 0 },\n' +
          '    show: {\n' +
          '      opacity: 1,\n' +
          '      transition: { staggerChildren: 0.1 }\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const itemVariants = {\n' +
          '    hidden: { opacity: 0, y: 15, scale: 0.95 },\n' +
          '    show: { opacity: 1, y: 0, scale: 1, transition: springConfig }\n' +
          '  };\n' +
          '\n' +
          '  return (\n' +
          '    <PageTransition>\n' +
          '      <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">\n' +
          '        \n' +
          '        <div className="noise-overlay"></div>\n' +
          '\n' +
          '        <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">\n' +
          '          \n' +
          '          {/* Header */}\n' +
          '          <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">\n' +
          '            <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">\n' +
          '               <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />\n' +
          '            </div>\n' +
          '            <div>\n' +
          '              <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>\n' +
          '              <p className="eyebrow flex items-center gap-2 mt-1">\n' +
          '                <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online\n' +
          '              </p>\n' +
          '            </div>\n' +
          '          </div>\n' +
          '\n' +
          '          {/* Chat Area */}\n' +
          '          <div \n' +
          '            ref={scrollRef}\n' +
          '            className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"\n' +
          '          >\n' +
          '            <AnimatePresence initial={false}>\n' +
          '              {messages.map((msg) => (\n' +
          '                <motion.div\n' +
          '                  key={msg.id}\n' +
          '                  initial={{ opacity: 0, y: 20, scale: 0.95 }}\n' +
          '                  animate={{ opacity: 1, y: 0, scale: 1 }}\n' +
          '                  transition={springConfig}\n' +
          "                  className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}\n" +
          '                >\n' +
          "                  <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>\n" +
          '                    \n' +
          "                    <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>\n" +
          "                      {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}\n" +
          '                    </div>\n' +
          '\n' +
          '                    <div className={cn(\n' +
          '                      "p-5 text-sm l'... 10894 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import { useState, FormEvent, useEffect, useRef } from 'react';
        import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';
        import PageTransition from '../components/PageTransition';
        import RangeSlider from '../components/RangeSlider';
        import { trackEvent } from '../lib/analytics';
        import { buildEstimateMailto } from '../lib/contact';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '../lib/utils';
        
        type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;
        type PrepLevel = 'standard' | 'premium' | null;
        
        interface ChatMessage {
          id: string;
          sender: 'bot' | 'user';
          text: React.ReactNode;
          isComponent?: boolean;
        }
        
        const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };
        
        export default function EstimatePage() {
          const [messages, setMessages] = useState<ChatMessage[]>([]);
          const [isTyping, setIsTyping] = useState(false);
          const scrollRef = useRef<HTMLDivElement>(null);
        
          // State
          const [step, setStep] = useState(0);
          const [projectType, setProjectType] = useState<ProjectType>(null);
          
          // Interior State
          const [roomType, setRoomType] = useState('Bedroom');
          const [width, setWidth] = useState(12);
          const [length, setLength] = useState(14);
          const [height, setHeight] = useState(8);
        
          // Exterior State
          const [stories, setStories] = useState('1 Story');
          const [siding, setSiding] = useState('Wood / LP SmartSide');
        
          // Cabinet State
          const [cabinetCount, setCabinetCount] = useState(20);
        
          // Common State
          const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);
        
          // Lead State
          const [name, setName] = useState('');
          const [phone, setPhone] = useState('');
          const [email, setEmail] = useState('');
          const [city, setCity] = useState('');
          const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');
          const [submitMessage, setSubmitMessage] = useState('');
        
          const scrollToBottom = () => {
            if (scrollRef.current) {
              scrollRef.current.scrollTo({
                top: scrollRef.current.scrollHeight,
                behavior: 'smooth'
              });
            }
          };
        
          useEffect(() => {
            scrollToBottom();
          }, [messages, isTyping, step]);
        
          const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {
            setIsTyping(true);
            setTimeout(() => {
              setIsTyping(false);
              setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);
            }, delay);
          };
        
          const addUserMessage = (text: string) => {
            setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);
          };
        
          // Initialize
          useEffect(() => {
            if (step === 0) {
              addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);
              setTimeout(() => setStep(1), 1200);
            }
          }, []);
        
          const handleProjectType = (type: ProjectType, label: string) => {
            addUserMessage(label);
            setProjectType(type);
            
            if (type === 'interior') {
              addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");
              setStep(2);
            } else if (type === 'exterior') {
              addBotMessage("Exterior painting. How many stories is the home?");
              setStep(3);
            } else if (type === 'cabinets') {
              addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");
              setStep(4);
            }
          };
        
          const handleInteriorRoom = (type: string) => {
            addUserMessage(type);
            setRoomType(type);
            addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");
            setStep(2.1);
          };
        
          const submitInteriorDimensions = () => {
            addUserMessage(`${width}x${length}x${height} ft`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handleExteriorStories = (val: string) => {
            addUserMessage(val);
            setStories(val);
            addBotMessage("What type of siding do you have?");
            setStep(3.1);
          };
        
          const handleExteriorSiding = (val: string) => {
            addUserMessage(val);
            setSiding(val);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const submitCabinets = () => {
            addUserMessage(`${cabinetCount} doors/drawers`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handlePrep = (prep: PrepLevel, label: string) => {
            addUserMessage(label);
            setPrepLevel(prep);
            addBotMessage("Calculating your estimate...", 400);
            setTimeout(() => {
              calculateFinal();
            }, 1500);
            setStep(6);
          };
        
          const calculateFinal = () => {
            let low = 0;
            let high = 0;
        
            if (projectType === 'interior') {
              const wallArea = 2 * (width + length) * height;
              const base = wallArea * 3.50;
              const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;
              const total = base + prepVal + 250;
              low = total * 0.90;
              high = total * 1.15;
            } else if (projectType === 'exterior') {
              let base = 3500;
              if (stories === '2 Story') base = 5500;
              if (stories === '3+ Story') base = 8500;
              
              let mult = 1.0;
              if (siding === 'Stucco') mult = 1.3;
              if (siding === 'Brick / Masonry') mult = 1.4;
        
              const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);
              low = total * 0.85;
              high = total * 1.20;
            } else if (projectType === 'cabinets') {
              const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);
              low = total * 0.90;
              high = total * 1.15;
            }
        
            low = Math.round(low / 100) * 100;
            high = Math.round(high / 100) * 100;
        
            addBotMessage(
              <div className="space-y-4 reveal-up">
                <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>
                <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>
                  <p className="eyebrow mb-2">Estimated Range</p>
                  <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>
                </div>
                <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>
              </div>
            , 200, true);
        
            setStep(7);
          };
        
          const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {
            e.preventDefault();
            const payload = {
              source: 'Chatbot Estimate',
              page: '/estimate',
              name, phone, email, city,
              projectType, prepLevel,
              notes: `Project: ${projectType} \nPrep: ${prepLevel}`
            };
        
            setStatus('submitting');
            try {
              const response = await fetch('/api/leads', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload),
              });
              if (response.ok) {
                setStatus('sent');
                addUserMessage("Submitted my info.");
                addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);
              } else {
                setStatus('fallback');
              }
            } catch {
              setStatus('fallback');
            }
          };
        
          const containerVariants = {
            hidden: { opacity: 0 },
            show: {
              opacity: 1,
              transition: { staggerChildren: 0.1 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, y: 15, scale: 0.95 },
            show: { opacity: 1, y: 0, scale: 1, transition: springConfig }
          };
        
          return (
            <PageTransition>
              <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">
                
                <div className="noise-overlay"></div>
        
                <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">
                  
                  {/* Header */}
                  <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">
                    <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">
                       <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />
                    </div>
                    <div>
                      <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>
                      <p className="eyebrow flex items-center gap-2 mt-1">
                        <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online
                      </p>
                    </div>
                  </div>
        
                  {/* Chat Area */}
                  <div 
                    ref={scrollRef}
                    className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"
                  >
                    <AnimatePresence initial={false}>
                      {messages.map((msg) => (
                        <motion.div
                          key={msg.id}
                          initial={{ opacity: 0, y: 20, scale: 0.95 }}
                          animate={{ opacity: 1, y: 0, scale: 1 }}
                          transition={springConfig}
                          className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}
                        >
                          <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>
                            
                            <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>
                              {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}
                            </div>
        
                            <div className={cn(
                              "p-5 text-sm leading-relaxed",
                              msg.sender === 'user' 
                                ? "bg-[#FF5A00] text-white" 
                                : msg.isComponent 
                                  ? "bg-transparent p-0 w-full"
                                  : "bg-[#11100d] border border-white/10 text-[#c9c1b4]"
                            )}>
                              {msg.text}
                            </div>
                          </div>
                        </motion.div>
                      ))}
        
                      {isTyping && (
                        <motion.div
                          initial={{ opacity: 0, y: 10 }}
                          animate={{ opacity: 1, y: 0 }}
                          exit={{ opacity: 0, scale: 0.95 }}
                          transition={{ duration: 0.2 }}
                          className="flex justify-start"
                        >
                          <div className="flex gap-4">
                            <div className="shrink-0 w-10 h-10 flex items-center justify-center bg-[#182023] text-white border border-white/10">
                              <Bot size={18} strokeWidth={1.5} />
                            </div>
                            <div className="bg-[#11100d] border border-white/10 p-5 flex items-center gap-2 h-[60px]">
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.3s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.15s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce"></div>
                            </div>
                          </div>
                        </motion.div>
                      )}
                    </AnimatePresence>
                  </div>
        
                  {/* Controls Footer */}
                  <div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-[#050505] via-[#050505]/95 to-transparent pt-12 pb-6 px-4 sm:px-8 border-t border-white/5 z-20">
                     <AnimatePresence mode="wait">
                       {step === 1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col sm:flex-row gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('interior', 'Interior Rooms')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Interior Rooms</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('exterior', 'Exterior Painting')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Exterior Painting</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('cabinets', 'Cabinet Refinishing')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Cabinet Refinishing</motion.button>
                         </motion.div>
                       )}
        
                       {step === 2 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Bedroom', 'Living Room', 'Kitchen', 'Bathroom', 'Hallway', 'Other'].map(r => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={r} onClick={() => handleInteriorRoom(r)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">{r}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 2.1 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="w" label="Width" value={width} min={5} max={40} suffix="FT" onChange={setWidth} />
                            <RangeSlider id="l" label="Length" value={length} min={5} max={40} suffix="FT" onChange={setLength} />
                            <RangeSlider id="h" label="Ceiling Height" value={height} min={7} max={20} suffix="FT" onChange={setHeight} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitInteriorDimensions} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Dimensions</motion.button>
                         </motion.div>
                       )}
        
                       {step === 3 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           {['1 Story', '2 Story', '3+ Story'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorStories(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-left">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 3.1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Wood / LP SmartSide', 'Stucco', 'Vinyl / Aluminum', 'Brick / Masonry'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorSiding(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-center flex items-center justify-center h-full">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 4 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="c" label="Total Doors & Drawers" value={cabinetCount} min={5} max={60} onChange={setCabinetCount} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitCabinets} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Count</motion.button>
                         </motion.div>
                       )}
        
                       {step === 5 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('standard', 'Standard Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-white">Standard Prep</p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Light αª╕αªéαªòαªƒ, minor caulk, 1 coat primer & topcoat. Great for minor refreshes.</p>
                           </motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('premium', 'Premium Detail Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-[#FF5A00] flex items-center gap-2">Premium Detail Prep <span className="bg-[#FF5A00]/20 text-[#FF5A00] text-[10px] uppercase px-2 py-0.5 tracking-wider font-bold">Recommended</span></p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Elite multi-stage sanding, deep caulking, wood stabilization, premium coats.</p>
                           </motion.button>
                         </motion.div>
                       )}
        
                       {step === 7 && !isTyping && status !== 'sent' && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={springConfig} className="bg-[#11100d] border border-[#FF5A00]/30 p-6 shadow-2xl relative overflow-hidden">
                           <div className="absolute top-0 left-0 right-0 h-1 bg-[#FF5A00]"></div>
                           <form onSubmit={handleFinalSubmit} className="space-y-4">
                             <p className="eyebrow mb-4">Request Walkthrough</p>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="text" placeholder="Name" value={name} onChange={e => setName(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="tel" placeholder="Phone" value={phone} onChange={e => setPhone(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="email" placeholder="Email" value={email} onChange={e => setEmail(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="text" placeholder="City" value={city} onChange={e => setCity(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} disabled={status === 'submitting'} type="submit" className="w-full bg-[#FF5A00] text-white p-4 text-sm font-bold flex items-center justify-center gap-2 hover:bg-[#ff7a33] transition-colors disabled:opacity-50 mt-2 shimmer-cta">
                               {status === 'submitting' ? <Loader2 className="animate-spin" size={16} /> : 'Lock In Estimate'}
                             </motion.button>
                           </form>
                         </motion.div>
                       )}
                     </AnimatePresence>
                  </div>
        
                </div>
                <div className="text-center text-xs text-gray-600 mt-2 pb-4">
                  reg: ir816596 | painting
                </div>
              </section>
            </PageTransition>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:185:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: T1.24 Room Calculator - Selecting cabinet doors/drawers increments high-margin cabinet paint costs
    not ok 24 - T1.24 Room Calculator - Selecting cabinet doors/drawers increments high-margin cabinet paint costs
      ---
      duration_ms: 1.8549
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:195:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /cabinetDoors \+ .*cabinetDrawers.* \* 125/. Input:
        
        "'use client';\n" +
          '\n' +
          "import { useState, FormEvent, useEffect, useRef } from 'react';\n" +
          "import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';\n" +
          "import PageTransition from '../components/PageTransition';\n" +
          "import RangeSlider from '../components/RangeSlider';\n" +
          "import { trackEvent } from '../lib/analytics';\n" +
          "import { buildEstimateMailto } from '../lib/contact';\n" +
          "import { motion, AnimatePresence } from 'motion/react';\n" +
          "import { cn } from '../lib/utils';\n" +
          '\n' +
          "type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;\n" +
          "type PrepLevel = 'standard' | 'premium' | null;\n" +
          '\n' +
          'interface ChatMessage {\n' +
          '  id: string;\n' +
          "  sender: 'bot' | 'user';\n" +
          '  text: React.ReactNode;\n' +
          '  isComponent?: boolean;\n' +
          '}\n' +
          '\n' +
          'const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };\n' +
          '\n' +
          'export default function EstimatePage() {\n' +
          '  const [messages, setMessages] = useState<ChatMessage[]>([]);\n' +
          '  const [isTyping, setIsTyping] = useState(false);\n' +
          '  const scrollRef = useRef<HTMLDivElement>(null);\n' +
          '\n' +
          '  // State\n' +
          '  const [step, setStep] = useState(0);\n' +
          '  const [projectType, setProjectType] = useState<ProjectType>(null);\n' +
          '  \n' +
          '  // Interior State\n' +
          "  const [roomType, setRoomType] = useState('Bedroom');\n" +
          '  const [width, setWidth] = useState(12);\n' +
          '  const [length, setLength] = useState(14);\n' +
          '  const [height, setHeight] = useState(8);\n' +
          '\n' +
          '  // Exterior State\n' +
          "  const [stories, setStories] = useState('1 Story');\n" +
          "  const [siding, setSiding] = useState('Wood / LP SmartSide');\n" +
          '\n' +
          '  // Cabinet State\n' +
          '  const [cabinetCount, setCabinetCount] = useState(20);\n' +
          '\n' +
          '  // Common State\n' +
          '  const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);\n' +
          '\n' +
          '  // Lead State\n' +
          "  const [name, setName] = useState('');\n" +
          "  const [phone, setPhone] = useState('');\n" +
          "  const [email, setEmail] = useState('');\n" +
          "  const [city, setCity] = useState('');\n" +
          "  const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');\n" +
          "  const [submitMessage, setSubmitMessage] = useState('');\n" +
          '\n' +
          '  const scrollToBottom = () => {\n' +
          '    if (scrollRef.current) {\n' +
          '      scrollRef.current.scrollTo({\n' +
          '        top: scrollRef.current.scrollHeight,\n' +
          "        behavior: 'smooth'\n" +
          '      });\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  useEffect(() => {\n' +
          '    scrollToBottom();\n' +
          '  }, [messages, isTyping, step]);\n' +
          '\n' +
          '  const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {\n' +
          '    setIsTyping(true);\n' +
          '    setTimeout(() => {\n' +
          '      setIsTyping(false);\n' +
          "      setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);\n" +
          '    }, delay);\n' +
          '  };\n' +
          '\n' +
          '  const addUserMessage = (text: string) => {\n' +
          "    setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);\n" +
          '  };\n' +
          '\n' +
          '  // Initialize\n' +
          '  useEffect(() => {\n' +
          '    if (step === 0) {\n' +
          `      addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);\n` +
          '      setTimeout(() => setStep(1), 1200);\n' +
          '    }\n' +
          '  }, []);\n' +
          '\n' +
          '  const handleProjectType = (type: ProjectType, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setProjectType(type);\n' +
          '    \n' +
          "    if (type === 'interior') {\n" +
          '      addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");\n' +
          '      setStep(2);\n' +
          "    } else if (type === 'exterior') {\n" +
          '      addBotMessage("Exterior painting. How many stories is the home?");\n' +
          '      setStep(3);\n' +
          "    } else if (type === 'cabinets') {\n" +
          '      addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");\n' +
          '      setStep(4);\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const handleInteriorRoom = (type: string) => {\n' +
          '    addUserMessage(type);\n' +
          '    setRoomType(type);\n' +
          '    addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");\n' +
          '    setStep(2.1);\n' +
          '  };\n' +
          '\n' +
          '  const submitInteriorDimensions = () => {\n' +
          '    addUserMessage(`${width}x${length}x${height} ft`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorStories = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setStories(val);\n' +
          '    addBotMessage("What type of siding do you have?");\n' +
          '    setStep(3.1);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorSiding = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setSiding(val);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const submitCabinets = () => {\n' +
          '    addUserMessage(`${cabinetCount} doors/drawers`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handlePrep = (prep: PrepLevel, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setPrepLevel(prep);\n' +
          '    addBotMessage("Calculating your estimate...", 400);\n' +
          '    setTimeout(() => {\n' +
          '      calculateFinal();\n' +
          '    }, 1500);\n' +
          '    setStep(6);\n' +
          '  };\n' +
          '\n' +
          '  const calculateFinal = () => {\n' +
          '    let low = 0;\n' +
          '    let high = 0;\n' +
          '\n' +
          "    if (projectType === 'interior') {\n" +
          '      const wallArea = 2 * (width + length) * height;\n' +
          '      const base = wallArea * 3.50;\n' +
          "      const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;\n" +
          '      const total = base + prepVal + 250;\n' +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          "    } else if (projectType === 'exterior') {\n" +
          '      let base = 3500;\n' +
          "      if (stories === '2 Story') base = 5500;\n" +
          "      if (stories === '3+ Story') base = 8500;\n" +
          '      \n' +
          '      let mult = 1.0;\n' +
          "      if (siding === 'Stucco') mult = 1.3;\n" +
          "      if (siding === 'Brick / Masonry') mult = 1.4;\n" +
          '\n' +
          "      const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);\n" +
          '      low = total * 0.85;\n' +
          '      high = total * 1.20;\n' +
          "    } else if (projectType === 'cabinets') {\n" +
          "      const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);\n" +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          '    }\n' +
          '\n' +
          '    low = Math.round(low / 100) * 100;\n' +
          '    high = Math.round(high / 100) * 100;\n' +
          '\n' +
          '    addBotMessage(\n' +
          '      <div className="space-y-4 reveal-up">\n' +
          '        <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>\n' +
          '        <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">\n' +
          '          <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>\n' +
          '          <p className="eyebrow mb-2">Estimated Range</p>\n' +
          '          <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>\n' +
          '        </div>\n' +
          '        <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>\n' +
          '      </div>\n' +
          '    , 200, true);\n' +
          '\n' +
          '    setStep(7);\n' +
          '  };\n' +
          '\n' +
          '  const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {\n' +
          '    e.preventDefault();\n' +
          '    const payload = {\n' +
          "      source: 'Chatbot Estimate',\n" +
          "      page: '/estimate',\n" +
          '      name, phone, email, city,\n' +
          '      projectType, prepLevel,\n' +
          '      notes: `Project: ${projectType} \\nPrep: ${prepLevel}`\n' +
          '    };\n' +
          '\n' +
          "    setStatus('submitting');\n" +
          '    try {\n' +
          "      const response = await fetch('/api/leads', {\n" +
          "        method: 'POST',\n" +
          "        headers: { 'Content-Type': 'application/json' },\n" +
          '        body: JSON.stringify(payload),\n' +
          '      });\n' +
          '      if (response.ok) {\n' +
          "        setStatus('sent');\n" +
          '        addUserMessage("Submitted my info.");\n' +
          `        addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);\n` +
          '      } else {\n' +
          "        setStatus('fallback');\n" +
          '      }\n' +
          '    } catch {\n' +
          "      setStatus('fallback');\n" +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const containerVariants = {\n' +
          '    hidden: { opacity: 0 },\n' +
          '    show: {\n' +
          '      opacity: 1,\n' +
          '      transition: { staggerChildren: 0.1 }\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const itemVariants = {\n' +
          '    hidden: { opacity: 0, y: 15, scale: 0.95 },\n' +
          '    show: { opacity: 1, y: 0, scale: 1, transition: springConfig }\n' +
          '  };\n' +
          '\n' +
          '  return (\n' +
          '    <PageTransition>\n' +
          '      <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">\n' +
          '        \n' +
          '        <div className="noise-overlay"></div>\n' +
          '\n' +
          '        <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">\n' +
          '          \n' +
          '          {/* Header */}\n' +
          '          <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">\n' +
          '            <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">\n' +
          '               <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />\n' +
          '            </div>\n' +
          '            <div>\n' +
          '              <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>\n' +
          '              <p className="eyebrow flex items-center gap-2 mt-1">\n' +
          '                <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online\n' +
          '              </p>\n' +
          '            </div>\n' +
          '          </div>\n' +
          '\n' +
          '          {/* Chat Area */}\n' +
          '          <div \n' +
          '            ref={scrollRef}\n' +
          '            className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"\n' +
          '          >\n' +
          '            <AnimatePresence initial={false}>\n' +
          '              {messages.map((msg) => (\n' +
          '                <motion.div\n' +
          '                  key={msg.id}\n' +
          '                  initial={{ opacity: 0, y: 20, scale: 0.95 }}\n' +
          '                  animate={{ opacity: 1, y: 0, scale: 1 }}\n' +
          '                  transition={springConfig}\n' +
          "                  className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}\n" +
          '                >\n' +
          "                  <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>\n" +
          '                    \n' +
          "                    <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>\n" +
          "                      {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}\n" +
          '                    </div>\n' +
          '\n' +
          '                    <div className={cn(\n' +
          '                      "p-5 text-sm l'... 10894 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import { useState, FormEvent, useEffect, useRef } from 'react';
        import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';
        import PageTransition from '../components/PageTransition';
        import RangeSlider from '../components/RangeSlider';
        import { trackEvent } from '../lib/analytics';
        import { buildEstimateMailto } from '../lib/contact';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '../lib/utils';
        
        type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;
        type PrepLevel = 'standard' | 'premium' | null;
        
        interface ChatMessage {
          id: string;
          sender: 'bot' | 'user';
          text: React.ReactNode;
          isComponent?: boolean;
        }
        
        const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };
        
        export default function EstimatePage() {
          const [messages, setMessages] = useState<ChatMessage[]>([]);
          const [isTyping, setIsTyping] = useState(false);
          const scrollRef = useRef<HTMLDivElement>(null);
        
          // State
          const [step, setStep] = useState(0);
          const [projectType, setProjectType] = useState<ProjectType>(null);
          
          // Interior State
          const [roomType, setRoomType] = useState('Bedroom');
          const [width, setWidth] = useState(12);
          const [length, setLength] = useState(14);
          const [height, setHeight] = useState(8);
        
          // Exterior State
          const [stories, setStories] = useState('1 Story');
          const [siding, setSiding] = useState('Wood / LP SmartSide');
        
          // Cabinet State
          const [cabinetCount, setCabinetCount] = useState(20);
        
          // Common State
          const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);
        
          // Lead State
          const [name, setName] = useState('');
          const [phone, setPhone] = useState('');
          const [email, setEmail] = useState('');
          const [city, setCity] = useState('');
          const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');
          const [submitMessage, setSubmitMessage] = useState('');
        
          const scrollToBottom = () => {
            if (scrollRef.current) {
              scrollRef.current.scrollTo({
                top: scrollRef.current.scrollHeight,
                behavior: 'smooth'
              });
            }
          };
        
          useEffect(() => {
            scrollToBottom();
          }, [messages, isTyping, step]);
        
          const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {
            setIsTyping(true);
            setTimeout(() => {
              setIsTyping(false);
              setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);
            }, delay);
          };
        
          const addUserMessage = (text: string) => {
            setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);
          };
        
          // Initialize
          useEffect(() => {
            if (step === 0) {
              addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);
              setTimeout(() => setStep(1), 1200);
            }
          }, []);
        
          const handleProjectType = (type: ProjectType, label: string) => {
            addUserMessage(label);
            setProjectType(type);
            
            if (type === 'interior') {
              addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");
              setStep(2);
            } else if (type === 'exterior') {
              addBotMessage("Exterior painting. How many stories is the home?");
              setStep(3);
            } else if (type === 'cabinets') {
              addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");
              setStep(4);
            }
          };
        
          const handleInteriorRoom = (type: string) => {
            addUserMessage(type);
            setRoomType(type);
            addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");
            setStep(2.1);
          };
        
          const submitInteriorDimensions = () => {
            addUserMessage(`${width}x${length}x${height} ft`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handleExteriorStories = (val: string) => {
            addUserMessage(val);
            setStories(val);
            addBotMessage("What type of siding do you have?");
            setStep(3.1);
          };
        
          const handleExteriorSiding = (val: string) => {
            addUserMessage(val);
            setSiding(val);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const submitCabinets = () => {
            addUserMessage(`${cabinetCount} doors/drawers`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handlePrep = (prep: PrepLevel, label: string) => {
            addUserMessage(label);
            setPrepLevel(prep);
            addBotMessage("Calculating your estimate...", 400);
            setTimeout(() => {
              calculateFinal();
            }, 1500);
            setStep(6);
          };
        
          const calculateFinal = () => {
            let low = 0;
            let high = 0;
        
            if (projectType === 'interior') {
              const wallArea = 2 * (width + length) * height;
              const base = wallArea * 3.50;
              const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;
              const total = base + prepVal + 250;
              low = total * 0.90;
              high = total * 1.15;
            } else if (projectType === 'exterior') {
              let base = 3500;
              if (stories === '2 Story') base = 5500;
              if (stories === '3+ Story') base = 8500;
              
              let mult = 1.0;
              if (siding === 'Stucco') mult = 1.3;
              if (siding === 'Brick / Masonry') mult = 1.4;
        
              const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);
              low = total * 0.85;
              high = total * 1.20;
            } else if (projectType === 'cabinets') {
              const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);
              low = total * 0.90;
              high = total * 1.15;
            }
        
            low = Math.round(low / 100) * 100;
            high = Math.round(high / 100) * 100;
        
            addBotMessage(
              <div className="space-y-4 reveal-up">
                <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>
                <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>
                  <p className="eyebrow mb-2">Estimated Range</p>
                  <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>
                </div>
                <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>
              </div>
            , 200, true);
        
            setStep(7);
          };
        
          const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {
            e.preventDefault();
            const payload = {
              source: 'Chatbot Estimate',
              page: '/estimate',
              name, phone, email, city,
              projectType, prepLevel,
              notes: `Project: ${projectType} \nPrep: ${prepLevel}`
            };
        
            setStatus('submitting');
            try {
              const response = await fetch('/api/leads', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload),
              });
              if (response.ok) {
                setStatus('sent');
                addUserMessage("Submitted my info.");
                addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);
              } else {
                setStatus('fallback');
              }
            } catch {
              setStatus('fallback');
            }
          };
        
          const containerVariants = {
            hidden: { opacity: 0 },
            show: {
              opacity: 1,
              transition: { staggerChildren: 0.1 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, y: 15, scale: 0.95 },
            show: { opacity: 1, y: 0, scale: 1, transition: springConfig }
          };
        
          return (
            <PageTransition>
              <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">
                
                <div className="noise-overlay"></div>
        
                <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">
                  
                  {/* Header */}
                  <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">
                    <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">
                       <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />
                    </div>
                    <div>
                      <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>
                      <p className="eyebrow flex items-center gap-2 mt-1">
                        <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online
                      </p>
                    </div>
                  </div>
        
                  {/* Chat Area */}
                  <div 
                    ref={scrollRef}
                    className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"
                  >
                    <AnimatePresence initial={false}>
                      {messages.map((msg) => (
                        <motion.div
                          key={msg.id}
                          initial={{ opacity: 0, y: 20, scale: 0.95 }}
                          animate={{ opacity: 1, y: 0, scale: 1 }}
                          transition={springConfig}
                          className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}
                        >
                          <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>
                            
                            <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>
                              {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}
                            </div>
        
                            <div className={cn(
                              "p-5 text-sm leading-relaxed",
                              msg.sender === 'user' 
                                ? "bg-[#FF5A00] text-white" 
                                : msg.isComponent 
                                  ? "bg-transparent p-0 w-full"
                                  : "bg-[#11100d] border border-white/10 text-[#c9c1b4]"
                            )}>
                              {msg.text}
                            </div>
                          </div>
                        </motion.div>
                      ))}
        
                      {isTyping && (
                        <motion.div
                          initial={{ opacity: 0, y: 10 }}
                          animate={{ opacity: 1, y: 0 }}
                          exit={{ opacity: 0, scale: 0.95 }}
                          transition={{ duration: 0.2 }}
                          className="flex justify-start"
                        >
                          <div className="flex gap-4">
                            <div className="shrink-0 w-10 h-10 flex items-center justify-center bg-[#182023] text-white border border-white/10">
                              <Bot size={18} strokeWidth={1.5} />
                            </div>
                            <div className="bg-[#11100d] border border-white/10 p-5 flex items-center gap-2 h-[60px]">
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.3s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.15s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce"></div>
                            </div>
                          </div>
                        </motion.div>
                      )}
                    </AnimatePresence>
                  </div>
        
                  {/* Controls Footer */}
                  <div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-[#050505] via-[#050505]/95 to-transparent pt-12 pb-6 px-4 sm:px-8 border-t border-white/5 z-20">
                     <AnimatePresence mode="wait">
                       {step === 1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col sm:flex-row gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('interior', 'Interior Rooms')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Interior Rooms</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('exterior', 'Exterior Painting')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Exterior Painting</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('cabinets', 'Cabinet Refinishing')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Cabinet Refinishing</motion.button>
                         </motion.div>
                       )}
        
                       {step === 2 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Bedroom', 'Living Room', 'Kitchen', 'Bathroom', 'Hallway', 'Other'].map(r => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={r} onClick={() => handleInteriorRoom(r)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">{r}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 2.1 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="w" label="Width" value={width} min={5} max={40} suffix="FT" onChange={setWidth} />
                            <RangeSlider id="l" label="Length" value={length} min={5} max={40} suffix="FT" onChange={setLength} />
                            <RangeSlider id="h" label="Ceiling Height" value={height} min={7} max={20} suffix="FT" onChange={setHeight} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitInteriorDimensions} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Dimensions</motion.button>
                         </motion.div>
                       )}
        
                       {step === 3 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           {['1 Story', '2 Story', '3+ Story'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorStories(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-left">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 3.1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Wood / LP SmartSide', 'Stucco', 'Vinyl / Aluminum', 'Brick / Masonry'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorSiding(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-center flex items-center justify-center h-full">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 4 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="c" label="Total Doors & Drawers" value={cabinetCount} min={5} max={60} onChange={setCabinetCount} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitCabinets} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Count</motion.button>
                         </motion.div>
                       )}
        
                       {step === 5 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('standard', 'Standard Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-white">Standard Prep</p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Light αª╕αªéαªòαªƒ, minor caulk, 1 coat primer & topcoat. Great for minor refreshes.</p>
                           </motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('premium', 'Premium Detail Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-[#FF5A00] flex items-center gap-2">Premium Detail Prep <span className="bg-[#FF5A00]/20 text-[#FF5A00] text-[10px] uppercase px-2 py-0.5 tracking-wider font-bold">Recommended</span></p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Elite multi-stage sanding, deep caulking, wood stabilization, premium coats.</p>
                           </motion.button>
                         </motion.div>
                       )}
        
                       {step === 7 && !isTyping && status !== 'sent' && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={springConfig} className="bg-[#11100d] border border-[#FF5A00]/30 p-6 shadow-2xl relative overflow-hidden">
                           <div className="absolute top-0 left-0 right-0 h-1 bg-[#FF5A00]"></div>
                           <form onSubmit={handleFinalSubmit} className="space-y-4">
                             <p className="eyebrow mb-4">Request Walkthrough</p>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="text" placeholder="Name" value={name} onChange={e => setName(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="tel" placeholder="Phone" value={phone} onChange={e => setPhone(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="email" placeholder="Email" value={email} onChange={e => setEmail(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="text" placeholder="City" value={city} onChange={e => setCity(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} disabled={status === 'submitting'} type="submit" className="w-full bg-[#FF5A00] text-white p-4 text-sm font-bold flex items-center justify-center gap-2 hover:bg-[#ff7a33] transition-colors disabled:opacity-50 mt-2 shimmer-cta">
                               {status === 'submitting' ? <Loader2 className="animate-spin" size={16} /> : 'Lock In Estimate'}
                             </motion.button>
                           </form>
                         </motion.div>
                       )}
                     </AnimatePresence>
                  </div>
        
                </div>
                <div className="text-center text-xs text-gray-600 mt-2 pb-4">
                  reg: ir816596 | painting
                </div>
              </section>
            </PageTransition>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:197:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: T1.25 Room Calculator - Base estimate calculation returns output range between 90% (low) and 115% (high)
    ok 25 - T1.25 Room Calculator - Base estimate calculation returns output range between 90% (low) and 115% (high)
      ---
      duration_ms: 0.3684
      type: 'test'
      ...
    # Subtest: T1.26 Reputation Funnel - Rating 4 stars displays the Google Review redirect prompt
    ok 26 - T1.26 Reputation Funnel - Rating 4 stars displays the Google Review redirect prompt
      ---
      duration_ms: 0.4464
      type: 'test'
      ...
    # Subtest: T1.27 Reputation Funnel - Google Review link opens in a new tab pointing to the GBP review page
    ok 27 - T1.27 Reputation Funnel - Google Review link opens in a new tab pointing to the GBP review page
      ---
      duration_ms: 0.4413
      type: 'test'
      ...
    # Subtest: T1.28 Reputation Funnel - Rating 1, 2, or 3 stars intercepts user and displays private feedback form
    ok 28 - T1.28 Reputation Funnel - Rating 1, 2, or 3 stars intercepts user and displays private feedback form
      ---
      duration_ms: 0.3766
      type: 'test'
      ...
    # Subtest: T1.29 Reputation Funnel - Feedback form submits private reviews to Formspree endpoint
    ok 29 - T1.29 Reputation Funnel - Feedback form submits private reviews to Formspree endpoint
      ---
      duration_ms: 4.0901
      type: 'test'
      ...
    # Subtest: T1.30 Reputation Funnel - Clicking "Change rating" button resets rating state
    ok 30 - T1.30 Reputation Funnel - Clicking "Change rating" button resets rating state
      ---
      duration_ms: 0.8318
      type: 'test'
      ...
    # Subtest: T1.31 Lead Capture & API - Submitting LeadForm sends POST request to /api/leads
    ok 31 - T1.31 Lead Capture & API - Submitting LeadForm sends POST request to /api/leads
      ---
      duration_ms: 2.9171
      type: 'test'
      ...
    # Subtest: T1.32 Lead Capture & API - Local Storage lead queue saves submission payloads when offline
    ok 32 - T1.32 Lead Capture & API - Local Storage lead queue saves submission payloads when offline
      ---
      duration_ms: 0.5898
      type: 'test'
      ...
    # Subtest: T1.33 Lead Capture & API - Regaining online status triggers immediate flushing of lead queue
    ok 33 - T1.33 Lead Capture & API - Regaining online status triggers immediate flushing of lead queue
      ---
      duration_ms: 0.6884
      type: 'test'
      ...
    # Subtest: T1.34 Lead Capture & API - /api/leads validates email structures and throws bad request
    ok 34 - T1.34 Lead Capture & API - /api/leads validates email structures and throws bad request
      ---
      duration_ms: 0.5203
      type: 'test'
      ...
    # Subtest: T1.35 Lead Capture & API - /api/manychat extracts and normalizes payload parameters
    ok 35 - T1.35 Lead Capture & API - /api/manychat extracts and normalizes payload parameters
      ---
      duration_ms: 0.474
      type: 'test'
      ...
    1..35
not ok 49 - Tier 1: Feature Coverage
  ---
  duration_ms: 60.2663
  type: 'suite'
  location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:24:1'
  failureType: 'subtestsFailed'
  error: '7 subtests failed'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: Tier 2: Boundary/Corner Cases
    # Subtest: T2.1 Routing & Navigation - Mobile menu toggle handles accessibility attributes
    not ok 1 - T2.1 Routing & Navigation - Mobile menu toggle handles accessibility attributes
      ---
      duration_ms: 1.6767
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:282:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /aria-label=\{mobileMenuOpen \?/. Input:
        
        "'use client';\r\n" +
          '\r\n' +
          "import React, { useState, useEffect } from 'react';\n" +
          "import Link from 'next/link';\n" +
          "import Image from 'next/image';\n" +
          "import { usePathname } from 'next/navigation';\r\n" +
          "import { Menu, X, Phone, PaintRoller, ChevronDown, Brush, Warehouse } from 'lucide-react';\r\n" +
          'import { \r\n' +
          '  Sheet, \r\n' +
          '  SheetContent, \r\n' +
          '  SheetTrigger, \r\n' +
          '  SheetHeader,\r\n' +
          '  SheetTitle,\r\n' +
          '  SheetDescription\r\n' +
          "} from '@/components/ui/sheet';\r\n" +
          "import { motion, AnimatePresence } from 'motion/react';\r\n" +
          "import { cn } from '@/lib/utils';\r\n" +
          '\r\n' +
          'export default function ConversionHeader() {\r\n' +
          '  const [isScrolled, setIsScrolled] = useState(false);\r\n' +
          '  const [isServicesOpen, setIsServicesOpen] = useState(false);\r\n' +
          '  const pathname = usePathname();\r\n' +
          '\r\n' +
          '  useEffect(() => {\r\n' +
          '    const handleScroll = () => {\r\n' +
          '      setIsScrolled(window.scrollY > 20);\r\n' +
          '    };\r\n' +
          "    window.addEventListener('scroll', handleScroll);\r\n" +
          "    return () => window.removeEventListener('scroll', handleScroll);\r\n" +
          '  }, []);\r\n' +
          '\r\n' +
          '  // Close dropdown on route change\r\n' +
          '  useEffect(() => {\r\n' +
          '    setIsServicesOpen(false);\r\n' +
          '  }, [pathname]);\r\n' +
          '\r\n' +
          '  const navClass = cn(\r\n' +
          '    "fixed top-0 left-0 right-0 z-50 transition-all duration-300",\r\n' +
          '    isScrolled \r\n' +
          '      ? "bg-[#0A0A0A]/95 backdrop-blur-xl border-b border-zinc-800 py-3" \n' +
          '      : "bg-transparent py-5"\r\n' +
          '  );\r\n' +
          '\r\n' +
          '  const containerVariants = {\r\n' +
          '    hidden: { opacity: 0, y: -5 },\r\n' +
          '    show: {\r\n' +
          '      opacity: 1,\r\n' +
          '      y: 0,\r\n' +
          '      transition: {\r\n' +
          '        staggerChildren: 0.05,\r\n' +
          '        duration: 0.2,\r\n' +
          '      }\r\n' +
          '    },\r\n' +
          '    exit: {\r\n' +
          '      opacity: 0,\r\n' +
          '      y: -5,\r\n' +
          '      transition: { duration: 0.15 }\r\n' +
          '    }\r\n' +
          '  };\r\n' +
          '\r\n' +
          '  const itemVariants = {\r\n' +
          '    hidden: { opacity: 0, x: -10 },\r\n' +
          '    show: { opacity: 1, x: 0, transition: { type: "spring", stiffness: 300, damping: 24 } as any }\r\n' +
          '  };\r\n' +
          '\r\n' +
          '  return (\r\n' +
          '    <header className={navClass}>\r\n' +
          '      <div className="container-page mx-auto">\r\n' +
          '        <div className="flex items-center justify-between">\r\n' +
          '          \r\n' +
          '          {/* Logo */}\r\n' +
          '          <Link href="/" className="flex items-center gap-3 group">\r\n' +
          '            <div className="flex h-10 w-10 shrink-0 items-center justify-center border border-zinc-700 bg-[#0A0A0A] u-transition group-hover:border-[#FF5A00]">\n' +
          `              <Image src="/brand/SkyLLP_BrandLogo.svg" alt="Sky's the Limit Painting" width={24} height={24} priority />\n` +
          '            </div>\r\n' +
          '            <div className="hidden sm:block">\r\n' +
          `              <span className="font-display font-bold text-lg leading-none tracking-tight block text-white">SKY'S THE LIMIT</span>\r\n` +
          '              <span className="eyebrow block mt-0.5 text-[#FF5A00]">PAINTING</span>\r\n' +
          '            </div>\r\n' +
          '          </Link>\r\n' +
          '\r\n' +
          '          {/* Desktop Nav */}\r\n' +
          '          <nav className="hidden lg:flex items-center gap-8">\r\n' +
          '            <div className="relative" onMouseLeave={() => setIsServicesOpen(false)}>\r\n' +
          '              <button \r\n' +
          '                className={cn(\r\n' +
          '                  "flex items-center gap-1 text-sm font-bold tracking-wider uppercase transition-colors py-2",\r\n' +
          `                  isServicesOpen || pathname.includes('/painting-services') ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]"\r\n` +
          '                )}\r\n' +
          '                onMouseEnter={() => setIsServicesOpen(true)}\r\n' +
          '                onClick={() => setIsServicesOpen(!isServicesOpen)}\r\n' +
          '              >\r\n' +
          '                Services <ChevronDown size={14} className={cn("transition-transform duration-200", isServicesOpen && "rotate-180")} />\r\n' +
          '              </button>\r\n' +
          '\r\n' +
          '              <AnimatePresence>\r\n' +
          '                {isServicesOpen && (\r\n' +
          '                  <motion.div \r\n' +
          '                    variants={containerVariants}\r\n' +
          '                    initial="hidden"\r\n' +
          '                    animate="show"\r\n' +
          '                    exit="exit"\r\n' +
          '                    className="absolute top-full left-1/2 -translate-x-1/2 mt-2 w-[480px] bg-[#0B0B0D] border border-white/10 shadow-2xl p-4 grid grid-cols-2 gap-2"\r\n' +
          '                  >\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/interior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Interior Painting</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Walls, trim, ceilings, & prep.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/exterior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><Brush size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Exterior Painting</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Siding, brick, stucco, & decks.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/cabinet-refinishing" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Cabinet Refinishing</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Factory-finish transformations.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/commercial" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><Warehouse size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Commercial</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">HOA, property mgmt, retail.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '                  </motion.div>\r\n' +
          '                )}\r\n' +
          '              </AnimatePresence>\r\n' +
          '            </div>\r\n' +
          '\r\n' +
          '            <Link href="/projects" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/projects" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              Projects\r\n' +
          '            </Link>\r\n' +
          '            <Link href="/service-area" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/service-area" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              Service Area\r\n' +
          '            </Link>\r\n' +
          '            <Link href="/about" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/about" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              About Us\r\n' +
          '            </Link>\r\n' +
          '          </nav>\r\n' +
          '\r\n' +
          '          {/* CTA & Mobile Toggle */}\r\n' +
          '          <div className="flex items-center gap-4">\r\n' +
          '            <a href="tel:+16514104196" className="hidden lg:flex items-center gap-2 text-sm font-bold text-white hover:text-[#FF5A00] transition-colors">\n' +
          '              <Phone size={16} />\r\n' +
          '              (651) 410-4196\n' +
          '            </a>\r\n' +
          '            \r\n' +
          '            <Link \r\n' +
          '              href="/estimate" \r\n' +
          '              className="hidden bg-[#FF5A00] px-6 py-2.5 text-sm font-bold uppercase tracking-wider text-white transition-colors hover:bg-[#d94d00] md:inline-flex"\n' +
          '            >\r\n' +
          '              Get Estimate\r\n' +
          '            </Link>\r\n' +
          '\r\n' +
          '            {/* Mobile Menu */}\r\n' +
          '            <Sheet>\r\n' +
          '              <SheetTrigger>\r\n' +
          '                <button className="lg:hidden text-white p-2" aria-label="Open Menu">\r\n' +
          '                  <Menu size={24} />\r\n' +
          '                </button>\r\n' +
          '              </SheetTrigger>\r\n' +
          '              <SheetContent side="right" className="w-full sm:w-[400px] bg-[#050505] border-l border-white/10 p-0">\r\n' +
          '                <SheetHeader className="p-6 border-b border-white/10 text-left">\r\n' +
          '                  <SheetTitle className="text-white font-display text-xl uppercase">Menu</SheetTitle>\r\n' +
          `                  <SheetDescription className="sr-only">Navigate around the Sky's the Limit Painting website.</SheetDescription>\r\n` +
          '                </SheetHeader>\r\n' +
          '                \r\n' +
          '                <div className="p-6 overflow-y-auto h-[calc(100vh-80px)]">\r\n' +
          '                  <nav className="flex flex-col gap-6 text-xl">\r\n' +
          '                    <Link href="/">Home</Link>\r\n' +
          '                    <div className="pt-4 pb-2">\r\n' +
          '                      <p className="eyebrow mb-4">Painting Services</p>\r\n' +
          '                      <div className="flex flex-col gap-4 pl-4 border-l border-white/10">\r\n' +
          '                        <Link href="/painting-services/interior-painting" className="text-lg">Interior Painting</Link>\r\n' +
          '                        <Link href="/painting-services/exterior-painting" className="text-lg">Exterior Painting</Link>\r\n' +
          '                        <Link href="/painting-services/cabinet-refinishing" className="text-lg">Cabinet Refinishing</Link>\r\n' +
          '                        <Link href="/commercial" className="text-lg">Commercial & HOA</Link>\r\n' +
          '                      </div>\r\n' +
          '                    </div>\r\n' +
          '                    <Link href="/projects">Projects</Link>\r\n' +
          '                    <Link href="/service-area">Service Area</Link>\r\n' +
          '                    <Link href="/about">About Us</Link>\r\n' +
          '                    <Link href="/contact">Contact</Link>\r\n' +
          '   '... 755 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import React, { useState, useEffect } from 'react';
        import Link from 'next/link';
        import Image from 'next/image';
        import { usePathname } from 'next/navigation';
        import { Menu, X, Phone, PaintRoller, ChevronDown, Brush, Warehouse } from 'lucide-react';
        import { 
          Sheet, 
          SheetContent, 
          SheetTrigger, 
          SheetHeader,
          SheetTitle,
          SheetDescription
        } from '@/components/ui/sheet';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '@/lib/utils';
        
        export default function ConversionHeader() {
          const [isScrolled, setIsScrolled] = useState(false);
          const [isServicesOpen, setIsServicesOpen] = useState(false);
          const pathname = usePathname();
        
          useEffect(() => {
            const handleScroll = () => {
              setIsScrolled(window.scrollY > 20);
            };
            window.addEventListener('scroll', handleScroll);
            return () => window.removeEventListener('scroll', handleScroll);
          }, []);
        
          // Close dropdown on route change
          useEffect(() => {
            setIsServicesOpen(false);
          }, [pathname]);
        
          const navClass = cn(
            "fixed top-0 left-0 right-0 z-50 transition-all duration-300",
            isScrolled 
              ? "bg-[#0A0A0A]/95 backdrop-blur-xl border-b border-zinc-800 py-3" 
              : "bg-transparent py-5"
          );
        
          const containerVariants = {
            hidden: { opacity: 0, y: -5 },
            show: {
              opacity: 1,
              y: 0,
              transition: {
                staggerChildren: 0.05,
                duration: 0.2,
              }
            },
            exit: {
              opacity: 0,
              y: -5,
              transition: { duration: 0.15 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, x: -10 },
            show: { opacity: 1, x: 0, transition: { type: "spring", stiffness: 300, damping: 24 } as any }
          };
        
          return (
            <header className={navClass}>
              <div className="container-page mx-auto">
                <div className="flex items-center justify-between">
                  
                  {/* Logo */}
                  <Link href="/" className="flex items-center gap-3 group">
                    <div className="flex h-10 w-10 shrink-0 items-center justify-center border border-zinc-700 bg-[#0A0A0A] u-transition group-hover:border-[#FF5A00]">
                      <Image src="/brand/SkyLLP_BrandLogo.svg" alt="Sky's the Limit Painting" width={24} height={24} priority />
                    </div>
                    <div className="hidden sm:block">
                      <span className="font-display font-bold text-lg leading-none tracking-tight block text-white">SKY'S THE LIMIT</span>
                      <span className="eyebrow block mt-0.5 text-[#FF5A00]">PAINTING</span>
                    </div>
                  </Link>
        
                  {/* Desktop Nav */}
                  <nav className="hidden lg:flex items-center gap-8">
                    <div className="relative" onMouseLeave={() => setIsServicesOpen(false)}>
                      <button 
                        className={cn(
                          "flex items-center gap-1 text-sm font-bold tracking-wider uppercase transition-colors py-2",
                          isServicesOpen || pathname.includes('/painting-services') ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]"
                        )}
                        onMouseEnter={() => setIsServicesOpen(true)}
                        onClick={() => setIsServicesOpen(!isServicesOpen)}
                      >
                        Services <ChevronDown size={14} className={cn("transition-transform duration-200", isServicesOpen && "rotate-180")} />
                      </button>
        
                      <AnimatePresence>
                        {isServicesOpen && (
                          <motion.div 
                            variants={containerVariants}
                            initial="hidden"
                            animate="show"
                            exit="exit"
                            className="absolute top-full left-1/2 -translate-x-1/2 mt-2 w-[480px] bg-[#0B0B0D] border border-white/10 shadow-2xl p-4 grid grid-cols-2 gap-2"
                          >
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/interior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Interior Painting</div>
                                  <div className="text-xs text-gray-400 mt-1">Walls, trim, ceilings, & prep.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/exterior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><Brush size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Exterior Painting</div>
                                  <div className="text-xs text-gray-400 mt-1">Siding, brick, stucco, & decks.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/cabinet-refinishing" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Cabinet Refinishing</div>
                                  <div className="text-xs text-gray-400 mt-1">Factory-finish transformations.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/commercial" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><Warehouse size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Commercial</div>
                                  <div className="text-xs text-gray-400 mt-1">HOA, property mgmt, retail.</div>
                                </div>
                              </Link>
                            </motion.div>
                          </motion.div>
                        )}
                      </AnimatePresence>
                    </div>
        
                    <Link href="/projects" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/projects" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      Projects
                    </Link>
                    <Link href="/service-area" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/service-area" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      Service Area
                    </Link>
                    <Link href="/about" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/about" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      About Us
                    </Link>
                  </nav>
        
                  {/* CTA & Mobile Toggle */}
                  <div className="flex items-center gap-4">
                    <a href="tel:+16514104196" className="hidden lg:flex items-center gap-2 text-sm font-bold text-white hover:text-[#FF5A00] transition-colors">
                      <Phone size={16} />
                      (651) 410-4196
                    </a>
                    
                    <Link 
                      href="/estimate" 
                      className="hidden bg-[#FF5A00] px-6 py-2.5 text-sm font-bold uppercase tracking-wider text-white transition-colors hover:bg-[#d94d00] md:inline-flex"
                    >
                      Get Estimate
                    </Link>
        
                    {/* Mobile Menu */}
                    <Sheet>
                      <SheetTrigger>
                        <button className="lg:hidden text-white p-2" aria-label="Open Menu">
                          <Menu size={24} />
                        </button>
                      </SheetTrigger>
                      <SheetContent side="right" className="w-full sm:w-[400px] bg-[#050505] border-l border-white/10 p-0">
                        <SheetHeader className="p-6 border-b border-white/10 text-left">
                          <SheetTitle className="text-white font-display text-xl uppercase">Menu</SheetTitle>
                          <SheetDescription className="sr-only">Navigate around the Sky's the Limit Painting website.</SheetDescription>
                        </SheetHeader>
                        
                        <div className="p-6 overflow-y-auto h-[calc(100vh-80px)]">
                          <nav className="flex flex-col gap-6 text-xl">
                            <Link href="/">Home</Link>
                            <div className="pt-4 pb-2">
                              <p className="eyebrow mb-4">Painting Services</p>
                              <div className="flex flex-col gap-4 pl-4 border-l border-white/10">
                                <Link href="/painting-services/interior-painting" className="text-lg">Interior Painting</Link>
                                <Link href="/painting-services/exterior-painting" className="text-lg">Exterior Painting</Link>
                                <Link href="/painting-services/cabinet-refinishing" className="text-lg">Cabinet Refinishing</Link>
                                <Link href="/commercial" className="text-lg">Commercial & HOA</Link>
                              </div>
                            </div>
                            <Link href="/projects">Projects</Link>
                            <Link href="/service-area">Service Area</Link>
                            <Link href="/about">About Us</Link>
                            <Link href="/contact">Contact</Link>
                          </nav>
                          
                          <div className="mt-12 flex flex-col gap-4">
                            <Link href="/estimate" className="bg-[#FF5A00] text-white text-center py-4 font-bold tracking-wider uppercase">
                              Get Your Free Estimate
                            </Link>
                            <a href="tel:+16514104196" className="border border-white/10 text-white text-center py-4 font-bold flex items-center justify-center gap-2">
                              <Phone size={18} />
                              (651) 410-4196
                            </a>
                          </div>
                        </div>
                      </SheetContent>
                    </Sheet>
                  </div>
                </div>
              </div>
            </header>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:284:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Test.start (node:internal/test_runner/test:944:17)
        node:internal/test_runner/test:1440:71
        node:internal/per_context/primordials:466:82
        new Promise (<anonymous>)
        new SafePromise (node:internal/per_context/primordials:435:3)
        node:internal/per_context/primordials:466:9
        Array.map (<anonymous>)
      ...
    # Subtest: T2.2 Routing & Navigation - Sticky mobile call CTAs render on narrow layouts
    ok 2 - T2.2 Routing & Navigation - Sticky mobile call CTAs render on narrow layouts
      ---
      duration_ms: 0.5739
      type: 'test'
      ...
    # Subtest: T2.3 Routing & Navigation - CSP and HTTP security headers are configured in vercel.ts
    ok 3 - T2.3 Routing & Navigation - CSP and HTTP security headers are configured in vercel.ts
      ---
      duration_ms: 0.4106
      type: 'test'
      ...
    # Subtest: T2.4 Routing & Navigation - Referral parameters are parsed and stored in LocalStorage
    not ok 4 - T2.4 Routing & Navigation - Referral parameters are parsed and stored in LocalStorage
      ---
      duration_ms: 1.2002
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:303:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /localStorage\.setItem\('referrer_email'/. Input:
        
        "'use client';\r\n" +
          '\r\n' +
          "import React, { useState, useEffect } from 'react';\n" +
          "import Link from 'next/link';\n" +
          "import Image from 'next/image';\n" +
          "import { usePathname } from 'next/navigation';\r\n" +
          "import { Menu, X, Phone, PaintRoller, ChevronDown, Brush, Warehouse } from 'lucide-react';\r\n" +
          'import { \r\n' +
          '  Sheet, \r\n' +
          '  SheetContent, \r\n' +
          '  SheetTrigger, \r\n' +
          '  SheetHeader,\r\n' +
          '  SheetTitle,\r\n' +
          '  SheetDescription\r\n' +
          "} from '@/components/ui/sheet';\r\n" +
          "import { motion, AnimatePresence } from 'motion/react';\r\n" +
          "import { cn } from '@/lib/utils';\r\n" +
          '\r\n' +
          'export default function ConversionHeader() {\r\n' +
          '  const [isScrolled, setIsScrolled] = useState(false);\r\n' +
          '  const [isServicesOpen, setIsServicesOpen] = useState(false);\r\n' +
          '  const pathname = usePathname();\r\n' +
          '\r\n' +
          '  useEffect(() => {\r\n' +
          '    const handleScroll = () => {\r\n' +
          '      setIsScrolled(window.scrollY > 20);\r\n' +
          '    };\r\n' +
          "    window.addEventListener('scroll', handleScroll);\r\n" +
          "    return () => window.removeEventListener('scroll', handleScroll);\r\n" +
          '  }, []);\r\n' +
          '\r\n' +
          '  // Close dropdown on route change\r\n' +
          '  useEffect(() => {\r\n' +
          '    setIsServicesOpen(false);\r\n' +
          '  }, [pathname]);\r\n' +
          '\r\n' +
          '  const navClass = cn(\r\n' +
          '    "fixed top-0 left-0 right-0 z-50 transition-all duration-300",\r\n' +
          '    isScrolled \r\n' +
          '      ? "bg-[#0A0A0A]/95 backdrop-blur-xl border-b border-zinc-800 py-3" \n' +
          '      : "bg-transparent py-5"\r\n' +
          '  );\r\n' +
          '\r\n' +
          '  const containerVariants = {\r\n' +
          '    hidden: { opacity: 0, y: -5 },\r\n' +
          '    show: {\r\n' +
          '      opacity: 1,\r\n' +
          '      y: 0,\r\n' +
          '      transition: {\r\n' +
          '        staggerChildren: 0.05,\r\n' +
          '        duration: 0.2,\r\n' +
          '      }\r\n' +
          '    },\r\n' +
          '    exit: {\r\n' +
          '      opacity: 0,\r\n' +
          '      y: -5,\r\n' +
          '      transition: { duration: 0.15 }\r\n' +
          '    }\r\n' +
          '  };\r\n' +
          '\r\n' +
          '  const itemVariants = {\r\n' +
          '    hidden: { opacity: 0, x: -10 },\r\n' +
          '    show: { opacity: 1, x: 0, transition: { type: "spring", stiffness: 300, damping: 24 } as any }\r\n' +
          '  };\r\n' +
          '\r\n' +
          '  return (\r\n' +
          '    <header className={navClass}>\r\n' +
          '      <div className="container-page mx-auto">\r\n' +
          '        <div className="flex items-center justify-between">\r\n' +
          '          \r\n' +
          '          {/* Logo */}\r\n' +
          '          <Link href="/" className="flex items-center gap-3 group">\r\n' +
          '            <div className="flex h-10 w-10 shrink-0 items-center justify-center border border-zinc-700 bg-[#0A0A0A] u-transition group-hover:border-[#FF5A00]">\n' +
          `              <Image src="/brand/SkyLLP_BrandLogo.svg" alt="Sky's the Limit Painting" width={24} height={24} priority />\n` +
          '            </div>\r\n' +
          '            <div className="hidden sm:block">\r\n' +
          `              <span className="font-display font-bold text-lg leading-none tracking-tight block text-white">SKY'S THE LIMIT</span>\r\n` +
          '              <span className="eyebrow block mt-0.5 text-[#FF5A00]">PAINTING</span>\r\n' +
          '            </div>\r\n' +
          '          </Link>\r\n' +
          '\r\n' +
          '          {/* Desktop Nav */}\r\n' +
          '          <nav className="hidden lg:flex items-center gap-8">\r\n' +
          '            <div className="relative" onMouseLeave={() => setIsServicesOpen(false)}>\r\n' +
          '              <button \r\n' +
          '                className={cn(\r\n' +
          '                  "flex items-center gap-1 text-sm font-bold tracking-wider uppercase transition-colors py-2",\r\n' +
          `                  isServicesOpen || pathname.includes('/painting-services') ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]"\r\n` +
          '                )}\r\n' +
          '                onMouseEnter={() => setIsServicesOpen(true)}\r\n' +
          '                onClick={() => setIsServicesOpen(!isServicesOpen)}\r\n' +
          '              >\r\n' +
          '                Services <ChevronDown size={14} className={cn("transition-transform duration-200", isServicesOpen && "rotate-180")} />\r\n' +
          '              </button>\r\n' +
          '\r\n' +
          '              <AnimatePresence>\r\n' +
          '                {isServicesOpen && (\r\n' +
          '                  <motion.div \r\n' +
          '                    variants={containerVariants}\r\n' +
          '                    initial="hidden"\r\n' +
          '                    animate="show"\r\n' +
          '                    exit="exit"\r\n' +
          '                    className="absolute top-full left-1/2 -translate-x-1/2 mt-2 w-[480px] bg-[#0B0B0D] border border-white/10 shadow-2xl p-4 grid grid-cols-2 gap-2"\r\n' +
          '                  >\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/interior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Interior Painting</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Walls, trim, ceilings, & prep.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/exterior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><Brush size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Exterior Painting</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Siding, brick, stucco, & decks.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/cabinet-refinishing" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Cabinet Refinishing</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Factory-finish transformations.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/commercial" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><Warehouse size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Commercial</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">HOA, property mgmt, retail.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '                  </motion.div>\r\n' +
          '                )}\r\n' +
          '              </AnimatePresence>\r\n' +
          '            </div>\r\n' +
          '\r\n' +
          '            <Link href="/projects" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/projects" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              Projects\r\n' +
          '            </Link>\r\n' +
          '            <Link href="/service-area" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/service-area" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              Service Area\r\n' +
          '            </Link>\r\n' +
          '            <Link href="/about" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/about" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              About Us\r\n' +
          '            </Link>\r\n' +
          '          </nav>\r\n' +
          '\r\n' +
          '          {/* CTA & Mobile Toggle */}\r\n' +
          '          <div className="flex items-center gap-4">\r\n' +
          '            <a href="tel:+16514104196" className="hidden lg:flex items-center gap-2 text-sm font-bold text-white hover:text-[#FF5A00] transition-colors">\n' +
          '              <Phone size={16} />\r\n' +
          '              (651) 410-4196\n' +
          '            </a>\r\n' +
          '            \r\n' +
          '            <Link \r\n' +
          '              href="/estimate" \r\n' +
          '              className="hidden bg-[#FF5A00] px-6 py-2.5 text-sm font-bold uppercase tracking-wider text-white transition-colors hover:bg-[#d94d00] md:inline-flex"\n' +
          '            >\r\n' +
          '              Get Estimate\r\n' +
          '            </Link>\r\n' +
          '\r\n' +
          '            {/* Mobile Menu */}\r\n' +
          '            <Sheet>\r\n' +
          '              <SheetTrigger>\r\n' +
          '                <button className="lg:hidden text-white p-2" aria-label="Open Menu">\r\n' +
          '                  <Menu size={24} />\r\n' +
          '                </button>\r\n' +
          '              </SheetTrigger>\r\n' +
          '              <SheetContent side="right" className="w-full sm:w-[400px] bg-[#050505] border-l border-white/10 p-0">\r\n' +
          '                <SheetHeader className="p-6 border-b border-white/10 text-left">\r\n' +
          '                  <SheetTitle className="text-white font-display text-xl uppercase">Menu</SheetTitle>\r\n' +
          `                  <SheetDescription className="sr-only">Navigate around the Sky's the Limit Painting website.</SheetDescription>\r\n` +
          '                </SheetHeader>\r\n' +
          '                \r\n' +
          '                <div className="p-6 overflow-y-auto h-[calc(100vh-80px)]">\r\n' +
          '                  <nav className="flex flex-col gap-6 text-xl">\r\n' +
          '                    <Link href="/">Home</Link>\r\n' +
          '                    <div className="pt-4 pb-2">\r\n' +
          '                      <p className="eyebrow mb-4">Painting Services</p>\r\n' +
          '                      <div className="flex flex-col gap-4 pl-4 border-l border-white/10">\r\n' +
          '                        <Link href="/painting-services/interior-painting" className="text-lg">Interior Painting</Link>\r\n' +
          '                        <Link href="/painting-services/exterior-painting" className="text-lg">Exterior Painting</Link>\r\n' +
          '                        <Link href="/painting-services/cabinet-refinishing" className="text-lg">Cabinet Refinishing</Link>\r\n' +
          '                        <Link href="/commercial" className="text-lg">Commercial & HOA</Link>\r\n' +
          '                      </div>\r\n' +
          '                    </div>\r\n' +
          '                    <Link href="/projects">Projects</Link>\r\n' +
          '                    <Link href="/service-area">Service Area</Link>\r\n' +
          '                    <Link href="/about">About Us</Link>\r\n' +
          '                    <Link href="/contact">Contact</Link>\r\n' +
          '   '... 755 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import React, { useState, useEffect } from 'react';
        import Link from 'next/link';
        import Image from 'next/image';
        import { usePathname } from 'next/navigation';
        import { Menu, X, Phone, PaintRoller, ChevronDown, Brush, Warehouse } from 'lucide-react';
        import { 
          Sheet, 
          SheetContent, 
          SheetTrigger, 
          SheetHeader,
          SheetTitle,
          SheetDescription
        } from '@/components/ui/sheet';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '@/lib/utils';
        
        export default function ConversionHeader() {
          const [isScrolled, setIsScrolled] = useState(false);
          const [isServicesOpen, setIsServicesOpen] = useState(false);
          const pathname = usePathname();
        
          useEffect(() => {
            const handleScroll = () => {
              setIsScrolled(window.scrollY > 20);
            };
            window.addEventListener('scroll', handleScroll);
            return () => window.removeEventListener('scroll', handleScroll);
          }, []);
        
          // Close dropdown on route change
          useEffect(() => {
            setIsServicesOpen(false);
          }, [pathname]);
        
          const navClass = cn(
            "fixed top-0 left-0 right-0 z-50 transition-all duration-300",
            isScrolled 
              ? "bg-[#0A0A0A]/95 backdrop-blur-xl border-b border-zinc-800 py-3" 
              : "bg-transparent py-5"
          );
        
          const containerVariants = {
            hidden: { opacity: 0, y: -5 },
            show: {
              opacity: 1,
              y: 0,
              transition: {
                staggerChildren: 0.05,
                duration: 0.2,
              }
            },
            exit: {
              opacity: 0,
              y: -5,
              transition: { duration: 0.15 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, x: -10 },
            show: { opacity: 1, x: 0, transition: { type: "spring", stiffness: 300, damping: 24 } as any }
          };
        
          return (
            <header className={navClass}>
              <div className="container-page mx-auto">
                <div className="flex items-center justify-between">
                  
                  {/* Logo */}
                  <Link href="/" className="flex items-center gap-3 group">
                    <div className="flex h-10 w-10 shrink-0 items-center justify-center border border-zinc-700 bg-[#0A0A0A] u-transition group-hover:border-[#FF5A00]">
                      <Image src="/brand/SkyLLP_BrandLogo.svg" alt="Sky's the Limit Painting" width={24} height={24} priority />
                    </div>
                    <div className="hidden sm:block">
                      <span className="font-display font-bold text-lg leading-none tracking-tight block text-white">SKY'S THE LIMIT</span>
                      <span className="eyebrow block mt-0.5 text-[#FF5A00]">PAINTING</span>
                    </div>
                  </Link>
        
                  {/* Desktop Nav */}
                  <nav className="hidden lg:flex items-center gap-8">
                    <div className="relative" onMouseLeave={() => setIsServicesOpen(false)}>
                      <button 
                        className={cn(
                          "flex items-center gap-1 text-sm font-bold tracking-wider uppercase transition-colors py-2",
                          isServicesOpen || pathname.includes('/painting-services') ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]"
                        )}
                        onMouseEnter={() => setIsServicesOpen(true)}
                        onClick={() => setIsServicesOpen(!isServicesOpen)}
                      >
                        Services <ChevronDown size={14} className={cn("transition-transform duration-200", isServicesOpen && "rotate-180")} />
                      </button>
        
                      <AnimatePresence>
                        {isServicesOpen && (
                          <motion.div 
                            variants={containerVariants}
                            initial="hidden"
                            animate="show"
                            exit="exit"
                            className="absolute top-full left-1/2 -translate-x-1/2 mt-2 w-[480px] bg-[#0B0B0D] border border-white/10 shadow-2xl p-4 grid grid-cols-2 gap-2"
                          >
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/interior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Interior Painting</div>
                                  <div className="text-xs text-gray-400 mt-1">Walls, trim, ceilings, & prep.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/exterior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><Brush size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Exterior Painting</div>
                                  <div className="text-xs text-gray-400 mt-1">Siding, brick, stucco, & decks.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/cabinet-refinishing" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Cabinet Refinishing</div>
                                  <div className="text-xs text-gray-400 mt-1">Factory-finish transformations.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/commercial" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><Warehouse size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Commercial</div>
                                  <div className="text-xs text-gray-400 mt-1">HOA, property mgmt, retail.</div>
                                </div>
                              </Link>
                            </motion.div>
                          </motion.div>
                        )}
                      </AnimatePresence>
                    </div>
        
                    <Link href="/projects" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/projects" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      Projects
                    </Link>
                    <Link href="/service-area" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/service-area" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      Service Area
                    </Link>
                    <Link href="/about" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/about" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      About Us
                    </Link>
                  </nav>
        
                  {/* CTA & Mobile Toggle */}
                  <div className="flex items-center gap-4">
                    <a href="tel:+16514104196" className="hidden lg:flex items-center gap-2 text-sm font-bold text-white hover:text-[#FF5A00] transition-colors">
                      <Phone size={16} />
                      (651) 410-4196
                    </a>
                    
                    <Link 
                      href="/estimate" 
                      className="hidden bg-[#FF5A00] px-6 py-2.5 text-sm font-bold uppercase tracking-wider text-white transition-colors hover:bg-[#d94d00] md:inline-flex"
                    >
                      Get Estimate
                    </Link>
        
                    {/* Mobile Menu */}
                    <Sheet>
                      <SheetTrigger>
                        <button className="lg:hidden text-white p-2" aria-label="Open Menu">
                          <Menu size={24} />
                        </button>
                      </SheetTrigger>
                      <SheetContent side="right" className="w-full sm:w-[400px] bg-[#050505] border-l border-white/10 p-0">
                        <SheetHeader className="p-6 border-b border-white/10 text-left">
                          <SheetTitle className="text-white font-display text-xl uppercase">Menu</SheetTitle>
                          <SheetDescription className="sr-only">Navigate around the Sky's the Limit Painting website.</SheetDescription>
                        </SheetHeader>
                        
                        <div className="p-6 overflow-y-auto h-[calc(100vh-80px)]">
                          <nav className="flex flex-col gap-6 text-xl">
                            <Link href="/">Home</Link>
                            <div className="pt-4 pb-2">
                              <p className="eyebrow mb-4">Painting Services</p>
                              <div className="flex flex-col gap-4 pl-4 border-l border-white/10">
                                <Link href="/painting-services/interior-painting" className="text-lg">Interior Painting</Link>
                                <Link href="/painting-services/exterior-painting" className="text-lg">Exterior Painting</Link>
                                <Link href="/painting-services/cabinet-refinishing" className="text-lg">Cabinet Refinishing</Link>
                                <Link href="/commercial" className="text-lg">Commercial & HOA</Link>
                              </div>
                            </div>
                            <Link href="/projects">Projects</Link>
                            <Link href="/service-area">Service Area</Link>
                            <Link href="/about">About Us</Link>
                            <Link href="/contact">Contact</Link>
                          </nav>
                          
                          <div className="mt-12 flex flex-col gap-4">
                            <Link href="/estimate" className="bg-[#FF5A00] text-white text-center py-4 font-bold tracking-wider uppercase">
                              Get Your Free Estimate
                            </Link>
                            <a href="tel:+16514104196" className="border border-white/10 text-white text-center py-4 font-bold flex items-center justify-center gap-2">
                              <Phone size={18} />
                              (651) 410-4196
                            </a>
                          </div>
                        </div>
                      </SheetContent>
                    </Sheet>
                  </div>
                </div>
              </div>
            </header>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:306:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: T2.5 Routing & Navigation - E.164 phone numbers are format-compliant
    ok 5 - T2.5 Routing & Navigation - E.164 phone numbers are format-compliant
      ---
      duration_ms: 0.6098
      type: 'test'
      ...
    # Subtest: T2.6 Three-Market Content - Emojis are 100% absent in code/components/markup
    ok 6 - T2.6 Three-Market Content - Emojis are 100% absent in code/components/markup
      ---
      duration_ms: 6.7613
      type: 'test'
      ...
    # Subtest: T2.7 Three-Market Content - Licensed or Bonded claims are absent on Home page
    ok 7 - T2.7 Three-Market Content - Licensed or Bonded claims are absent on Home page
      ---
      duration_ms: 0.7447
      type: 'test'
      ...
    # Subtest: T2.8 Three-Market Content - Contractor registration ID is present on all pages
    ok 8 - T2.8 Three-Market Content - Contractor registration ID is present on all pages
      ---
      duration_ms: 0.4548
      type: 'test'
      ...
    # Subtest: T2.9 Three-Market Content - Workers comp exemption statement is present near insurance references
    ok 9 - T2.9 Three-Market Content - Workers comp exemption statement is present near insurance references
      ---
      duration_ms: 0.4043
      type: 'test'
      ...
    # Subtest: T2.10 Three-Market Content - SAM.gov active claims are absent on the Capabilities page
    ok 10 - T2.10 Three-Market Content - SAM.gov active claims are absent on the Capabilities page
      ---
      duration_ms: 0.4943
      type: 'test'
      ...
    # Subtest: T2.11 Local SEO Pages - Invalid SEO slugs return 404
    ok 11 - T2.11 Local SEO Pages - Invalid SEO slugs return 404
      ---
      duration_ms: 0.4585
      type: 'test'
      ...
    # Subtest: T2.12 Local SEO Pages - Schema breadcrumbs handle nesting without double-slash errors
    ok 12 - T2.12 Local SEO Pages - Schema breadcrumbs handle nesting without double-slash errors
      ---
      duration_ms: 0.3983
      type: 'test'
      ...
    # Subtest: T2.13 Local SEO Pages - robots.txt declared sitemap and protects review page
    ok 13 - T2.13 Local SEO Pages - robots.txt declared sitemap and protects review page
      ---
      duration_ms: 0.3668
      type: 'test'
      ...
    # Subtest: T2.14 Local SEO Pages - App Router generateMetadata covers dynamic SEO slugs
    ok 14 - T2.14 Local SEO Pages - App Router generateMetadata covers dynamic SEO slugs
      ---
      duration_ms: 0.5028
      type: 'test'
      ...
    # Subtest: T2.15 Local SEO Pages - LocalBusiness schema includes required pricing and hours
    ok 15 - T2.15 Local SEO Pages - LocalBusiness schema includes required pricing and hours
      ---
      duration_ms: 0.3781
      type: 'test'
      ...
    # Subtest: T2.16 Interactive Components - BeforeAfterSlider keyboard controls support left/right arrows
    ok 16 - T2.16 Interactive Components - BeforeAfterSlider keyboard controls support left/right arrows
      ---
      duration_ms: 0.3404
      type: 'test'
      ...
    # Subtest: T2.17 Interactive Components - Slider position clamps exactly to 0 and 100 on Home/End key presses
    ok 17 - T2.17 Interactive Components - Slider position clamps exactly to 0 and 100 on Home/End key presses
      ---
      duration_ms: 0.3438
      type: 'test'
      ...
    # Subtest: T2.18 Interactive Components - ServiceAreaMap honors reduced motion requests
    ok 18 - T2.18 Interactive Components - ServiceAreaMap honors reduced motion requests
      ---
      duration_ms: 0.334
      type: 'test'
      ...
    # Subtest: T2.19 Interactive Components - root layout has no next/dynamic ssr:false theater
    ok 19 - T2.19 Interactive Components - root layout has no next/dynamic ssr:false theater
      ---
      duration_ms: 0.4864
      type: 'test'
      ...
    # Subtest: T2.20 Interactive Components - ServiceAreaMap SVG has alt elements and role
    ok 20 - T2.20 Interactive Components - ServiceAreaMap SVG has alt elements and role
      ---
      duration_ms: 0.3492
      type: 'test'
      ...
    # Subtest: T2.21 Room Calculator - Width values clamp strictly to 5 (min) and 50 (max)
    not ok 21 - T2.21 Room Calculator - Width values clamp strictly to 5 (min) and 50 (max)
      ---
      duration_ms: 1.0399
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:421:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /5, 50/. Input:
        
        "'use client';\n" +
          '\n' +
          "import { useState, FormEvent, useEffect, useRef } from 'react';\n" +
          "import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';\n" +
          "import PageTransition from '../components/PageTransition';\n" +
          "import RangeSlider from '../components/RangeSlider';\n" +
          "import { trackEvent } from '../lib/analytics';\n" +
          "import { buildEstimateMailto } from '../lib/contact';\n" +
          "import { motion, AnimatePresence } from 'motion/react';\n" +
          "import { cn } from '../lib/utils';\n" +
          '\n' +
          "type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;\n" +
          "type PrepLevel = 'standard' | 'premium' | null;\n" +
          '\n' +
          'interface ChatMessage {\n' +
          '  id: string;\n' +
          "  sender: 'bot' | 'user';\n" +
          '  text: React.ReactNode;\n' +
          '  isComponent?: boolean;\n' +
          '}\n' +
          '\n' +
          'const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };\n' +
          '\n' +
          'export default function EstimatePage() {\n' +
          '  const [messages, setMessages] = useState<ChatMessage[]>([]);\n' +
          '  const [isTyping, setIsTyping] = useState(false);\n' +
          '  const scrollRef = useRef<HTMLDivElement>(null);\n' +
          '\n' +
          '  // State\n' +
          '  const [step, setStep] = useState(0);\n' +
          '  const [projectType, setProjectType] = useState<ProjectType>(null);\n' +
          '  \n' +
          '  // Interior State\n' +
          "  const [roomType, setRoomType] = useState('Bedroom');\n" +
          '  const [width, setWidth] = useState(12);\n' +
          '  const [length, setLength] = useState(14);\n' +
          '  const [height, setHeight] = useState(8);\n' +
          '\n' +
          '  // Exterior State\n' +
          "  const [stories, setStories] = useState('1 Story');\n" +
          "  const [siding, setSiding] = useState('Wood / LP SmartSide');\n" +
          '\n' +
          '  // Cabinet State\n' +
          '  const [cabinetCount, setCabinetCount] = useState(20);\n' +
          '\n' +
          '  // Common State\n' +
          '  const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);\n' +
          '\n' +
          '  // Lead State\n' +
          "  const [name, setName] = useState('');\n" +
          "  const [phone, setPhone] = useState('');\n" +
          "  const [email, setEmail] = useState('');\n" +
          "  const [city, setCity] = useState('');\n" +
          "  const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');\n" +
          "  const [submitMessage, setSubmitMessage] = useState('');\n" +
          '\n' +
          '  const scrollToBottom = () => {\n' +
          '    if (scrollRef.current) {\n' +
          '      scrollRef.current.scrollTo({\n' +
          '        top: scrollRef.current.scrollHeight,\n' +
          "        behavior: 'smooth'\n" +
          '      });\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  useEffect(() => {\n' +
          '    scrollToBottom();\n' +
          '  }, [messages, isTyping, step]);\n' +
          '\n' +
          '  const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {\n' +
          '    setIsTyping(true);\n' +
          '    setTimeout(() => {\n' +
          '      setIsTyping(false);\n' +
          "      setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);\n" +
          '    }, delay);\n' +
          '  };\n' +
          '\n' +
          '  const addUserMessage = (text: string) => {\n' +
          "    setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);\n" +
          '  };\n' +
          '\n' +
          '  // Initialize\n' +
          '  useEffect(() => {\n' +
          '    if (step === 0) {\n' +
          `      addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);\n` +
          '      setTimeout(() => setStep(1), 1200);\n' +
          '    }\n' +
          '  }, []);\n' +
          '\n' +
          '  const handleProjectType = (type: ProjectType, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setProjectType(type);\n' +
          '    \n' +
          "    if (type === 'interior') {\n" +
          '      addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");\n' +
          '      setStep(2);\n' +
          "    } else if (type === 'exterior') {\n" +
          '      addBotMessage("Exterior painting. How many stories is the home?");\n' +
          '      setStep(3);\n' +
          "    } else if (type === 'cabinets') {\n" +
          '      addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");\n' +
          '      setStep(4);\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const handleInteriorRoom = (type: string) => {\n' +
          '    addUserMessage(type);\n' +
          '    setRoomType(type);\n' +
          '    addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");\n' +
          '    setStep(2.1);\n' +
          '  };\n' +
          '\n' +
          '  const submitInteriorDimensions = () => {\n' +
          '    addUserMessage(`${width}x${length}x${height} ft`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorStories = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setStories(val);\n' +
          '    addBotMessage("What type of siding do you have?");\n' +
          '    setStep(3.1);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorSiding = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setSiding(val);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const submitCabinets = () => {\n' +
          '    addUserMessage(`${cabinetCount} doors/drawers`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handlePrep = (prep: PrepLevel, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setPrepLevel(prep);\n' +
          '    addBotMessage("Calculating your estimate...", 400);\n' +
          '    setTimeout(() => {\n' +
          '      calculateFinal();\n' +
          '    }, 1500);\n' +
          '    setStep(6);\n' +
          '  };\n' +
          '\n' +
          '  const calculateFinal = () => {\n' +
          '    let low = 0;\n' +
          '    let high = 0;\n' +
          '\n' +
          "    if (projectType === 'interior') {\n" +
          '      const wallArea = 2 * (width + length) * height;\n' +
          '      const base = wallArea * 3.50;\n' +
          "      const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;\n" +
          '      const total = base + prepVal + 250;\n' +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          "    } else if (projectType === 'exterior') {\n" +
          '      let base = 3500;\n' +
          "      if (stories === '2 Story') base = 5500;\n" +
          "      if (stories === '3+ Story') base = 8500;\n" +
          '      \n' +
          '      let mult = 1.0;\n' +
          "      if (siding === 'Stucco') mult = 1.3;\n" +
          "      if (siding === 'Brick / Masonry') mult = 1.4;\n" +
          '\n' +
          "      const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);\n" +
          '      low = total * 0.85;\n' +
          '      high = total * 1.20;\n' +
          "    } else if (projectType === 'cabinets') {\n" +
          "      const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);\n" +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          '    }\n' +
          '\n' +
          '    low = Math.round(low / 100) * 100;\n' +
          '    high = Math.round(high / 100) * 100;\n' +
          '\n' +
          '    addBotMessage(\n' +
          '      <div className="space-y-4 reveal-up">\n' +
          '        <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>\n' +
          '        <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">\n' +
          '          <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>\n' +
          '          <p className="eyebrow mb-2">Estimated Range</p>\n' +
          '          <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>\n' +
          '        </div>\n' +
          '        <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>\n' +
          '      </div>\n' +
          '    , 200, true);\n' +
          '\n' +
          '    setStep(7);\n' +
          '  };\n' +
          '\n' +
          '  const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {\n' +
          '    e.preventDefault();\n' +
          '    const payload = {\n' +
          "      source: 'Chatbot Estimate',\n" +
          "      page: '/estimate',\n" +
          '      name, phone, email, city,\n' +
          '      projectType, prepLevel,\n' +
          '      notes: `Project: ${projectType} \\nPrep: ${prepLevel}`\n' +
          '    };\n' +
          '\n' +
          "    setStatus('submitting');\n" +
          '    try {\n' +
          "      const response = await fetch('/api/leads', {\n" +
          "        method: 'POST',\n" +
          "        headers: { 'Content-Type': 'application/json' },\n" +
          '        body: JSON.stringify(payload),\n' +
          '      });\n' +
          '      if (response.ok) {\n' +
          "        setStatus('sent');\n" +
          '        addUserMessage("Submitted my info.");\n' +
          `        addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);\n` +
          '      } else {\n' +
          "        setStatus('fallback');\n" +
          '      }\n' +
          '    } catch {\n' +
          "      setStatus('fallback');\n" +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const containerVariants = {\n' +
          '    hidden: { opacity: 0 },\n' +
          '    show: {\n' +
          '      opacity: 1,\n' +
          '      transition: { staggerChildren: 0.1 }\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const itemVariants = {\n' +
          '    hidden: { opacity: 0, y: 15, scale: 0.95 },\n' +
          '    show: { opacity: 1, y: 0, scale: 1, transition: springConfig }\n' +
          '  };\n' +
          '\n' +
          '  return (\n' +
          '    <PageTransition>\n' +
          '      <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">\n' +
          '        \n' +
          '        <div className="noise-overlay"></div>\n' +
          '\n' +
          '        <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">\n' +
          '          \n' +
          '          {/* Header */}\n' +
          '          <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">\n' +
          '            <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">\n' +
          '               <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />\n' +
          '            </div>\n' +
          '            <div>\n' +
          '              <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>\n' +
          '              <p className="eyebrow flex items-center gap-2 mt-1">\n' +
          '                <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online\n' +
          '              </p>\n' +
          '            </div>\n' +
          '          </div>\n' +
          '\n' +
          '          {/* Chat Area */}\n' +
          '          <div \n' +
          '            ref={scrollRef}\n' +
          '            className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"\n' +
          '          >\n' +
          '            <AnimatePresence initial={false}>\n' +
          '              {messages.map((msg) => (\n' +
          '                <motion.div\n' +
          '                  key={msg.id}\n' +
          '                  initial={{ opacity: 0, y: 20, scale: 0.95 }}\n' +
          '                  animate={{ opacity: 1, y: 0, scale: 1 }}\n' +
          '                  transition={springConfig}\n' +
          "                  className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}\n" +
          '                >\n' +
          "                  <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>\n" +
          '                    \n' +
          "                    <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>\n" +
          "                      {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}\n" +
          '                    </div>\n' +
          '\n' +
          '                    <div className={cn(\n' +
          '                      "p-5 text-sm l'... 10894 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import { useState, FormEvent, useEffect, useRef } from 'react';
        import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';
        import PageTransition from '../components/PageTransition';
        import RangeSlider from '../components/RangeSlider';
        import { trackEvent } from '../lib/analytics';
        import { buildEstimateMailto } from '../lib/contact';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '../lib/utils';
        
        type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;
        type PrepLevel = 'standard' | 'premium' | null;
        
        interface ChatMessage {
          id: string;
          sender: 'bot' | 'user';
          text: React.ReactNode;
          isComponent?: boolean;
        }
        
        const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };
        
        export default function EstimatePage() {
          const [messages, setMessages] = useState<ChatMessage[]>([]);
          const [isTyping, setIsTyping] = useState(false);
          const scrollRef = useRef<HTMLDivElement>(null);
        
          // State
          const [step, setStep] = useState(0);
          const [projectType, setProjectType] = useState<ProjectType>(null);
          
          // Interior State
          const [roomType, setRoomType] = useState('Bedroom');
          const [width, setWidth] = useState(12);
          const [length, setLength] = useState(14);
          const [height, setHeight] = useState(8);
        
          // Exterior State
          const [stories, setStories] = useState('1 Story');
          const [siding, setSiding] = useState('Wood / LP SmartSide');
        
          // Cabinet State
          const [cabinetCount, setCabinetCount] = useState(20);
        
          // Common State
          const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);
        
          // Lead State
          const [name, setName] = useState('');
          const [phone, setPhone] = useState('');
          const [email, setEmail] = useState('');
          const [city, setCity] = useState('');
          const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');
          const [submitMessage, setSubmitMessage] = useState('');
        
          const scrollToBottom = () => {
            if (scrollRef.current) {
              scrollRef.current.scrollTo({
                top: scrollRef.current.scrollHeight,
                behavior: 'smooth'
              });
            }
          };
        
          useEffect(() => {
            scrollToBottom();
          }, [messages, isTyping, step]);
        
          const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {
            setIsTyping(true);
            setTimeout(() => {
              setIsTyping(false);
              setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);
            }, delay);
          };
        
          const addUserMessage = (text: string) => {
            setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);
          };
        
          // Initialize
          useEffect(() => {
            if (step === 0) {
              addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);
              setTimeout(() => setStep(1), 1200);
            }
          }, []);
        
          const handleProjectType = (type: ProjectType, label: string) => {
            addUserMessage(label);
            setProjectType(type);
            
            if (type === 'interior') {
              addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");
              setStep(2);
            } else if (type === 'exterior') {
              addBotMessage("Exterior painting. How many stories is the home?");
              setStep(3);
            } else if (type === 'cabinets') {
              addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");
              setStep(4);
            }
          };
        
          const handleInteriorRoom = (type: string) => {
            addUserMessage(type);
            setRoomType(type);
            addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");
            setStep(2.1);
          };
        
          const submitInteriorDimensions = () => {
            addUserMessage(`${width}x${length}x${height} ft`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handleExteriorStories = (val: string) => {
            addUserMessage(val);
            setStories(val);
            addBotMessage("What type of siding do you have?");
            setStep(3.1);
          };
        
          const handleExteriorSiding = (val: string) => {
            addUserMessage(val);
            setSiding(val);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const submitCabinets = () => {
            addUserMessage(`${cabinetCount} doors/drawers`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handlePrep = (prep: PrepLevel, label: string) => {
            addUserMessage(label);
            setPrepLevel(prep);
            addBotMessage("Calculating your estimate...", 400);
            setTimeout(() => {
              calculateFinal();
            }, 1500);
            setStep(6);
          };
        
          const calculateFinal = () => {
            let low = 0;
            let high = 0;
        
            if (projectType === 'interior') {
              const wallArea = 2 * (width + length) * height;
              const base = wallArea * 3.50;
              const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;
              const total = base + prepVal + 250;
              low = total * 0.90;
              high = total * 1.15;
            } else if (projectType === 'exterior') {
              let base = 3500;
              if (stories === '2 Story') base = 5500;
              if (stories === '3+ Story') base = 8500;
              
              let mult = 1.0;
              if (siding === 'Stucco') mult = 1.3;
              if (siding === 'Brick / Masonry') mult = 1.4;
        
              const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);
              low = total * 0.85;
              high = total * 1.20;
            } else if (projectType === 'cabinets') {
              const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);
              low = total * 0.90;
              high = total * 1.15;
            }
        
            low = Math.round(low / 100) * 100;
            high = Math.round(high / 100) * 100;
        
            addBotMessage(
              <div className="space-y-4 reveal-up">
                <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>
                <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>
                  <p className="eyebrow mb-2">Estimated Range</p>
                  <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>
                </div>
                <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>
              </div>
            , 200, true);
        
            setStep(7);
          };
        
          const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {
            e.preventDefault();
            const payload = {
              source: 'Chatbot Estimate',
              page: '/estimate',
              name, phone, email, city,
              projectType, prepLevel,
              notes: `Project: ${projectType} \nPrep: ${prepLevel}`
            };
        
            setStatus('submitting');
            try {
              const response = await fetch('/api/leads', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload),
              });
              if (response.ok) {
                setStatus('sent');
                addUserMessage("Submitted my info.");
                addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);
              } else {
                setStatus('fallback');
              }
            } catch {
              setStatus('fallback');
            }
          };
        
          const containerVariants = {
            hidden: { opacity: 0 },
            show: {
              opacity: 1,
              transition: { staggerChildren: 0.1 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, y: 15, scale: 0.95 },
            show: { opacity: 1, y: 0, scale: 1, transition: springConfig }
          };
        
          return (
            <PageTransition>
              <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">
                
                <div className="noise-overlay"></div>
        
                <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">
                  
                  {/* Header */}
                  <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">
                    <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">
                       <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />
                    </div>
                    <div>
                      <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>
                      <p className="eyebrow flex items-center gap-2 mt-1">
                        <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online
                      </p>
                    </div>
                  </div>
        
                  {/* Chat Area */}
                  <div 
                    ref={scrollRef}
                    className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"
                  >
                    <AnimatePresence initial={false}>
                      {messages.map((msg) => (
                        <motion.div
                          key={msg.id}
                          initial={{ opacity: 0, y: 20, scale: 0.95 }}
                          animate={{ opacity: 1, y: 0, scale: 1 }}
                          transition={springConfig}
                          className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}
                        >
                          <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>
                            
                            <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>
                              {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}
                            </div>
        
                            <div className={cn(
                              "p-5 text-sm leading-relaxed",
                              msg.sender === 'user' 
                                ? "bg-[#FF5A00] text-white" 
                                : msg.isComponent 
                                  ? "bg-transparent p-0 w-full"
                                  : "bg-[#11100d] border border-white/10 text-[#c9c1b4]"
                            )}>
                              {msg.text}
                            </div>
                          </div>
                        </motion.div>
                      ))}
        
                      {isTyping && (
                        <motion.div
                          initial={{ opacity: 0, y: 10 }}
                          animate={{ opacity: 1, y: 0 }}
                          exit={{ opacity: 0, scale: 0.95 }}
                          transition={{ duration: 0.2 }}
                          className="flex justify-start"
                        >
                          <div className="flex gap-4">
                            <div className="shrink-0 w-10 h-10 flex items-center justify-center bg-[#182023] text-white border border-white/10">
                              <Bot size={18} strokeWidth={1.5} />
                            </div>
                            <div className="bg-[#11100d] border border-white/10 p-5 flex items-center gap-2 h-[60px]">
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.3s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.15s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce"></div>
                            </div>
                          </div>
                        </motion.div>
                      )}
                    </AnimatePresence>
                  </div>
        
                  {/* Controls Footer */}
                  <div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-[#050505] via-[#050505]/95 to-transparent pt-12 pb-6 px-4 sm:px-8 border-t border-white/5 z-20">
                     <AnimatePresence mode="wait">
                       {step === 1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col sm:flex-row gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('interior', 'Interior Rooms')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Interior Rooms</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('exterior', 'Exterior Painting')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Exterior Painting</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('cabinets', 'Cabinet Refinishing')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Cabinet Refinishing</motion.button>
                         </motion.div>
                       )}
        
                       {step === 2 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Bedroom', 'Living Room', 'Kitchen', 'Bathroom', 'Hallway', 'Other'].map(r => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={r} onClick={() => handleInteriorRoom(r)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">{r}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 2.1 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="w" label="Width" value={width} min={5} max={40} suffix="FT" onChange={setWidth} />
                            <RangeSlider id="l" label="Length" value={length} min={5} max={40} suffix="FT" onChange={setLength} />
                            <RangeSlider id="h" label="Ceiling Height" value={height} min={7} max={20} suffix="FT" onChange={setHeight} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitInteriorDimensions} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Dimensions</motion.button>
                         </motion.div>
                       )}
        
                       {step === 3 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           {['1 Story', '2 Story', '3+ Story'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorStories(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-left">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 3.1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Wood / LP SmartSide', 'Stucco', 'Vinyl / Aluminum', 'Brick / Masonry'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorSiding(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-center flex items-center justify-center h-full">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 4 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="c" label="Total Doors & Drawers" value={cabinetCount} min={5} max={60} onChange={setCabinetCount} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitCabinets} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Count</motion.button>
                         </motion.div>
                       )}
        
                       {step === 5 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('standard', 'Standard Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-white">Standard Prep</p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Light αª╕αªéαªòαªƒ, minor caulk, 1 coat primer & topcoat. Great for minor refreshes.</p>
                           </motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('premium', 'Premium Detail Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-[#FF5A00] flex items-center gap-2">Premium Detail Prep <span className="bg-[#FF5A00]/20 text-[#FF5A00] text-[10px] uppercase px-2 py-0.5 tracking-wider font-bold">Recommended</span></p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Elite multi-stage sanding, deep caulking, wood stabilization, premium coats.</p>
                           </motion.button>
                         </motion.div>
                       )}
        
                       {step === 7 && !isTyping && status !== 'sent' && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={springConfig} className="bg-[#11100d] border border-[#FF5A00]/30 p-6 shadow-2xl relative overflow-hidden">
                           <div className="absolute top-0 left-0 right-0 h-1 bg-[#FF5A00]"></div>
                           <form onSubmit={handleFinalSubmit} className="space-y-4">
                             <p className="eyebrow mb-4">Request Walkthrough</p>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="text" placeholder="Name" value={name} onChange={e => setName(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="tel" placeholder="Phone" value={phone} onChange={e => setPhone(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="email" placeholder="Email" value={email} onChange={e => setEmail(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="text" placeholder="City" value={city} onChange={e => setCity(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} disabled={status === 'submitting'} type="submit" className="w-full bg-[#FF5A00] text-white p-4 text-sm font-bold flex items-center justify-center gap-2 hover:bg-[#ff7a33] transition-colors disabled:opacity-50 mt-2 shimmer-cta">
                               {status === 'submitting' ? <Loader2 className="animate-spin" size={16} /> : 'Lock In Estimate'}
                             </motion.button>
                           </form>
                         </motion.div>
                       )}
                     </AnimatePresence>
                  </div>
        
                </div>
                <div className="text-center text-xs text-gray-600 mt-2 pb-4">
                  reg: ir816596 | painting
                </div>
              </section>
            </PageTransition>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:423:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: T2.22 Room Calculator - Length values clamp strictly to 5 (min) and 50 (max)
    not ok 22 - T2.22 Room Calculator - Length values clamp strictly to 5 (min) and 50 (max)
      ---
      duration_ms: 0.9937
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:426:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /5, 50/. Input:
        
        "'use client';\n" +
          '\n' +
          "import { useState, FormEvent, useEffect, useRef } from 'react';\n" +
          "import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';\n" +
          "import PageTransition from '../components/PageTransition';\n" +
          "import RangeSlider from '../components/RangeSlider';\n" +
          "import { trackEvent } from '../lib/analytics';\n" +
          "import { buildEstimateMailto } from '../lib/contact';\n" +
          "import { motion, AnimatePresence } from 'motion/react';\n" +
          "import { cn } from '../lib/utils';\n" +
          '\n' +
          "type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;\n" +
          "type PrepLevel = 'standard' | 'premium' | null;\n" +
          '\n' +
          'interface ChatMessage {\n' +
          '  id: string;\n' +
          "  sender: 'bot' | 'user';\n" +
          '  text: React.ReactNode;\n' +
          '  isComponent?: boolean;\n' +
          '}\n' +
          '\n' +
          'const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };\n' +
          '\n' +
          'export default function EstimatePage() {\n' +
          '  const [messages, setMessages] = useState<ChatMessage[]>([]);\n' +
          '  const [isTyping, setIsTyping] = useState(false);\n' +
          '  const scrollRef = useRef<HTMLDivElement>(null);\n' +
          '\n' +
          '  // State\n' +
          '  const [step, setStep] = useState(0);\n' +
          '  const [projectType, setProjectType] = useState<ProjectType>(null);\n' +
          '  \n' +
          '  // Interior State\n' +
          "  const [roomType, setRoomType] = useState('Bedroom');\n" +
          '  const [width, setWidth] = useState(12);\n' +
          '  const [length, setLength] = useState(14);\n' +
          '  const [height, setHeight] = useState(8);\n' +
          '\n' +
          '  // Exterior State\n' +
          "  const [stories, setStories] = useState('1 Story');\n" +
          "  const [siding, setSiding] = useState('Wood / LP SmartSide');\n" +
          '\n' +
          '  // Cabinet State\n' +
          '  const [cabinetCount, setCabinetCount] = useState(20);\n' +
          '\n' +
          '  // Common State\n' +
          '  const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);\n' +
          '\n' +
          '  // Lead State\n' +
          "  const [name, setName] = useState('');\n" +
          "  const [phone, setPhone] = useState('');\n" +
          "  const [email, setEmail] = useState('');\n" +
          "  const [city, setCity] = useState('');\n" +
          "  const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');\n" +
          "  const [submitMessage, setSubmitMessage] = useState('');\n" +
          '\n' +
          '  const scrollToBottom = () => {\n' +
          '    if (scrollRef.current) {\n' +
          '      scrollRef.current.scrollTo({\n' +
          '        top: scrollRef.current.scrollHeight,\n' +
          "        behavior: 'smooth'\n" +
          '      });\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  useEffect(() => {\n' +
          '    scrollToBottom();\n' +
          '  }, [messages, isTyping, step]);\n' +
          '\n' +
          '  const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {\n' +
          '    setIsTyping(true);\n' +
          '    setTimeout(() => {\n' +
          '      setIsTyping(false);\n' +
          "      setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);\n" +
          '    }, delay);\n' +
          '  };\n' +
          '\n' +
          '  const addUserMessage = (text: string) => {\n' +
          "    setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);\n" +
          '  };\n' +
          '\n' +
          '  // Initialize\n' +
          '  useEffect(() => {\n' +
          '    if (step === 0) {\n' +
          `      addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);\n` +
          '      setTimeout(() => setStep(1), 1200);\n' +
          '    }\n' +
          '  }, []);\n' +
          '\n' +
          '  const handleProjectType = (type: ProjectType, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setProjectType(type);\n' +
          '    \n' +
          "    if (type === 'interior') {\n" +
          '      addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");\n' +
          '      setStep(2);\n' +
          "    } else if (type === 'exterior') {\n" +
          '      addBotMessage("Exterior painting. How many stories is the home?");\n' +
          '      setStep(3);\n' +
          "    } else if (type === 'cabinets') {\n" +
          '      addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");\n' +
          '      setStep(4);\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const handleInteriorRoom = (type: string) => {\n' +
          '    addUserMessage(type);\n' +
          '    setRoomType(type);\n' +
          '    addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");\n' +
          '    setStep(2.1);\n' +
          '  };\n' +
          '\n' +
          '  const submitInteriorDimensions = () => {\n' +
          '    addUserMessage(`${width}x${length}x${height} ft`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorStories = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setStories(val);\n' +
          '    addBotMessage("What type of siding do you have?");\n' +
          '    setStep(3.1);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorSiding = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setSiding(val);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const submitCabinets = () => {\n' +
          '    addUserMessage(`${cabinetCount} doors/drawers`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handlePrep = (prep: PrepLevel, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setPrepLevel(prep);\n' +
          '    addBotMessage("Calculating your estimate...", 400);\n' +
          '    setTimeout(() => {\n' +
          '      calculateFinal();\n' +
          '    }, 1500);\n' +
          '    setStep(6);\n' +
          '  };\n' +
          '\n' +
          '  const calculateFinal = () => {\n' +
          '    let low = 0;\n' +
          '    let high = 0;\n' +
          '\n' +
          "    if (projectType === 'interior') {\n" +
          '      const wallArea = 2 * (width + length) * height;\n' +
          '      const base = wallArea * 3.50;\n' +
          "      const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;\n" +
          '      const total = base + prepVal + 250;\n' +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          "    } else if (projectType === 'exterior') {\n" +
          '      let base = 3500;\n' +
          "      if (stories === '2 Story') base = 5500;\n" +
          "      if (stories === '3+ Story') base = 8500;\n" +
          '      \n' +
          '      let mult = 1.0;\n' +
          "      if (siding === 'Stucco') mult = 1.3;\n" +
          "      if (siding === 'Brick / Masonry') mult = 1.4;\n" +
          '\n' +
          "      const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);\n" +
          '      low = total * 0.85;\n' +
          '      high = total * 1.20;\n' +
          "    } else if (projectType === 'cabinets') {\n" +
          "      const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);\n" +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          '    }\n' +
          '\n' +
          '    low = Math.round(low / 100) * 100;\n' +
          '    high = Math.round(high / 100) * 100;\n' +
          '\n' +
          '    addBotMessage(\n' +
          '      <div className="space-y-4 reveal-up">\n' +
          '        <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>\n' +
          '        <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">\n' +
          '          <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>\n' +
          '          <p className="eyebrow mb-2">Estimated Range</p>\n' +
          '          <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>\n' +
          '        </div>\n' +
          '        <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>\n' +
          '      </div>\n' +
          '    , 200, true);\n' +
          '\n' +
          '    setStep(7);\n' +
          '  };\n' +
          '\n' +
          '  const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {\n' +
          '    e.preventDefault();\n' +
          '    const payload = {\n' +
          "      source: 'Chatbot Estimate',\n" +
          "      page: '/estimate',\n" +
          '      name, phone, email, city,\n' +
          '      projectType, prepLevel,\n' +
          '      notes: `Project: ${projectType} \\nPrep: ${prepLevel}`\n' +
          '    };\n' +
          '\n' +
          "    setStatus('submitting');\n" +
          '    try {\n' +
          "      const response = await fetch('/api/leads', {\n" +
          "        method: 'POST',\n" +
          "        headers: { 'Content-Type': 'application/json' },\n" +
          '        body: JSON.stringify(payload),\n' +
          '      });\n' +
          '      if (response.ok) {\n' +
          "        setStatus('sent');\n" +
          '        addUserMessage("Submitted my info.");\n' +
          `        addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);\n` +
          '      } else {\n' +
          "        setStatus('fallback');\n" +
          '      }\n' +
          '    } catch {\n' +
          "      setStatus('fallback');\n" +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const containerVariants = {\n' +
          '    hidden: { opacity: 0 },\n' +
          '    show: {\n' +
          '      opacity: 1,\n' +
          '      transition: { staggerChildren: 0.1 }\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const itemVariants = {\n' +
          '    hidden: { opacity: 0, y: 15, scale: 0.95 },\n' +
          '    show: { opacity: 1, y: 0, scale: 1, transition: springConfig }\n' +
          '  };\n' +
          '\n' +
          '  return (\n' +
          '    <PageTransition>\n' +
          '      <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">\n' +
          '        \n' +
          '        <div className="noise-overlay"></div>\n' +
          '\n' +
          '        <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">\n' +
          '          \n' +
          '          {/* Header */}\n' +
          '          <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">\n' +
          '            <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">\n' +
          '               <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />\n' +
          '            </div>\n' +
          '            <div>\n' +
          '              <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>\n' +
          '              <p className="eyebrow flex items-center gap-2 mt-1">\n' +
          '                <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online\n' +
          '              </p>\n' +
          '            </div>\n' +
          '          </div>\n' +
          '\n' +
          '          {/* Chat Area */}\n' +
          '          <div \n' +
          '            ref={scrollRef}\n' +
          '            className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"\n' +
          '          >\n' +
          '            <AnimatePresence initial={false}>\n' +
          '              {messages.map((msg) => (\n' +
          '                <motion.div\n' +
          '                  key={msg.id}\n' +
          '                  initial={{ opacity: 0, y: 20, scale: 0.95 }}\n' +
          '                  animate={{ opacity: 1, y: 0, scale: 1 }}\n' +
          '                  transition={springConfig}\n' +
          "                  className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}\n" +
          '                >\n' +
          "                  <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>\n" +
          '                    \n' +
          "                    <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>\n" +
          "                      {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}\n" +
          '                    </div>\n' +
          '\n' +
          '                    <div className={cn(\n' +
          '                      "p-5 text-sm l'... 10894 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import { useState, FormEvent, useEffect, useRef } from 'react';
        import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';
        import PageTransition from '../components/PageTransition';
        import RangeSlider from '../components/RangeSlider';
        import { trackEvent } from '../lib/analytics';
        import { buildEstimateMailto } from '../lib/contact';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '../lib/utils';
        
        type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;
        type PrepLevel = 'standard' | 'premium' | null;
        
        interface ChatMessage {
          id: string;
          sender: 'bot' | 'user';
          text: React.ReactNode;
          isComponent?: boolean;
        }
        
        const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };
        
        export default function EstimatePage() {
          const [messages, setMessages] = useState<ChatMessage[]>([]);
          const [isTyping, setIsTyping] = useState(false);
          const scrollRef = useRef<HTMLDivElement>(null);
        
          // State
          const [step, setStep] = useState(0);
          const [projectType, setProjectType] = useState<ProjectType>(null);
          
          // Interior State
          const [roomType, setRoomType] = useState('Bedroom');
          const [width, setWidth] = useState(12);
          const [length, setLength] = useState(14);
          const [height, setHeight] = useState(8);
        
          // Exterior State
          const [stories, setStories] = useState('1 Story');
          const [siding, setSiding] = useState('Wood / LP SmartSide');
        
          // Cabinet State
          const [cabinetCount, setCabinetCount] = useState(20);
        
          // Common State
          const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);
        
          // Lead State
          const [name, setName] = useState('');
          const [phone, setPhone] = useState('');
          const [email, setEmail] = useState('');
          const [city, setCity] = useState('');
          const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');
          const [submitMessage, setSubmitMessage] = useState('');
        
          const scrollToBottom = () => {
            if (scrollRef.current) {
              scrollRef.current.scrollTo({
                top: scrollRef.current.scrollHeight,
                behavior: 'smooth'
              });
            }
          };
        
          useEffect(() => {
            scrollToBottom();
          }, [messages, isTyping, step]);
        
          const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {
            setIsTyping(true);
            setTimeout(() => {
              setIsTyping(false);
              setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);
            }, delay);
          };
        
          const addUserMessage = (text: string) => {
            setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);
          };
        
          // Initialize
          useEffect(() => {
            if (step === 0) {
              addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);
              setTimeout(() => setStep(1), 1200);
            }
          }, []);
        
          const handleProjectType = (type: ProjectType, label: string) => {
            addUserMessage(label);
            setProjectType(type);
            
            if (type === 'interior') {
              addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");
              setStep(2);
            } else if (type === 'exterior') {
              addBotMessage("Exterior painting. How many stories is the home?");
              setStep(3);
            } else if (type === 'cabinets') {
              addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");
              setStep(4);
            }
          };
        
          const handleInteriorRoom = (type: string) => {
            addUserMessage(type);
            setRoomType(type);
            addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");
            setStep(2.1);
          };
        
          const submitInteriorDimensions = () => {
            addUserMessage(`${width}x${length}x${height} ft`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handleExteriorStories = (val: string) => {
            addUserMessage(val);
            setStories(val);
            addBotMessage("What type of siding do you have?");
            setStep(3.1);
          };
        
          const handleExteriorSiding = (val: string) => {
            addUserMessage(val);
            setSiding(val);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const submitCabinets = () => {
            addUserMessage(`${cabinetCount} doors/drawers`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handlePrep = (prep: PrepLevel, label: string) => {
            addUserMessage(label);
            setPrepLevel(prep);
            addBotMessage("Calculating your estimate...", 400);
            setTimeout(() => {
              calculateFinal();
            }, 1500);
            setStep(6);
          };
        
          const calculateFinal = () => {
            let low = 0;
            let high = 0;
        
            if (projectType === 'interior') {
              const wallArea = 2 * (width + length) * height;
              const base = wallArea * 3.50;
              const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;
              const total = base + prepVal + 250;
              low = total * 0.90;
              high = total * 1.15;
            } else if (projectType === 'exterior') {
              let base = 3500;
              if (stories === '2 Story') base = 5500;
              if (stories === '3+ Story') base = 8500;
              
              let mult = 1.0;
              if (siding === 'Stucco') mult = 1.3;
              if (siding === 'Brick / Masonry') mult = 1.4;
        
              const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);
              low = total * 0.85;
              high = total * 1.20;
            } else if (projectType === 'cabinets') {
              const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);
              low = total * 0.90;
              high = total * 1.15;
            }
        
            low = Math.round(low / 100) * 100;
            high = Math.round(high / 100) * 100;
        
            addBotMessage(
              <div className="space-y-4 reveal-up">
                <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>
                <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>
                  <p className="eyebrow mb-2">Estimated Range</p>
                  <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>
                </div>
                <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>
              </div>
            , 200, true);
        
            setStep(7);
          };
        
          const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {
            e.preventDefault();
            const payload = {
              source: 'Chatbot Estimate',
              page: '/estimate',
              name, phone, email, city,
              projectType, prepLevel,
              notes: `Project: ${projectType} \nPrep: ${prepLevel}`
            };
        
            setStatus('submitting');
            try {
              const response = await fetch('/api/leads', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload),
              });
              if (response.ok) {
                setStatus('sent');
                addUserMessage("Submitted my info.");
                addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);
              } else {
                setStatus('fallback');
              }
            } catch {
              setStatus('fallback');
            }
          };
        
          const containerVariants = {
            hidden: { opacity: 0 },
            show: {
              opacity: 1,
              transition: { staggerChildren: 0.1 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, y: 15, scale: 0.95 },
            show: { opacity: 1, y: 0, scale: 1, transition: springConfig }
          };
        
          return (
            <PageTransition>
              <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">
                
                <div className="noise-overlay"></div>
        
                <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">
                  
                  {/* Header */}
                  <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">
                    <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">
                       <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />
                    </div>
                    <div>
                      <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>
                      <p className="eyebrow flex items-center gap-2 mt-1">
                        <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online
                      </p>
                    </div>
                  </div>
        
                  {/* Chat Area */}
                  <div 
                    ref={scrollRef}
                    className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"
                  >
                    <AnimatePresence initial={false}>
                      {messages.map((msg) => (
                        <motion.div
                          key={msg.id}
                          initial={{ opacity: 0, y: 20, scale: 0.95 }}
                          animate={{ opacity: 1, y: 0, scale: 1 }}
                          transition={springConfig}
                          className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}
                        >
                          <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>
                            
                            <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>
                              {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}
                            </div>
        
                            <div className={cn(
                              "p-5 text-sm leading-relaxed",
                              msg.sender === 'user' 
                                ? "bg-[#FF5A00] text-white" 
                                : msg.isComponent 
                                  ? "bg-transparent p-0 w-full"
                                  : "bg-[#11100d] border border-white/10 text-[#c9c1b4]"
                            )}>
                              {msg.text}
                            </div>
                          </div>
                        </motion.div>
                      ))}
        
                      {isTyping && (
                        <motion.div
                          initial={{ opacity: 0, y: 10 }}
                          animate={{ opacity: 1, y: 0 }}
                          exit={{ opacity: 0, scale: 0.95 }}
                          transition={{ duration: 0.2 }}
                          className="flex justify-start"
                        >
                          <div className="flex gap-4">
                            <div className="shrink-0 w-10 h-10 flex items-center justify-center bg-[#182023] text-white border border-white/10">
                              <Bot size={18} strokeWidth={1.5} />
                            </div>
                            <div className="bg-[#11100d] border border-white/10 p-5 flex items-center gap-2 h-[60px]">
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.3s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.15s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce"></div>
                            </div>
                          </div>
                        </motion.div>
                      )}
                    </AnimatePresence>
                  </div>
        
                  {/* Controls Footer */}
                  <div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-[#050505] via-[#050505]/95 to-transparent pt-12 pb-6 px-4 sm:px-8 border-t border-white/5 z-20">
                     <AnimatePresence mode="wait">
                       {step === 1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col sm:flex-row gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('interior', 'Interior Rooms')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Interior Rooms</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('exterior', 'Exterior Painting')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Exterior Painting</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('cabinets', 'Cabinet Refinishing')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Cabinet Refinishing</motion.button>
                         </motion.div>
                       )}
        
                       {step === 2 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Bedroom', 'Living Room', 'Kitchen', 'Bathroom', 'Hallway', 'Other'].map(r => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={r} onClick={() => handleInteriorRoom(r)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">{r}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 2.1 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="w" label="Width" value={width} min={5} max={40} suffix="FT" onChange={setWidth} />
                            <RangeSlider id="l" label="Length" value={length} min={5} max={40} suffix="FT" onChange={setLength} />
                            <RangeSlider id="h" label="Ceiling Height" value={height} min={7} max={20} suffix="FT" onChange={setHeight} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitInteriorDimensions} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Dimensions</motion.button>
                         </motion.div>
                       )}
        
                       {step === 3 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           {['1 Story', '2 Story', '3+ Story'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorStories(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-left">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 3.1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Wood / LP SmartSide', 'Stucco', 'Vinyl / Aluminum', 'Brick / Masonry'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorSiding(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-center flex items-center justify-center h-full">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 4 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="c" label="Total Doors & Drawers" value={cabinetCount} min={5} max={60} onChange={setCabinetCount} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitCabinets} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Count</motion.button>
                         </motion.div>
                       )}
        
                       {step === 5 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('standard', 'Standard Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-white">Standard Prep</p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Light αª╕αªéαªòαªƒ, minor caulk, 1 coat primer & topcoat. Great for minor refreshes.</p>
                           </motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('premium', 'Premium Detail Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-[#FF5A00] flex items-center gap-2">Premium Detail Prep <span className="bg-[#FF5A00]/20 text-[#FF5A00] text-[10px] uppercase px-2 py-0.5 tracking-wider font-bold">Recommended</span></p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Elite multi-stage sanding, deep caulking, wood stabilization, premium coats.</p>
                           </motion.button>
                         </motion.div>
                       )}
        
                       {step === 7 && !isTyping && status !== 'sent' && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={springConfig} className="bg-[#11100d] border border-[#FF5A00]/30 p-6 shadow-2xl relative overflow-hidden">
                           <div className="absolute top-0 left-0 right-0 h-1 bg-[#FF5A00]"></div>
                           <form onSubmit={handleFinalSubmit} className="space-y-4">
                             <p className="eyebrow mb-4">Request Walkthrough</p>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="text" placeholder="Name" value={name} onChange={e => setName(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="tel" placeholder="Phone" value={phone} onChange={e => setPhone(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="email" placeholder="Email" value={email} onChange={e => setEmail(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="text" placeholder="City" value={city} onChange={e => setCity(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} disabled={status === 'submitting'} type="submit" className="w-full bg-[#FF5A00] text-white p-4 text-sm font-bold flex items-center justify-center gap-2 hover:bg-[#ff7a33] transition-colors disabled:opacity-50 mt-2 shimmer-cta">
                               {status === 'submitting' ? <Loader2 className="animate-spin" size={16} /> : 'Lock In Estimate'}
                             </motion.button>
                           </form>
                         </motion.div>
                       )}
                     </AnimatePresence>
                  </div>
        
                </div>
                <div className="text-center text-xs text-gray-600 mt-2 pb-4">
                  reg: ir816596 | painting
                </div>
              </section>
            </PageTransition>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:428:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: T2.23 Room Calculator - Ceiling height values clamp strictly to 7 (min) and 20 (max)
    not ok 23 - T2.23 Room Calculator - Ceiling height values clamp strictly to 7 (min) and 20 (max)
      ---
      duration_ms: 1.0323
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:431:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /7, 20/. Input:
        
        "'use client';\n" +
          '\n' +
          "import { useState, FormEvent, useEffect, useRef } from 'react';\n" +
          "import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';\n" +
          "import PageTransition from '../components/PageTransition';\n" +
          "import RangeSlider from '../components/RangeSlider';\n" +
          "import { trackEvent } from '../lib/analytics';\n" +
          "import { buildEstimateMailto } from '../lib/contact';\n" +
          "import { motion, AnimatePresence } from 'motion/react';\n" +
          "import { cn } from '../lib/utils';\n" +
          '\n' +
          "type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;\n" +
          "type PrepLevel = 'standard' | 'premium' | null;\n" +
          '\n' +
          'interface ChatMessage {\n' +
          '  id: string;\n' +
          "  sender: 'bot' | 'user';\n" +
          '  text: React.ReactNode;\n' +
          '  isComponent?: boolean;\n' +
          '}\n' +
          '\n' +
          'const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };\n' +
          '\n' +
          'export default function EstimatePage() {\n' +
          '  const [messages, setMessages] = useState<ChatMessage[]>([]);\n' +
          '  const [isTyping, setIsTyping] = useState(false);\n' +
          '  const scrollRef = useRef<HTMLDivElement>(null);\n' +
          '\n' +
          '  // State\n' +
          '  const [step, setStep] = useState(0);\n' +
          '  const [projectType, setProjectType] = useState<ProjectType>(null);\n' +
          '  \n' +
          '  // Interior State\n' +
          "  const [roomType, setRoomType] = useState('Bedroom');\n" +
          '  const [width, setWidth] = useState(12);\n' +
          '  const [length, setLength] = useState(14);\n' +
          '  const [height, setHeight] = useState(8);\n' +
          '\n' +
          '  // Exterior State\n' +
          "  const [stories, setStories] = useState('1 Story');\n" +
          "  const [siding, setSiding] = useState('Wood / LP SmartSide');\n" +
          '\n' +
          '  // Cabinet State\n' +
          '  const [cabinetCount, setCabinetCount] = useState(20);\n' +
          '\n' +
          '  // Common State\n' +
          '  const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);\n' +
          '\n' +
          '  // Lead State\n' +
          "  const [name, setName] = useState('');\n" +
          "  const [phone, setPhone] = useState('');\n" +
          "  const [email, setEmail] = useState('');\n" +
          "  const [city, setCity] = useState('');\n" +
          "  const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');\n" +
          "  const [submitMessage, setSubmitMessage] = useState('');\n" +
          '\n' +
          '  const scrollToBottom = () => {\n' +
          '    if (scrollRef.current) {\n' +
          '      scrollRef.current.scrollTo({\n' +
          '        top: scrollRef.current.scrollHeight,\n' +
          "        behavior: 'smooth'\n" +
          '      });\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  useEffect(() => {\n' +
          '    scrollToBottom();\n' +
          '  }, [messages, isTyping, step]);\n' +
          '\n' +
          '  const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {\n' +
          '    setIsTyping(true);\n' +
          '    setTimeout(() => {\n' +
          '      setIsTyping(false);\n' +
          "      setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);\n" +
          '    }, delay);\n' +
          '  };\n' +
          '\n' +
          '  const addUserMessage = (text: string) => {\n' +
          "    setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);\n" +
          '  };\n' +
          '\n' +
          '  // Initialize\n' +
          '  useEffect(() => {\n' +
          '    if (step === 0) {\n' +
          `      addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);\n` +
          '      setTimeout(() => setStep(1), 1200);\n' +
          '    }\n' +
          '  }, []);\n' +
          '\n' +
          '  const handleProjectType = (type: ProjectType, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setProjectType(type);\n' +
          '    \n' +
          "    if (type === 'interior') {\n" +
          '      addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");\n' +
          '      setStep(2);\n' +
          "    } else if (type === 'exterior') {\n" +
          '      addBotMessage("Exterior painting. How many stories is the home?");\n' +
          '      setStep(3);\n' +
          "    } else if (type === 'cabinets') {\n" +
          '      addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");\n' +
          '      setStep(4);\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const handleInteriorRoom = (type: string) => {\n' +
          '    addUserMessage(type);\n' +
          '    setRoomType(type);\n' +
          '    addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");\n' +
          '    setStep(2.1);\n' +
          '  };\n' +
          '\n' +
          '  const submitInteriorDimensions = () => {\n' +
          '    addUserMessage(`${width}x${length}x${height} ft`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorStories = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setStories(val);\n' +
          '    addBotMessage("What type of siding do you have?");\n' +
          '    setStep(3.1);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorSiding = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setSiding(val);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const submitCabinets = () => {\n' +
          '    addUserMessage(`${cabinetCount} doors/drawers`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handlePrep = (prep: PrepLevel, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setPrepLevel(prep);\n' +
          '    addBotMessage("Calculating your estimate...", 400);\n' +
          '    setTimeout(() => {\n' +
          '      calculateFinal();\n' +
          '    }, 1500);\n' +
          '    setStep(6);\n' +
          '  };\n' +
          '\n' +
          '  const calculateFinal = () => {\n' +
          '    let low = 0;\n' +
          '    let high = 0;\n' +
          '\n' +
          "    if (projectType === 'interior') {\n" +
          '      const wallArea = 2 * (width + length) * height;\n' +
          '      const base = wallArea * 3.50;\n' +
          "      const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;\n" +
          '      const total = base + prepVal + 250;\n' +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          "    } else if (projectType === 'exterior') {\n" +
          '      let base = 3500;\n' +
          "      if (stories === '2 Story') base = 5500;\n" +
          "      if (stories === '3+ Story') base = 8500;\n" +
          '      \n' +
          '      let mult = 1.0;\n' +
          "      if (siding === 'Stucco') mult = 1.3;\n" +
          "      if (siding === 'Brick / Masonry') mult = 1.4;\n" +
          '\n' +
          "      const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);\n" +
          '      low = total * 0.85;\n' +
          '      high = total * 1.20;\n' +
          "    } else if (projectType === 'cabinets') {\n" +
          "      const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);\n" +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          '    }\n' +
          '\n' +
          '    low = Math.round(low / 100) * 100;\n' +
          '    high = Math.round(high / 100) * 100;\n' +
          '\n' +
          '    addBotMessage(\n' +
          '      <div className="space-y-4 reveal-up">\n' +
          '        <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>\n' +
          '        <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">\n' +
          '          <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>\n' +
          '          <p className="eyebrow mb-2">Estimated Range</p>\n' +
          '          <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>\n' +
          '        </div>\n' +
          '        <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>\n' +
          '      </div>\n' +
          '    , 200, true);\n' +
          '\n' +
          '    setStep(7);\n' +
          '  };\n' +
          '\n' +
          '  const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {\n' +
          '    e.preventDefault();\n' +
          '    const payload = {\n' +
          "      source: 'Chatbot Estimate',\n" +
          "      page: '/estimate',\n" +
          '      name, phone, email, city,\n' +
          '      projectType, prepLevel,\n' +
          '      notes: `Project: ${projectType} \\nPrep: ${prepLevel}`\n' +
          '    };\n' +
          '\n' +
          "    setStatus('submitting');\n" +
          '    try {\n' +
          "      const response = await fetch('/api/leads', {\n" +
          "        method: 'POST',\n" +
          "        headers: { 'Content-Type': 'application/json' },\n" +
          '        body: JSON.stringify(payload),\n' +
          '      });\n' +
          '      if (response.ok) {\n' +
          "        setStatus('sent');\n" +
          '        addUserMessage("Submitted my info.");\n' +
          `        addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);\n` +
          '      } else {\n' +
          "        setStatus('fallback');\n" +
          '      }\n' +
          '    } catch {\n' +
          "      setStatus('fallback');\n" +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const containerVariants = {\n' +
          '    hidden: { opacity: 0 },\n' +
          '    show: {\n' +
          '      opacity: 1,\n' +
          '      transition: { staggerChildren: 0.1 }\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const itemVariants = {\n' +
          '    hidden: { opacity: 0, y: 15, scale: 0.95 },\n' +
          '    show: { opacity: 1, y: 0, scale: 1, transition: springConfig }\n' +
          '  };\n' +
          '\n' +
          '  return (\n' +
          '    <PageTransition>\n' +
          '      <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">\n' +
          '        \n' +
          '        <div className="noise-overlay"></div>\n' +
          '\n' +
          '        <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">\n' +
          '          \n' +
          '          {/* Header */}\n' +
          '          <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">\n' +
          '            <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">\n' +
          '               <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />\n' +
          '            </div>\n' +
          '            <div>\n' +
          '              <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>\n' +
          '              <p className="eyebrow flex items-center gap-2 mt-1">\n' +
          '                <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online\n' +
          '              </p>\n' +
          '            </div>\n' +
          '          </div>\n' +
          '\n' +
          '          {/* Chat Area */}\n' +
          '          <div \n' +
          '            ref={scrollRef}\n' +
          '            className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"\n' +
          '          >\n' +
          '            <AnimatePresence initial={false}>\n' +
          '              {messages.map((msg) => (\n' +
          '                <motion.div\n' +
          '                  key={msg.id}\n' +
          '                  initial={{ opacity: 0, y: 20, scale: 0.95 }}\n' +
          '                  animate={{ opacity: 1, y: 0, scale: 1 }}\n' +
          '                  transition={springConfig}\n' +
          "                  className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}\n" +
          '                >\n' +
          "                  <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>\n" +
          '                    \n' +
          "                    <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>\n" +
          "                      {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}\n" +
          '                    </div>\n' +
          '\n' +
          '                    <div className={cn(\n' +
          '                      "p-5 text-sm l'... 10894 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import { useState, FormEvent, useEffect, useRef } from 'react';
        import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';
        import PageTransition from '../components/PageTransition';
        import RangeSlider from '../components/RangeSlider';
        import { trackEvent } from '../lib/analytics';
        import { buildEstimateMailto } from '../lib/contact';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '../lib/utils';
        
        type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;
        type PrepLevel = 'standard' | 'premium' | null;
        
        interface ChatMessage {
          id: string;
          sender: 'bot' | 'user';
          text: React.ReactNode;
          isComponent?: boolean;
        }
        
        const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };
        
        export default function EstimatePage() {
          const [messages, setMessages] = useState<ChatMessage[]>([]);
          const [isTyping, setIsTyping] = useState(false);
          const scrollRef = useRef<HTMLDivElement>(null);
        
          // State
          const [step, setStep] = useState(0);
          const [projectType, setProjectType] = useState<ProjectType>(null);
          
          // Interior State
          const [roomType, setRoomType] = useState('Bedroom');
          const [width, setWidth] = useState(12);
          const [length, setLength] = useState(14);
          const [height, setHeight] = useState(8);
        
          // Exterior State
          const [stories, setStories] = useState('1 Story');
          const [siding, setSiding] = useState('Wood / LP SmartSide');
        
          // Cabinet State
          const [cabinetCount, setCabinetCount] = useState(20);
        
          // Common State
          const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);
        
          // Lead State
          const [name, setName] = useState('');
          const [phone, setPhone] = useState('');
          const [email, setEmail] = useState('');
          const [city, setCity] = useState('');
          const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');
          const [submitMessage, setSubmitMessage] = useState('');
        
          const scrollToBottom = () => {
            if (scrollRef.current) {
              scrollRef.current.scrollTo({
                top: scrollRef.current.scrollHeight,
                behavior: 'smooth'
              });
            }
          };
        
          useEffect(() => {
            scrollToBottom();
          }, [messages, isTyping, step]);
        
          const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {
            setIsTyping(true);
            setTimeout(() => {
              setIsTyping(false);
              setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);
            }, delay);
          };
        
          const addUserMessage = (text: string) => {
            setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);
          };
        
          // Initialize
          useEffect(() => {
            if (step === 0) {
              addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);
              setTimeout(() => setStep(1), 1200);
            }
          }, []);
        
          const handleProjectType = (type: ProjectType, label: string) => {
            addUserMessage(label);
            setProjectType(type);
            
            if (type === 'interior') {
              addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");
              setStep(2);
            } else if (type === 'exterior') {
              addBotMessage("Exterior painting. How many stories is the home?");
              setStep(3);
            } else if (type === 'cabinets') {
              addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");
              setStep(4);
            }
          };
        
          const handleInteriorRoom = (type: string) => {
            addUserMessage(type);
            setRoomType(type);
            addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");
            setStep(2.1);
          };
        
          const submitInteriorDimensions = () => {
            addUserMessage(`${width}x${length}x${height} ft`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handleExteriorStories = (val: string) => {
            addUserMessage(val);
            setStories(val);
            addBotMessage("What type of siding do you have?");
            setStep(3.1);
          };
        
          const handleExteriorSiding = (val: string) => {
            addUserMessage(val);
            setSiding(val);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const submitCabinets = () => {
            addUserMessage(`${cabinetCount} doors/drawers`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handlePrep = (prep: PrepLevel, label: string) => {
            addUserMessage(label);
            setPrepLevel(prep);
            addBotMessage("Calculating your estimate...", 400);
            setTimeout(() => {
              calculateFinal();
            }, 1500);
            setStep(6);
          };
        
          const calculateFinal = () => {
            let low = 0;
            let high = 0;
        
            if (projectType === 'interior') {
              const wallArea = 2 * (width + length) * height;
              const base = wallArea * 3.50;
              const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;
              const total = base + prepVal + 250;
              low = total * 0.90;
              high = total * 1.15;
            } else if (projectType === 'exterior') {
              let base = 3500;
              if (stories === '2 Story') base = 5500;
              if (stories === '3+ Story') base = 8500;
              
              let mult = 1.0;
              if (siding === 'Stucco') mult = 1.3;
              if (siding === 'Brick / Masonry') mult = 1.4;
        
              const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);
              low = total * 0.85;
              high = total * 1.20;
            } else if (projectType === 'cabinets') {
              const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);
              low = total * 0.90;
              high = total * 1.15;
            }
        
            low = Math.round(low / 100) * 100;
            high = Math.round(high / 100) * 100;
        
            addBotMessage(
              <div className="space-y-4 reveal-up">
                <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>
                <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>
                  <p className="eyebrow mb-2">Estimated Range</p>
                  <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>
                </div>
                <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>
              </div>
            , 200, true);
        
            setStep(7);
          };
        
          const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {
            e.preventDefault();
            const payload = {
              source: 'Chatbot Estimate',
              page: '/estimate',
              name, phone, email, city,
              projectType, prepLevel,
              notes: `Project: ${projectType} \nPrep: ${prepLevel}`
            };
        
            setStatus('submitting');
            try {
              const response = await fetch('/api/leads', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload),
              });
              if (response.ok) {
                setStatus('sent');
                addUserMessage("Submitted my info.");
                addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);
              } else {
                setStatus('fallback');
              }
            } catch {
              setStatus('fallback');
            }
          };
        
          const containerVariants = {
            hidden: { opacity: 0 },
            show: {
              opacity: 1,
              transition: { staggerChildren: 0.1 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, y: 15, scale: 0.95 },
            show: { opacity: 1, y: 0, scale: 1, transition: springConfig }
          };
        
          return (
            <PageTransition>
              <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">
                
                <div className="noise-overlay"></div>
        
                <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">
                  
                  {/* Header */}
                  <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">
                    <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">
                       <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />
                    </div>
                    <div>
                      <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>
                      <p className="eyebrow flex items-center gap-2 mt-1">
                        <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online
                      </p>
                    </div>
                  </div>
        
                  {/* Chat Area */}
                  <div 
                    ref={scrollRef}
                    className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"
                  >
                    <AnimatePresence initial={false}>
                      {messages.map((msg) => (
                        <motion.div
                          key={msg.id}
                          initial={{ opacity: 0, y: 20, scale: 0.95 }}
                          animate={{ opacity: 1, y: 0, scale: 1 }}
                          transition={springConfig}
                          className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}
                        >
                          <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>
                            
                            <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>
                              {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}
                            </div>
        
                            <div className={cn(
                              "p-5 text-sm leading-relaxed",
                              msg.sender === 'user' 
                                ? "bg-[#FF5A00] text-white" 
                                : msg.isComponent 
                                  ? "bg-transparent p-0 w-full"
                                  : "bg-[#11100d] border border-white/10 text-[#c9c1b4]"
                            )}>
                              {msg.text}
                            </div>
                          </div>
                        </motion.div>
                      ))}
        
                      {isTyping && (
                        <motion.div
                          initial={{ opacity: 0, y: 10 }}
                          animate={{ opacity: 1, y: 0 }}
                          exit={{ opacity: 0, scale: 0.95 }}
                          transition={{ duration: 0.2 }}
                          className="flex justify-start"
                        >
                          <div className="flex gap-4">
                            <div className="shrink-0 w-10 h-10 flex items-center justify-center bg-[#182023] text-white border border-white/10">
                              <Bot size={18} strokeWidth={1.5} />
                            </div>
                            <div className="bg-[#11100d] border border-white/10 p-5 flex items-center gap-2 h-[60px]">
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.3s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.15s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce"></div>
                            </div>
                          </div>
                        </motion.div>
                      )}
                    </AnimatePresence>
                  </div>
        
                  {/* Controls Footer */}
                  <div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-[#050505] via-[#050505]/95 to-transparent pt-12 pb-6 px-4 sm:px-8 border-t border-white/5 z-20">
                     <AnimatePresence mode="wait">
                       {step === 1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col sm:flex-row gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('interior', 'Interior Rooms')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Interior Rooms</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('exterior', 'Exterior Painting')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Exterior Painting</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('cabinets', 'Cabinet Refinishing')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Cabinet Refinishing</motion.button>
                         </motion.div>
                       )}
        
                       {step === 2 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Bedroom', 'Living Room', 'Kitchen', 'Bathroom', 'Hallway', 'Other'].map(r => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={r} onClick={() => handleInteriorRoom(r)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">{r}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 2.1 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="w" label="Width" value={width} min={5} max={40} suffix="FT" onChange={setWidth} />
                            <RangeSlider id="l" label="Length" value={length} min={5} max={40} suffix="FT" onChange={setLength} />
                            <RangeSlider id="h" label="Ceiling Height" value={height} min={7} max={20} suffix="FT" onChange={setHeight} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitInteriorDimensions} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Dimensions</motion.button>
                         </motion.div>
                       )}
        
                       {step === 3 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           {['1 Story', '2 Story', '3+ Story'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorStories(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-left">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 3.1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Wood / LP SmartSide', 'Stucco', 'Vinyl / Aluminum', 'Brick / Masonry'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorSiding(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-center flex items-center justify-center h-full">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 4 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="c" label="Total Doors & Drawers" value={cabinetCount} min={5} max={60} onChange={setCabinetCount} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitCabinets} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Count</motion.button>
                         </motion.div>
                       )}
        
                       {step === 5 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('standard', 'Standard Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-white">Standard Prep</p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Light αª╕αªéαªòαªƒ, minor caulk, 1 coat primer & topcoat. Great for minor refreshes.</p>
                           </motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('premium', 'Premium Detail Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-[#FF5A00] flex items-center gap-2">Premium Detail Prep <span className="bg-[#FF5A00]/20 text-[#FF5A00] text-[10px] uppercase px-2 py-0.5 tracking-wider font-bold">Recommended</span></p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Elite multi-stage sanding, deep caulking, wood stabilization, premium coats.</p>
                           </motion.button>
                         </motion.div>
                       )}
        
                       {step === 7 && !isTyping && status !== 'sent' && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={springConfig} className="bg-[#11100d] border border-[#FF5A00]/30 p-6 shadow-2xl relative overflow-hidden">
                           <div className="absolute top-0 left-0 right-0 h-1 bg-[#FF5A00]"></div>
                           <form onSubmit={handleFinalSubmit} className="space-y-4">
                             <p className="eyebrow mb-4">Request Walkthrough</p>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="text" placeholder="Name" value={name} onChange={e => setName(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="tel" placeholder="Phone" value={phone} onChange={e => setPhone(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="email" placeholder="Email" value={email} onChange={e => setEmail(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="text" placeholder="City" value={city} onChange={e => setCity(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} disabled={status === 'submitting'} type="submit" className="w-full bg-[#FF5A00] text-white p-4 text-sm font-bold flex items-center justify-center gap-2 hover:bg-[#ff7a33] transition-colors disabled:opacity-50 mt-2 shimmer-cta">
                               {status === 'submitting' ? <Loader2 className="animate-spin" size={16} /> : 'Lock In Estimate'}
                             </motion.button>
                           </form>
                         </motion.div>
                       )}
                     </AnimatePresence>
                  </div>
        
                </div>
                <div className="text-center text-xs text-gray-600 mt-2 pb-4">
                  reg: ir816596 | painting
                </div>
              </section>
            </PageTransition>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:433:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: T2.24 Room Calculator - Cabinet doors count clamps strictly to 0 (min) and 60 (max)
    not ok 24 - T2.24 Room Calculator - Cabinet doors count clamps strictly to 0 (min) and 60 (max)
      ---
      duration_ms: 1.2729
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:436:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /0, 60/. Input:
        
        "'use client';\n" +
          '\n' +
          "import { useState, FormEvent, useEffect, useRef } from 'react';\n" +
          "import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';\n" +
          "import PageTransition from '../components/PageTransition';\n" +
          "import RangeSlider from '../components/RangeSlider';\n" +
          "import { trackEvent } from '../lib/analytics';\n" +
          "import { buildEstimateMailto } from '../lib/contact';\n" +
          "import { motion, AnimatePresence } from 'motion/react';\n" +
          "import { cn } from '../lib/utils';\n" +
          '\n' +
          "type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;\n" +
          "type PrepLevel = 'standard' | 'premium' | null;\n" +
          '\n' +
          'interface ChatMessage {\n' +
          '  id: string;\n' +
          "  sender: 'bot' | 'user';\n" +
          '  text: React.ReactNode;\n' +
          '  isComponent?: boolean;\n' +
          '}\n' +
          '\n' +
          'const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };\n' +
          '\n' +
          'export default function EstimatePage() {\n' +
          '  const [messages, setMessages] = useState<ChatMessage[]>([]);\n' +
          '  const [isTyping, setIsTyping] = useState(false);\n' +
          '  const scrollRef = useRef<HTMLDivElement>(null);\n' +
          '\n' +
          '  // State\n' +
          '  const [step, setStep] = useState(0);\n' +
          '  const [projectType, setProjectType] = useState<ProjectType>(null);\n' +
          '  \n' +
          '  // Interior State\n' +
          "  const [roomType, setRoomType] = useState('Bedroom');\n" +
          '  const [width, setWidth] = useState(12);\n' +
          '  const [length, setLength] = useState(14);\n' +
          '  const [height, setHeight] = useState(8);\n' +
          '\n' +
          '  // Exterior State\n' +
          "  const [stories, setStories] = useState('1 Story');\n" +
          "  const [siding, setSiding] = useState('Wood / LP SmartSide');\n" +
          '\n' +
          '  // Cabinet State\n' +
          '  const [cabinetCount, setCabinetCount] = useState(20);\n' +
          '\n' +
          '  // Common State\n' +
          '  const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);\n' +
          '\n' +
          '  // Lead State\n' +
          "  const [name, setName] = useState('');\n" +
          "  const [phone, setPhone] = useState('');\n" +
          "  const [email, setEmail] = useState('');\n" +
          "  const [city, setCity] = useState('');\n" +
          "  const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');\n" +
          "  const [submitMessage, setSubmitMessage] = useState('');\n" +
          '\n' +
          '  const scrollToBottom = () => {\n' +
          '    if (scrollRef.current) {\n' +
          '      scrollRef.current.scrollTo({\n' +
          '        top: scrollRef.current.scrollHeight,\n' +
          "        behavior: 'smooth'\n" +
          '      });\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  useEffect(() => {\n' +
          '    scrollToBottom();\n' +
          '  }, [messages, isTyping, step]);\n' +
          '\n' +
          '  const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {\n' +
          '    setIsTyping(true);\n' +
          '    setTimeout(() => {\n' +
          '      setIsTyping(false);\n' +
          "      setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);\n" +
          '    }, delay);\n' +
          '  };\n' +
          '\n' +
          '  const addUserMessage = (text: string) => {\n' +
          "    setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);\n" +
          '  };\n' +
          '\n' +
          '  // Initialize\n' +
          '  useEffect(() => {\n' +
          '    if (step === 0) {\n' +
          `      addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);\n` +
          '      setTimeout(() => setStep(1), 1200);\n' +
          '    }\n' +
          '  }, []);\n' +
          '\n' +
          '  const handleProjectType = (type: ProjectType, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setProjectType(type);\n' +
          '    \n' +
          "    if (type === 'interior') {\n" +
          '      addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");\n' +
          '      setStep(2);\n' +
          "    } else if (type === 'exterior') {\n" +
          '      addBotMessage("Exterior painting. How many stories is the home?");\n' +
          '      setStep(3);\n' +
          "    } else if (type === 'cabinets') {\n" +
          '      addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");\n' +
          '      setStep(4);\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const handleInteriorRoom = (type: string) => {\n' +
          '    addUserMessage(type);\n' +
          '    setRoomType(type);\n' +
          '    addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");\n' +
          '    setStep(2.1);\n' +
          '  };\n' +
          '\n' +
          '  const submitInteriorDimensions = () => {\n' +
          '    addUserMessage(`${width}x${length}x${height} ft`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorStories = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setStories(val);\n' +
          '    addBotMessage("What type of siding do you have?");\n' +
          '    setStep(3.1);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorSiding = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setSiding(val);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const submitCabinets = () => {\n' +
          '    addUserMessage(`${cabinetCount} doors/drawers`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handlePrep = (prep: PrepLevel, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setPrepLevel(prep);\n' +
          '    addBotMessage("Calculating your estimate...", 400);\n' +
          '    setTimeout(() => {\n' +
          '      calculateFinal();\n' +
          '    }, 1500);\n' +
          '    setStep(6);\n' +
          '  };\n' +
          '\n' +
          '  const calculateFinal = () => {\n' +
          '    let low = 0;\n' +
          '    let high = 0;\n' +
          '\n' +
          "    if (projectType === 'interior') {\n" +
          '      const wallArea = 2 * (width + length) * height;\n' +
          '      const base = wallArea * 3.50;\n' +
          "      const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;\n" +
          '      const total = base + prepVal + 250;\n' +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          "    } else if (projectType === 'exterior') {\n" +
          '      let base = 3500;\n' +
          "      if (stories === '2 Story') base = 5500;\n" +
          "      if (stories === '3+ Story') base = 8500;\n" +
          '      \n' +
          '      let mult = 1.0;\n' +
          "      if (siding === 'Stucco') mult = 1.3;\n" +
          "      if (siding === 'Brick / Masonry') mult = 1.4;\n" +
          '\n' +
          "      const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);\n" +
          '      low = total * 0.85;\n' +
          '      high = total * 1.20;\n' +
          "    } else if (projectType === 'cabinets') {\n" +
          "      const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);\n" +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          '    }\n' +
          '\n' +
          '    low = Math.round(low / 100) * 100;\n' +
          '    high = Math.round(high / 100) * 100;\n' +
          '\n' +
          '    addBotMessage(\n' +
          '      <div className="space-y-4 reveal-up">\n' +
          '        <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>\n' +
          '        <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">\n' +
          '          <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>\n' +
          '          <p className="eyebrow mb-2">Estimated Range</p>\n' +
          '          <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>\n' +
          '        </div>\n' +
          '        <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>\n' +
          '      </div>\n' +
          '    , 200, true);\n' +
          '\n' +
          '    setStep(7);\n' +
          '  };\n' +
          '\n' +
          '  const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {\n' +
          '    e.preventDefault();\n' +
          '    const payload = {\n' +
          "      source: 'Chatbot Estimate',\n" +
          "      page: '/estimate',\n" +
          '      name, phone, email, city,\n' +
          '      projectType, prepLevel,\n' +
          '      notes: `Project: ${projectType} \\nPrep: ${prepLevel}`\n' +
          '    };\n' +
          '\n' +
          "    setStatus('submitting');\n" +
          '    try {\n' +
          "      const response = await fetch('/api/leads', {\n" +
          "        method: 'POST',\n" +
          "        headers: { 'Content-Type': 'application/json' },\n" +
          '        body: JSON.stringify(payload),\n' +
          '      });\n' +
          '      if (response.ok) {\n' +
          "        setStatus('sent');\n" +
          '        addUserMessage("Submitted my info.");\n' +
          `        addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);\n` +
          '      } else {\n' +
          "        setStatus('fallback');\n" +
          '      }\n' +
          '    } catch {\n' +
          "      setStatus('fallback');\n" +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const containerVariants = {\n' +
          '    hidden: { opacity: 0 },\n' +
          '    show: {\n' +
          '      opacity: 1,\n' +
          '      transition: { staggerChildren: 0.1 }\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const itemVariants = {\n' +
          '    hidden: { opacity: 0, y: 15, scale: 0.95 },\n' +
          '    show: { opacity: 1, y: 0, scale: 1, transition: springConfig }\n' +
          '  };\n' +
          '\n' +
          '  return (\n' +
          '    <PageTransition>\n' +
          '      <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">\n' +
          '        \n' +
          '        <div className="noise-overlay"></div>\n' +
          '\n' +
          '        <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">\n' +
          '          \n' +
          '          {/* Header */}\n' +
          '          <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">\n' +
          '            <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">\n' +
          '               <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />\n' +
          '            </div>\n' +
          '            <div>\n' +
          '              <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>\n' +
          '              <p className="eyebrow flex items-center gap-2 mt-1">\n' +
          '                <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online\n' +
          '              </p>\n' +
          '            </div>\n' +
          '          </div>\n' +
          '\n' +
          '          {/* Chat Area */}\n' +
          '          <div \n' +
          '            ref={scrollRef}\n' +
          '            className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"\n' +
          '          >\n' +
          '            <AnimatePresence initial={false}>\n' +
          '              {messages.map((msg) => (\n' +
          '                <motion.div\n' +
          '                  key={msg.id}\n' +
          '                  initial={{ opacity: 0, y: 20, scale: 0.95 }}\n' +
          '                  animate={{ opacity: 1, y: 0, scale: 1 }}\n' +
          '                  transition={springConfig}\n' +
          "                  className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}\n" +
          '                >\n' +
          "                  <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>\n" +
          '                    \n' +
          "                    <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>\n" +
          "                      {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}\n" +
          '                    </div>\n' +
          '\n' +
          '                    <div className={cn(\n' +
          '                      "p-5 text-sm l'... 10894 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import { useState, FormEvent, useEffect, useRef } from 'react';
        import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';
        import PageTransition from '../components/PageTransition';
        import RangeSlider from '../components/RangeSlider';
        import { trackEvent } from '../lib/analytics';
        import { buildEstimateMailto } from '../lib/contact';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '../lib/utils';
        
        type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;
        type PrepLevel = 'standard' | 'premium' | null;
        
        interface ChatMessage {
          id: string;
          sender: 'bot' | 'user';
          text: React.ReactNode;
          isComponent?: boolean;
        }
        
        const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };
        
        export default function EstimatePage() {
          const [messages, setMessages] = useState<ChatMessage[]>([]);
          const [isTyping, setIsTyping] = useState(false);
          const scrollRef = useRef<HTMLDivElement>(null);
        
          // State
          const [step, setStep] = useState(0);
          const [projectType, setProjectType] = useState<ProjectType>(null);
          
          // Interior State
          const [roomType, setRoomType] = useState('Bedroom');
          const [width, setWidth] = useState(12);
          const [length, setLength] = useState(14);
          const [height, setHeight] = useState(8);
        
          // Exterior State
          const [stories, setStories] = useState('1 Story');
          const [siding, setSiding] = useState('Wood / LP SmartSide');
        
          // Cabinet State
          const [cabinetCount, setCabinetCount] = useState(20);
        
          // Common State
          const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);
        
          // Lead State
          const [name, setName] = useState('');
          const [phone, setPhone] = useState('');
          const [email, setEmail] = useState('');
          const [city, setCity] = useState('');
          const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');
          const [submitMessage, setSubmitMessage] = useState('');
        
          const scrollToBottom = () => {
            if (scrollRef.current) {
              scrollRef.current.scrollTo({
                top: scrollRef.current.scrollHeight,
                behavior: 'smooth'
              });
            }
          };
        
          useEffect(() => {
            scrollToBottom();
          }, [messages, isTyping, step]);
        
          const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {
            setIsTyping(true);
            setTimeout(() => {
              setIsTyping(false);
              setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);
            }, delay);
          };
        
          const addUserMessage = (text: string) => {
            setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);
          };
        
          // Initialize
          useEffect(() => {
            if (step === 0) {
              addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);
              setTimeout(() => setStep(1), 1200);
            }
          }, []);
        
          const handleProjectType = (type: ProjectType, label: string) => {
            addUserMessage(label);
            setProjectType(type);
            
            if (type === 'interior') {
              addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");
              setStep(2);
            } else if (type === 'exterior') {
              addBotMessage("Exterior painting. How many stories is the home?");
              setStep(3);
            } else if (type === 'cabinets') {
              addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");
              setStep(4);
            }
          };
        
          const handleInteriorRoom = (type: string) => {
            addUserMessage(type);
            setRoomType(type);
            addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");
            setStep(2.1);
          };
        
          const submitInteriorDimensions = () => {
            addUserMessage(`${width}x${length}x${height} ft`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handleExteriorStories = (val: string) => {
            addUserMessage(val);
            setStories(val);
            addBotMessage("What type of siding do you have?");
            setStep(3.1);
          };
        
          const handleExteriorSiding = (val: string) => {
            addUserMessage(val);
            setSiding(val);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const submitCabinets = () => {
            addUserMessage(`${cabinetCount} doors/drawers`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handlePrep = (prep: PrepLevel, label: string) => {
            addUserMessage(label);
            setPrepLevel(prep);
            addBotMessage("Calculating your estimate...", 400);
            setTimeout(() => {
              calculateFinal();
            }, 1500);
            setStep(6);
          };
        
          const calculateFinal = () => {
            let low = 0;
            let high = 0;
        
            if (projectType === 'interior') {
              const wallArea = 2 * (width + length) * height;
              const base = wallArea * 3.50;
              const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;
              const total = base + prepVal + 250;
              low = total * 0.90;
              high = total * 1.15;
            } else if (projectType === 'exterior') {
              let base = 3500;
              if (stories === '2 Story') base = 5500;
              if (stories === '3+ Story') base = 8500;
              
              let mult = 1.0;
              if (siding === 'Stucco') mult = 1.3;
              if (siding === 'Brick / Masonry') mult = 1.4;
        
              const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);
              low = total * 0.85;
              high = total * 1.20;
            } else if (projectType === 'cabinets') {
              const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);
              low = total * 0.90;
              high = total * 1.15;
            }
        
            low = Math.round(low / 100) * 100;
            high = Math.round(high / 100) * 100;
        
            addBotMessage(
              <div className="space-y-4 reveal-up">
                <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>
                <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>
                  <p className="eyebrow mb-2">Estimated Range</p>
                  <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>
                </div>
                <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>
              </div>
            , 200, true);
        
            setStep(7);
          };
        
          const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {
            e.preventDefault();
            const payload = {
              source: 'Chatbot Estimate',
              page: '/estimate',
              name, phone, email, city,
              projectType, prepLevel,
              notes: `Project: ${projectType} \nPrep: ${prepLevel}`
            };
        
            setStatus('submitting');
            try {
              const response = await fetch('/api/leads', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload),
              });
              if (response.ok) {
                setStatus('sent');
                addUserMessage("Submitted my info.");
                addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);
              } else {
                setStatus('fallback');
              }
            } catch {
              setStatus('fallback');
            }
          };
        
          const containerVariants = {
            hidden: { opacity: 0 },
            show: {
              opacity: 1,
              transition: { staggerChildren: 0.1 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, y: 15, scale: 0.95 },
            show: { opacity: 1, y: 0, scale: 1, transition: springConfig }
          };
        
          return (
            <PageTransition>
              <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">
                
                <div className="noise-overlay"></div>
        
                <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">
                  
                  {/* Header */}
                  <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">
                    <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">
                       <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />
                    </div>
                    <div>
                      <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>
                      <p className="eyebrow flex items-center gap-2 mt-1">
                        <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online
                      </p>
                    </div>
                  </div>
        
                  {/* Chat Area */}
                  <div 
                    ref={scrollRef}
                    className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"
                  >
                    <AnimatePresence initial={false}>
                      {messages.map((msg) => (
                        <motion.div
                          key={msg.id}
                          initial={{ opacity: 0, y: 20, scale: 0.95 }}
                          animate={{ opacity: 1, y: 0, scale: 1 }}
                          transition={springConfig}
                          className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}
                        >
                          <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>
                            
                            <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>
                              {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}
                            </div>
        
                            <div className={cn(
                              "p-5 text-sm leading-relaxed",
                              msg.sender === 'user' 
                                ? "bg-[#FF5A00] text-white" 
                                : msg.isComponent 
                                  ? "bg-transparent p-0 w-full"
                                  : "bg-[#11100d] border border-white/10 text-[#c9c1b4]"
                            )}>
                              {msg.text}
                            </div>
                          </div>
                        </motion.div>
                      ))}
        
                      {isTyping && (
                        <motion.div
                          initial={{ opacity: 0, y: 10 }}
                          animate={{ opacity: 1, y: 0 }}
                          exit={{ opacity: 0, scale: 0.95 }}
                          transition={{ duration: 0.2 }}
                          className="flex justify-start"
                        >
                          <div className="flex gap-4">
                            <div className="shrink-0 w-10 h-10 flex items-center justify-center bg-[#182023] text-white border border-white/10">
                              <Bot size={18} strokeWidth={1.5} />
                            </div>
                            <div className="bg-[#11100d] border border-white/10 p-5 flex items-center gap-2 h-[60px]">
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.3s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.15s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce"></div>
                            </div>
                          </div>
                        </motion.div>
                      )}
                    </AnimatePresence>
                  </div>
        
                  {/* Controls Footer */}
                  <div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-[#050505] via-[#050505]/95 to-transparent pt-12 pb-6 px-4 sm:px-8 border-t border-white/5 z-20">
                     <AnimatePresence mode="wait">
                       {step === 1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col sm:flex-row gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('interior', 'Interior Rooms')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Interior Rooms</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('exterior', 'Exterior Painting')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Exterior Painting</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('cabinets', 'Cabinet Refinishing')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Cabinet Refinishing</motion.button>
                         </motion.div>
                       )}
        
                       {step === 2 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Bedroom', 'Living Room', 'Kitchen', 'Bathroom', 'Hallway', 'Other'].map(r => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={r} onClick={() => handleInteriorRoom(r)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">{r}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 2.1 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="w" label="Width" value={width} min={5} max={40} suffix="FT" onChange={setWidth} />
                            <RangeSlider id="l" label="Length" value={length} min={5} max={40} suffix="FT" onChange={setLength} />
                            <RangeSlider id="h" label="Ceiling Height" value={height} min={7} max={20} suffix="FT" onChange={setHeight} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitInteriorDimensions} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Dimensions</motion.button>
                         </motion.div>
                       )}
        
                       {step === 3 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           {['1 Story', '2 Story', '3+ Story'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorStories(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-left">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 3.1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Wood / LP SmartSide', 'Stucco', 'Vinyl / Aluminum', 'Brick / Masonry'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorSiding(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-center flex items-center justify-center h-full">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 4 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="c" label="Total Doors & Drawers" value={cabinetCount} min={5} max={60} onChange={setCabinetCount} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitCabinets} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Count</motion.button>
                         </motion.div>
                       )}
        
                       {step === 5 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('standard', 'Standard Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-white">Standard Prep</p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Light αª╕αªéαªòαªƒ, minor caulk, 1 coat primer & topcoat. Great for minor refreshes.</p>
                           </motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('premium', 'Premium Detail Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-[#FF5A00] flex items-center gap-2">Premium Detail Prep <span className="bg-[#FF5A00]/20 text-[#FF5A00] text-[10px] uppercase px-2 py-0.5 tracking-wider font-bold">Recommended</span></p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Elite multi-stage sanding, deep caulking, wood stabilization, premium coats.</p>
                           </motion.button>
                         </motion.div>
                       )}
        
                       {step === 7 && !isTyping && status !== 'sent' && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={springConfig} className="bg-[#11100d] border border-[#FF5A00]/30 p-6 shadow-2xl relative overflow-hidden">
                           <div className="absolute top-0 left-0 right-0 h-1 bg-[#FF5A00]"></div>
                           <form onSubmit={handleFinalSubmit} className="space-y-4">
                             <p className="eyebrow mb-4">Request Walkthrough</p>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="text" placeholder="Name" value={name} onChange={e => setName(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="tel" placeholder="Phone" value={phone} onChange={e => setPhone(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="email" placeholder="Email" value={email} onChange={e => setEmail(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="text" placeholder="City" value={city} onChange={e => setCity(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} disabled={status === 'submitting'} type="submit" className="w-full bg-[#FF5A00] text-white p-4 text-sm font-bold flex items-center justify-center gap-2 hover:bg-[#ff7a33] transition-colors disabled:opacity-50 mt-2 shimmer-cta">
                               {status === 'submitting' ? <Loader2 className="animate-spin" size={16} /> : 'Lock In Estimate'}
                             </motion.button>
                           </form>
                         </motion.div>
                       )}
                     </AnimatePresence>
                  </div>
        
                </div>
                <div className="text-center text-xs text-gray-600 mt-2 pb-4">
                  reg: ir816596 | painting
                </div>
              </section>
            </PageTransition>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:438:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: T2.25 Room Calculator - State is persisted in localStorage to prevent loss
    not ok 25 - T2.25 Room Calculator - State is persisted in localStorage to prevent loss
      ---
      duration_ms: 3.5337
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:441:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /sky_estimate_progress/. Input:
        
        "'use client';\n" +
          '\n' +
          "import { useState, FormEvent, useEffect, useRef } from 'react';\n" +
          "import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';\n" +
          "import PageTransition from '../components/PageTransition';\n" +
          "import RangeSlider from '../components/RangeSlider';\n" +
          "import { trackEvent } from '../lib/analytics';\n" +
          "import { buildEstimateMailto } from '../lib/contact';\n" +
          "import { motion, AnimatePresence } from 'motion/react';\n" +
          "import { cn } from '../lib/utils';\n" +
          '\n' +
          "type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;\n" +
          "type PrepLevel = 'standard' | 'premium' | null;\n" +
          '\n' +
          'interface ChatMessage {\n' +
          '  id: string;\n' +
          "  sender: 'bot' | 'user';\n" +
          '  text: React.ReactNode;\n' +
          '  isComponent?: boolean;\n' +
          '}\n' +
          '\n' +
          'const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };\n' +
          '\n' +
          'export default function EstimatePage() {\n' +
          '  const [messages, setMessages] = useState<ChatMessage[]>([]);\n' +
          '  const [isTyping, setIsTyping] = useState(false);\n' +
          '  const scrollRef = useRef<HTMLDivElement>(null);\n' +
          '\n' +
          '  // State\n' +
          '  const [step, setStep] = useState(0);\n' +
          '  const [projectType, setProjectType] = useState<ProjectType>(null);\n' +
          '  \n' +
          '  // Interior State\n' +
          "  const [roomType, setRoomType] = useState('Bedroom');\n" +
          '  const [width, setWidth] = useState(12);\n' +
          '  const [length, setLength] = useState(14);\n' +
          '  const [height, setHeight] = useState(8);\n' +
          '\n' +
          '  // Exterior State\n' +
          "  const [stories, setStories] = useState('1 Story');\n" +
          "  const [siding, setSiding] = useState('Wood / LP SmartSide');\n" +
          '\n' +
          '  // Cabinet State\n' +
          '  const [cabinetCount, setCabinetCount] = useState(20);\n' +
          '\n' +
          '  // Common State\n' +
          '  const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);\n' +
          '\n' +
          '  // Lead State\n' +
          "  const [name, setName] = useState('');\n" +
          "  const [phone, setPhone] = useState('');\n" +
          "  const [email, setEmail] = useState('');\n" +
          "  const [city, setCity] = useState('');\n" +
          "  const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');\n" +
          "  const [submitMessage, setSubmitMessage] = useState('');\n" +
          '\n' +
          '  const scrollToBottom = () => {\n' +
          '    if (scrollRef.current) {\n' +
          '      scrollRef.current.scrollTo({\n' +
          '        top: scrollRef.current.scrollHeight,\n' +
          "        behavior: 'smooth'\n" +
          '      });\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  useEffect(() => {\n' +
          '    scrollToBottom();\n' +
          '  }, [messages, isTyping, step]);\n' +
          '\n' +
          '  const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {\n' +
          '    setIsTyping(true);\n' +
          '    setTimeout(() => {\n' +
          '      setIsTyping(false);\n' +
          "      setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);\n" +
          '    }, delay);\n' +
          '  };\n' +
          '\n' +
          '  const addUserMessage = (text: string) => {\n' +
          "    setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);\n" +
          '  };\n' +
          '\n' +
          '  // Initialize\n' +
          '  useEffect(() => {\n' +
          '    if (step === 0) {\n' +
          `      addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);\n` +
          '      setTimeout(() => setStep(1), 1200);\n' +
          '    }\n' +
          '  }, []);\n' +
          '\n' +
          '  const handleProjectType = (type: ProjectType, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setProjectType(type);\n' +
          '    \n' +
          "    if (type === 'interior') {\n" +
          '      addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");\n' +
          '      setStep(2);\n' +
          "    } else if (type === 'exterior') {\n" +
          '      addBotMessage("Exterior painting. How many stories is the home?");\n' +
          '      setStep(3);\n' +
          "    } else if (type === 'cabinets') {\n" +
          '      addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");\n' +
          '      setStep(4);\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const handleInteriorRoom = (type: string) => {\n' +
          '    addUserMessage(type);\n' +
          '    setRoomType(type);\n' +
          '    addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");\n' +
          '    setStep(2.1);\n' +
          '  };\n' +
          '\n' +
          '  const submitInteriorDimensions = () => {\n' +
          '    addUserMessage(`${width}x${length}x${height} ft`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorStories = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setStories(val);\n' +
          '    addBotMessage("What type of siding do you have?");\n' +
          '    setStep(3.1);\n' +
          '  };\n' +
          '\n' +
          '  const handleExteriorSiding = (val: string) => {\n' +
          '    addUserMessage(val);\n' +
          '    setSiding(val);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const submitCabinets = () => {\n' +
          '    addUserMessage(`${cabinetCount} doors/drawers`);\n' +
          '    addBotMessage("What level of prep and finish are you looking for?");\n' +
          '    setStep(5);\n' +
          '  };\n' +
          '\n' +
          '  const handlePrep = (prep: PrepLevel, label: string) => {\n' +
          '    addUserMessage(label);\n' +
          '    setPrepLevel(prep);\n' +
          '    addBotMessage("Calculating your estimate...", 400);\n' +
          '    setTimeout(() => {\n' +
          '      calculateFinal();\n' +
          '    }, 1500);\n' +
          '    setStep(6);\n' +
          '  };\n' +
          '\n' +
          '  const calculateFinal = () => {\n' +
          '    let low = 0;\n' +
          '    let high = 0;\n' +
          '\n' +
          "    if (projectType === 'interior') {\n" +
          '      const wallArea = 2 * (width + length) * height;\n' +
          '      const base = wallArea * 3.50;\n' +
          "      const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;\n" +
          '      const total = base + prepVal + 250;\n' +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          "    } else if (projectType === 'exterior') {\n" +
          '      let base = 3500;\n' +
          "      if (stories === '2 Story') base = 5500;\n" +
          "      if (stories === '3+ Story') base = 8500;\n" +
          '      \n' +
          '      let mult = 1.0;\n' +
          "      if (siding === 'Stucco') mult = 1.3;\n" +
          "      if (siding === 'Brick / Masonry') mult = 1.4;\n" +
          '\n' +
          "      const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);\n" +
          '      low = total * 0.85;\n' +
          '      high = total * 1.20;\n' +
          "    } else if (projectType === 'cabinets') {\n" +
          "      const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);\n" +
          '      low = total * 0.90;\n' +
          '      high = total * 1.15;\n' +
          '    }\n' +
          '\n' +
          '    low = Math.round(low / 100) * 100;\n' +
          '    high = Math.round(high / 100) * 100;\n' +
          '\n' +
          '    addBotMessage(\n' +
          '      <div className="space-y-4 reveal-up">\n' +
          '        <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>\n' +
          '        <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">\n' +
          '          <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>\n' +
          '          <p className="eyebrow mb-2">Estimated Range</p>\n' +
          '          <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>\n' +
          '        </div>\n' +
          '        <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>\n' +
          '      </div>\n' +
          '    , 200, true);\n' +
          '\n' +
          '    setStep(7);\n' +
          '  };\n' +
          '\n' +
          '  const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {\n' +
          '    e.preventDefault();\n' +
          '    const payload = {\n' +
          "      source: 'Chatbot Estimate',\n" +
          "      page: '/estimate',\n" +
          '      name, phone, email, city,\n' +
          '      projectType, prepLevel,\n' +
          '      notes: `Project: ${projectType} \\nPrep: ${prepLevel}`\n' +
          '    };\n' +
          '\n' +
          "    setStatus('submitting');\n" +
          '    try {\n' +
          "      const response = await fetch('/api/leads', {\n" +
          "        method: 'POST',\n" +
          "        headers: { 'Content-Type': 'application/json' },\n" +
          '        body: JSON.stringify(payload),\n' +
          '      });\n' +
          '      if (response.ok) {\n' +
          "        setStatus('sent');\n" +
          '        addUserMessage("Submitted my info.");\n' +
          `        addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);\n` +
          '      } else {\n' +
          "        setStatus('fallback');\n" +
          '      }\n' +
          '    } catch {\n' +
          "      setStatus('fallback');\n" +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const containerVariants = {\n' +
          '    hidden: { opacity: 0 },\n' +
          '    show: {\n' +
          '      opacity: 1,\n' +
          '      transition: { staggerChildren: 0.1 }\n' +
          '    }\n' +
          '  };\n' +
          '\n' +
          '  const itemVariants = {\n' +
          '    hidden: { opacity: 0, y: 15, scale: 0.95 },\n' +
          '    show: { opacity: 1, y: 0, scale: 1, transition: springConfig }\n' +
          '  };\n' +
          '\n' +
          '  return (\n' +
          '    <PageTransition>\n' +
          '      <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">\n' +
          '        \n' +
          '        <div className="noise-overlay"></div>\n' +
          '\n' +
          '        <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">\n' +
          '          \n' +
          '          {/* Header */}\n' +
          '          <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">\n' +
          '            <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">\n' +
          '               <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />\n' +
          '            </div>\n' +
          '            <div>\n' +
          '              <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>\n' +
          '              <p className="eyebrow flex items-center gap-2 mt-1">\n' +
          '                <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online\n' +
          '              </p>\n' +
          '            </div>\n' +
          '          </div>\n' +
          '\n' +
          '          {/* Chat Area */}\n' +
          '          <div \n' +
          '            ref={scrollRef}\n' +
          '            className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"\n' +
          '          >\n' +
          '            <AnimatePresence initial={false}>\n' +
          '              {messages.map((msg) => (\n' +
          '                <motion.div\n' +
          '                  key={msg.id}\n' +
          '                  initial={{ opacity: 0, y: 20, scale: 0.95 }}\n' +
          '                  animate={{ opacity: 1, y: 0, scale: 1 }}\n' +
          '                  transition={springConfig}\n' +
          "                  className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}\n" +
          '                >\n' +
          "                  <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>\n" +
          '                    \n' +
          "                    <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>\n" +
          "                      {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}\n" +
          '                    </div>\n' +
          '\n' +
          '                    <div className={cn(\n' +
          '                      "p-5 text-sm l'... 10894 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import { useState, FormEvent, useEffect, useRef } from 'react';
        import { Calculator, ArrowRight, ShieldCheck, CheckCircle2, User, Bot, Loader2 } from 'lucide-react';
        import PageTransition from '../components/PageTransition';
        import RangeSlider from '../components/RangeSlider';
        import { trackEvent } from '../lib/analytics';
        import { buildEstimateMailto } from '../lib/contact';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '../lib/utils';
        
        type ProjectType = 'interior' | 'exterior' | 'cabinets' | null;
        type PrepLevel = 'standard' | 'premium' | null;
        
        interface ChatMessage {
          id: string;
          sender: 'bot' | 'user';
          text: React.ReactNode;
          isComponent?: boolean;
        }
        
        const springConfig: any = { type: "spring", stiffness: 300, damping: 24 };
        
        export default function EstimatePage() {
          const [messages, setMessages] = useState<ChatMessage[]>([]);
          const [isTyping, setIsTyping] = useState(false);
          const scrollRef = useRef<HTMLDivElement>(null);
        
          // State
          const [step, setStep] = useState(0);
          const [projectType, setProjectType] = useState<ProjectType>(null);
          
          // Interior State
          const [roomType, setRoomType] = useState('Bedroom');
          const [width, setWidth] = useState(12);
          const [length, setLength] = useState(14);
          const [height, setHeight] = useState(8);
        
          // Exterior State
          const [stories, setStories] = useState('1 Story');
          const [siding, setSiding] = useState('Wood / LP SmartSide');
        
          // Cabinet State
          const [cabinetCount, setCabinetCount] = useState(20);
        
          // Common State
          const [prepLevel, setPrepLevel] = useState<PrepLevel>(null);
        
          // Lead State
          const [name, setName] = useState('');
          const [phone, setPhone] = useState('');
          const [email, setEmail] = useState('');
          const [city, setCity] = useState('');
          const [status, setStatus] = useState<'idle' | 'submitting' | 'sent' | 'fallback' | 'error'>('idle');
          const [submitMessage, setSubmitMessage] = useState('');
        
          const scrollToBottom = () => {
            if (scrollRef.current) {
              scrollRef.current.scrollTo({
                top: scrollRef.current.scrollHeight,
                behavior: 'smooth'
              });
            }
          };
        
          useEffect(() => {
            scrollToBottom();
          }, [messages, isTyping, step]);
        
          const addBotMessage = (text: React.ReactNode, delay = 600, isComponent = false) => {
            setIsTyping(true);
            setTimeout(() => {
              setIsTyping(false);
              setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'bot', text, isComponent }]);
            }, delay);
          };
        
          const addUserMessage = (text: string) => {
            setMessages(prev => [...prev, { id: Math.random().toString(), sender: 'user', text }]);
          };
        
          // Initialize
          useEffect(() => {
            if (step === 0) {
              addBotMessage("Hi there! I'm the Sky's the Limit pricing assistant. I can help you build a rough price range for your painting project in under a minute.", 400);
              setTimeout(() => setStep(1), 1200);
            }
          }, []);
        
          const handleProjectType = (type: ProjectType, label: string) => {
            addUserMessage(label);
            setProjectType(type);
            
            if (type === 'interior') {
              addBotMessage("Great. Interior painting it is. What kind of room are we looking at?");
              setStep(2);
            } else if (type === 'exterior') {
              addBotMessage("Exterior painting. How many stories is the home?");
              setStep(3);
            } else if (type === 'cabinets') {
              addBotMessage("Cabinet refinishing makes a huge impact. Roughly how many doors and drawers are there total?");
              setStep(4);
            }
          };
        
          const handleInteriorRoom = (type: string) => {
            addUserMessage(type);
            setRoomType(type);
            addBotMessage("Got it. Adjust the rough dimensions below so I can calculate the surface area.");
            setStep(2.1);
          };
        
          const submitInteriorDimensions = () => {
            addUserMessage(`${width}x${length}x${height} ft`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handleExteriorStories = (val: string) => {
            addUserMessage(val);
            setStories(val);
            addBotMessage("What type of siding do you have?");
            setStep(3.1);
          };
        
          const handleExteriorSiding = (val: string) => {
            addUserMessage(val);
            setSiding(val);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const submitCabinets = () => {
            addUserMessage(`${cabinetCount} doors/drawers`);
            addBotMessage("What level of prep and finish are you looking for?");
            setStep(5);
          };
        
          const handlePrep = (prep: PrepLevel, label: string) => {
            addUserMessage(label);
            setPrepLevel(prep);
            addBotMessage("Calculating your estimate...", 400);
            setTimeout(() => {
              calculateFinal();
            }, 1500);
            setStep(6);
          };
        
          const calculateFinal = () => {
            let low = 0;
            let high = 0;
        
            if (projectType === 'interior') {
              const wallArea = 2 * (width + length) * height;
              const base = wallArea * 3.50;
              const prepVal = prepLevel === 'premium' ? base * 0.35 : 0;
              const total = base + prepVal + 250;
              low = total * 0.90;
              high = total * 1.15;
            } else if (projectType === 'exterior') {
              let base = 3500;
              if (stories === '2 Story') base = 5500;
              if (stories === '3+ Story') base = 8500;
              
              let mult = 1.0;
              if (siding === 'Stucco') mult = 1.3;
              if (siding === 'Brick / Masonry') mult = 1.4;
        
              const total = base * mult * (prepLevel === 'premium' ? 1.3 : 1.0);
              low = total * 0.85;
              high = total * 1.20;
            } else if (projectType === 'cabinets') {
              const total = cabinetCount * (prepLevel === 'premium' ? 150 : 115);
              low = total * 0.90;
              high = total * 1.15;
            }
        
            low = Math.round(low / 100) * 100;
            high = Math.round(high / 100) * 100;
        
            addBotMessage(
              <div className="space-y-4 reveal-up">
                <p className="text-sm font-medium">Based on your selections, here is your rough planning range:</p>
                <div className="bg-[#11100d] border border-white/10 p-6 shadow-2xl relative overflow-hidden">
                  <div className="absolute inset-0 bg-gradient-to-br from-[#FF5A00]/5 to-transparent pointer-events-none"></div>
                  <p className="eyebrow mb-2">Estimated Range</p>
                  <p className="text-4xl font-black text-white tnum">${low.toLocaleString()} - ${high.toLocaleString()}</p>
                </div>
                <p className="text-sm">If this fits your budget, fill out the form below and our team will reach out to schedule a firm, on-site walkthrough.</p>
              </div>
            , 200, true);
        
            setStep(7);
          };
        
          const handleFinalSubmit = async (e: FormEvent<HTMLFormElement>) => {
            e.preventDefault();
            const payload = {
              source: 'Chatbot Estimate',
              page: '/estimate',
              name, phone, email, city,
              projectType, prepLevel,
              notes: `Project: ${projectType} \nPrep: ${prepLevel}`
            };
        
            setStatus('submitting');
            try {
              const response = await fetch('/api/leads', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload),
              });
              if (response.ok) {
                setStatus('sent');
                addUserMessage("Submitted my info.");
                addBotMessage("Thanks! We've received your request and will be in touch shortly to schedule a walkthrough.", 600);
              } else {
                setStatus('fallback');
              }
            } catch {
              setStatus('fallback');
            }
          };
        
          const containerVariants = {
            hidden: { opacity: 0 },
            show: {
              opacity: 1,
              transition: { staggerChildren: 0.1 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, y: 15, scale: 0.95 },
            show: { opacity: 1, y: 0, scale: 1, transition: springConfig }
          };
        
          return (
            <PageTransition>
              <section className="relative min-h-[calc(100svh-116px)] flex items-center justify-center bg-[#050505] py-12 px-4 sm:px-6 lg:px-8 overflow-hidden mesh-gradient-bg">
                
                <div className="noise-overlay"></div>
        
                <div className="relative w-full max-w-2xl flex flex-col h-[85svh] border border-white/10 bg-white/5 backdrop-blur-2xl shadow-2xl overflow-hidden perspective-1000">
                  
                  {/* Header */}
                  <div className="flex items-center gap-4 bg-[#070706]/90 backdrop-blur-md p-5 border-b border-white/10 z-10 shrink-0">
                    <div className="h-10 w-10 bg-white flex items-center justify-center shrink-0">
                       <img src="/brand/SkyLLP_BrandLogo.svg" alt="Sky" className="h-6 w-6" />
                    </div>
                    <div>
                      <h2 className="font-bold text-white text-lg tracking-tight uppercase">Pricing Engine</h2>
                      <p className="eyebrow flex items-center gap-2 mt-1">
                        <span className="w-1.5 h-1.5 bg-[#FF5A00] animate-pulse"></span> Agent Online
                      </p>
                    </div>
                  </div>
        
                  {/* Chat Area */}
                  <div 
                    ref={scrollRef}
                    className="flex-1 overflow-y-auto p-4 sm:p-8 space-y-8 scroll-smooth pb-32"
                  >
                    <AnimatePresence initial={false}>
                      {messages.map((msg) => (
                        <motion.div
                          key={msg.id}
                          initial={{ opacity: 0, y: 20, scale: 0.95 }}
                          animate={{ opacity: 1, y: 0, scale: 1 }}
                          transition={springConfig}
                          className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}
                        >
                          <div className={`max-w-[85%] flex gap-4 ${msg.sender === 'user' ? 'flex-row-reverse' : 'flex-row'}`}>
                            
                            <div className={`shrink-0 w-10 h-10 flex items-center justify-center ${msg.sender === 'user' ? 'bg-[#FF5A00]/20 text-[#FF5A00]' : 'bg-[#182023] text-white border border-white/10'}`}>
                              {msg.sender === 'user' ? <User size={18} strokeWidth={1.5} /> : <Bot size={18} strokeWidth={1.5} />}
                            </div>
        
                            <div className={cn(
                              "p-5 text-sm leading-relaxed",
                              msg.sender === 'user' 
                                ? "bg-[#FF5A00] text-white" 
                                : msg.isComponent 
                                  ? "bg-transparent p-0 w-full"
                                  : "bg-[#11100d] border border-white/10 text-[#c9c1b4]"
                            )}>
                              {msg.text}
                            </div>
                          </div>
                        </motion.div>
                      ))}
        
                      {isTyping && (
                        <motion.div
                          initial={{ opacity: 0, y: 10 }}
                          animate={{ opacity: 1, y: 0 }}
                          exit={{ opacity: 0, scale: 0.95 }}
                          transition={{ duration: 0.2 }}
                          className="flex justify-start"
                        >
                          <div className="flex gap-4">
                            <div className="shrink-0 w-10 h-10 flex items-center justify-center bg-[#182023] text-white border border-white/10">
                              <Bot size={18} strokeWidth={1.5} />
                            </div>
                            <div className="bg-[#11100d] border border-white/10 p-5 flex items-center gap-2 h-[60px]">
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.3s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce [animation-delay:-0.15s]"></div>
                              <div className="w-1.5 h-1.5 bg-white/40 animate-bounce"></div>
                            </div>
                          </div>
                        </motion.div>
                      )}
                    </AnimatePresence>
                  </div>
        
                  {/* Controls Footer */}
                  <div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-[#050505] via-[#050505]/95 to-transparent pt-12 pb-6 px-4 sm:px-8 border-t border-white/5 z-20">
                     <AnimatePresence mode="wait">
                       {step === 1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col sm:flex-row gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('interior', 'Interior Rooms')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Interior Rooms</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('exterior', 'Exterior Painting')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Exterior Painting</motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} onClick={() => handleProjectType('cabinets', 'Cabinet Refinishing')} className="flex-1 bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">Cabinet Refinishing</motion.button>
                         </motion.div>
                       )}
        
                       {step === 2 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Bedroom', 'Living Room', 'Kitchen', 'Bathroom', 'Hallway', 'Other'].map(r => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={r} onClick={() => handleInteriorRoom(r)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg">{r}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 2.1 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="w" label="Width" value={width} min={5} max={40} suffix="FT" onChange={setWidth} />
                            <RangeSlider id="l" label="Length" value={length} min={5} max={40} suffix="FT" onChange={setLength} />
                            <RangeSlider id="h" label="Ceiling Height" value={height} min={7} max={20} suffix="FT" onChange={setHeight} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitInteriorDimensions} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Dimensions</motion.button>
                         </motion.div>
                       )}
        
                       {step === 3 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           {['1 Story', '2 Story', '3+ Story'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorStories(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-left">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 3.1 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="grid grid-cols-2 gap-3">
                           {['Wood / LP SmartSide', 'Stucco', 'Vinyl / Aluminum', 'Brick / Masonry'].map(s => (
                             <motion.button variants={itemVariants} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} key={s} onClick={() => handleExteriorSiding(s)} className="bg-[#182023] hover:bg-white/10 border border-white/10 p-4 text-sm font-bold transition-colors shadow-lg text-center flex items-center justify-center h-full">{s}</motion.button>
                           ))}
                         </motion.div>
                       )}
        
                       {step === 4 && !isTyping && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 20 }} transition={springConfig} className="bg-[#11100d] border border-white/10 p-6 space-y-6 shadow-2xl">
                            <RangeSlider id="c" label="Total Doors & Drawers" value={cabinetCount} min={5} max={60} onChange={setCabinetCount} />
                            <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={submitCabinets} className="w-full bg-white text-black p-4 text-sm font-bold hover:bg-gray-200 transition-colors mt-4">Confirm Count</motion.button>
                         </motion.div>
                       )}
        
                       {step === 5 && !isTyping && (
                         <motion.div variants={containerVariants} initial="hidden" animate="show" exit="hidden" className="flex flex-col gap-3">
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('standard', 'Standard Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-white">Standard Prep</p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Light αª╕αªéαªòαªƒ, minor caulk, 1 coat primer & topcoat. Great for minor refreshes.</p>
                           </motion.button>
                           <motion.button variants={itemVariants} whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} onClick={() => handlePrep('premium', 'Premium Detail Prep')} className="text-left bg-[#182023] hover:bg-white/10 border border-white/10 p-5 transition-colors shadow-lg">
                             <p className="font-bold text-sm text-[#FF5A00] flex items-center gap-2">Premium Detail Prep <span className="bg-[#FF5A00]/20 text-[#FF5A00] text-[10px] uppercase px-2 py-0.5 tracking-wider font-bold">Recommended</span></p>
                             <p className="text-xs text-[#9ca3af] mt-2 leading-relaxed">Elite multi-stage sanding, deep caulking, wood stabilization, premium coats.</p>
                           </motion.button>
                         </motion.div>
                       )}
        
                       {step === 7 && !isTyping && status !== 'sent' && (
                         <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={springConfig} className="bg-[#11100d] border border-[#FF5A00]/30 p-6 shadow-2xl relative overflow-hidden">
                           <div className="absolute top-0 left-0 right-0 h-1 bg-[#FF5A00]"></div>
                           <form onSubmit={handleFinalSubmit} className="space-y-4">
                             <p className="eyebrow mb-4">Request Walkthrough</p>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="text" placeholder="Name" value={name} onChange={e => setName(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="tel" placeholder="Phone" value={phone} onChange={e => setPhone(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <div className="grid grid-cols-2 gap-4">
                               <input required type="email" placeholder="Email" value={email} onChange={e => setEmail(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                               <input required type="text" placeholder="City" value={city} onChange={e => setCity(e.target.value)} className="bg-[#050505] border border-white/10 p-3 text-sm focus:border-[#FF5A00] outline-none transition-colors text-white placeholder:text-gray-600" />
                             </div>
                             <motion.button whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.99 }} disabled={status === 'submitting'} type="submit" className="w-full bg-[#FF5A00] text-white p-4 text-sm font-bold flex items-center justify-center gap-2 hover:bg-[#ff7a33] transition-colors disabled:opacity-50 mt-2 shimmer-cta">
                               {status === 'submitting' ? <Loader2 className="animate-spin" size={16} /> : 'Lock In Estimate'}
                             </motion.button>
                           </form>
                         </motion.div>
                       )}
                     </AnimatePresence>
                  </div>
        
                </div>
                <div className="text-center text-xs text-gray-600 mt-2 pb-4">
                  reg: ir816596 | painting
                </div>
              </section>
            </PageTransition>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:443:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: T2.26 Reputation Funnel - Empty reviews submitted to Formspree block with validation warnings
    ok 26 - T2.26 Reputation Funnel - Empty reviews submitted to Formspree block with validation warnings
      ---
      duration_ms: 0.9366
      type: 'test'
      ...
    # Subtest: T2.27 Reputation Funnel - Formspree connection failures show a clear fallback contact message
    ok 27 - T2.27 Reputation Funnel - Formspree connection failures show a clear fallback contact message
      ---
      duration_ms: 0.4598
      type: 'test'
      ...
    # Subtest: T2.28 Reputation Funnel - Selected review rating events are logged to the analytics engine
    ok 28 - T2.28 Reputation Funnel - Selected review rating events are logged to the analytics engine
      ---
      duration_ms: 0.5347
      type: 'test'
      ...
    # Subtest: T2.29 Reputation Funnel - Hovering over star selection scales buttons and alters fill
    ok 29 - T2.29 Reputation Funnel - Hovering over star selection scales buttons and alters fill
      ---
      duration_ms: 0.4232
      type: 'test'
      ...
    # Subtest: T2.30 Reputation Funnel - Submitting a 0-star rating is blocked
    ok 30 - T2.30 Reputation Funnel - Submitting a 0-star rating is blocked
      ---
      duration_ms: 0.3865
      type: 'test'
      ...
    # Subtest: T2.31 Lead Capture & API - Honeypot field triggers immediate success page for bots
    ok 31 - T2.31 Lead Capture & API - Honeypot field triggers immediate success page for bots
      ---
      duration_ms: 2.3646
      type: 'test'
      ...
    # Subtest: T2.32 Lead Capture & API - API rate limiting blocks requests exceeding 5 per minute
    ok 32 - T2.32 Lead Capture & API - API rate limiting blocks requests exceeding 5 per minute
      ---
      duration_ms: 0.4715
      type: 'test'
      ...
    # Subtest: T2.33 Lead Capture & API - Upload photo URLs are validated and rejected if protocol is missing
    ok 33 - T2.33 Lead Capture & API - Upload photo URLs are validated and rejected if protocol is missing
      ---
      duration_ms: 0.4142
      type: 'test'
      ...
    # Subtest: T2.34 Lead Capture & API - HubSpot CRM integration skips gracefully if credentials are missing
    ok 34 - T2.34 Lead Capture & API - HubSpot CRM integration skips gracefully if credentials are missing
      ---
      duration_ms: 0.4202
      type: 'test'
      ...
    # Subtest: T2.35 Lead Capture & API - API endpoints return 500 with email draft fallbacks if Resend is missing
    ok 35 - T2.35 Lead Capture & API - API endpoints return 500 with email draft fallbacks if Resend is missing
      ---
      duration_ms: 0.5269
      type: 'test'
      ...
    1..35
not ok 50 - Tier 2: Boundary/Corner Cases
  ---
  duration_ms: 34.6694
  type: 'suite'
  location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:280:1'
  failureType: 'subtestsFailed'
  error: '7 subtests failed'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: Tier 3: Cross-Feature Combinations
    # Subtest: T3.1 Offline + Calculator - Submitting estimate lead while offline queues it locally
    ok 1 - T3.1 Offline + Calculator - Submitting estimate lead while offline queues it locally
      ---
      duration_ms: 0.9372
      type: 'test'
      ...
    # Subtest: T3.2 Offline + Review Funnel - Submitting negative reviews offline triggers fallback message
    ok 2 - T3.2 Offline + Review Funnel - Submitting negative reviews offline triggers fallback message
      ---
      duration_ms: 0.4151
      type: 'test'
      ...
    # Subtest: T3.3 SEO Landing + Lead Form - Lead submissions from local SEO routes capture page paths
    ok 3 - T3.3 SEO Landing + Lead Form - Lead submissions from local SEO routes capture page paths
      ---
      duration_ms: 0.4992
      type: 'test'
      ...
    # Subtest: T3.4 Product theater components stay deleted
    ok 4 - T3.4 Product theater components stay deleted
      ---
      duration_ms: 2.0227
      type: 'test'
      ...
    # Subtest: T3.5 Accessibility + Lead Form - Reduced motion options disable animations during interaction
    ok 5 - T3.5 Accessibility + Lead Form - Reduced motion options disable animations during interaction
      ---
      duration_ms: 0.6996
      type: 'test'
      ...
    # Subtest: T3.6 Slider + Room Calculator - Keyboard events do not conflict between sliders
    ok 6 - T3.6 Slider + Room Calculator - Keyboard events do not conflict between sliders
      ---
      duration_ms: 0.9669
      type: 'test'
      ...
    # Subtest: T3.7 Review Funnel + Analytics - Redirecting to Google Business Reviews logs conversions
    ok 7 - T3.7 Review Funnel + Analytics - Redirecting to Google Business Reviews logs conversions
      ---
      duration_ms: 0.4966
      type: 'test'
      ...
    1..7
ok 51 - Tier 3: Cross-Feature Combinations
  ---
  duration_ms: 10.8282
  type: 'suite'
  ...
# Subtest: Tier 4: Real-World Scenarios
    # Subtest: T4.1 Customer Acquisition - Referral, slider comparison, room calculation, and lead submission flow
    not ok 1 - T4.1 Customer Acquisition - Referral, slider comparison, room calculation, and lead submission flow
      ---
      duration_ms: 3.3646
      type: 'test'
      location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:553:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /localStorage\.setItem\('referrer_email'/. Input:
        
        "'use client';\r\n" +
          '\r\n' +
          "import React, { useState, useEffect } from 'react';\n" +
          "import Link from 'next/link';\n" +
          "import Image from 'next/image';\n" +
          "import { usePathname } from 'next/navigation';\r\n" +
          "import { Menu, X, Phone, PaintRoller, ChevronDown, Brush, Warehouse } from 'lucide-react';\r\n" +
          'import { \r\n' +
          '  Sheet, \r\n' +
          '  SheetContent, \r\n' +
          '  SheetTrigger, \r\n' +
          '  SheetHeader,\r\n' +
          '  SheetTitle,\r\n' +
          '  SheetDescription\r\n' +
          "} from '@/components/ui/sheet';\r\n" +
          "import { motion, AnimatePresence } from 'motion/react';\r\n" +
          "import { cn } from '@/lib/utils';\r\n" +
          '\r\n' +
          'export default function ConversionHeader() {\r\n' +
          '  const [isScrolled, setIsScrolled] = useState(false);\r\n' +
          '  const [isServicesOpen, setIsServicesOpen] = useState(false);\r\n' +
          '  const pathname = usePathname();\r\n' +
          '\r\n' +
          '  useEffect(() => {\r\n' +
          '    const handleScroll = () => {\r\n' +
          '      setIsScrolled(window.scrollY > 20);\r\n' +
          '    };\r\n' +
          "    window.addEventListener('scroll', handleScroll);\r\n" +
          "    return () => window.removeEventListener('scroll', handleScroll);\r\n" +
          '  }, []);\r\n' +
          '\r\n' +
          '  // Close dropdown on route change\r\n' +
          '  useEffect(() => {\r\n' +
          '    setIsServicesOpen(false);\r\n' +
          '  }, [pathname]);\r\n' +
          '\r\n' +
          '  const navClass = cn(\r\n' +
          '    "fixed top-0 left-0 right-0 z-50 transition-all duration-300",\r\n' +
          '    isScrolled \r\n' +
          '      ? "bg-[#0A0A0A]/95 backdrop-blur-xl border-b border-zinc-800 py-3" \n' +
          '      : "bg-transparent py-5"\r\n' +
          '  );\r\n' +
          '\r\n' +
          '  const containerVariants = {\r\n' +
          '    hidden: { opacity: 0, y: -5 },\r\n' +
          '    show: {\r\n' +
          '      opacity: 1,\r\n' +
          '      y: 0,\r\n' +
          '      transition: {\r\n' +
          '        staggerChildren: 0.05,\r\n' +
          '        duration: 0.2,\r\n' +
          '      }\r\n' +
          '    },\r\n' +
          '    exit: {\r\n' +
          '      opacity: 0,\r\n' +
          '      y: -5,\r\n' +
          '      transition: { duration: 0.15 }\r\n' +
          '    }\r\n' +
          '  };\r\n' +
          '\r\n' +
          '  const itemVariants = {\r\n' +
          '    hidden: { opacity: 0, x: -10 },\r\n' +
          '    show: { opacity: 1, x: 0, transition: { type: "spring", stiffness: 300, damping: 24 } as any }\r\n' +
          '  };\r\n' +
          '\r\n' +
          '  return (\r\n' +
          '    <header className={navClass}>\r\n' +
          '      <div className="container-page mx-auto">\r\n' +
          '        <div className="flex items-center justify-between">\r\n' +
          '          \r\n' +
          '          {/* Logo */}\r\n' +
          '          <Link href="/" className="flex items-center gap-3 group">\r\n' +
          '            <div className="flex h-10 w-10 shrink-0 items-center justify-center border border-zinc-700 bg-[#0A0A0A] u-transition group-hover:border-[#FF5A00]">\n' +
          `              <Image src="/brand/SkyLLP_BrandLogo.svg" alt="Sky's the Limit Painting" width={24} height={24} priority />\n` +
          '            </div>\r\n' +
          '            <div className="hidden sm:block">\r\n' +
          `              <span className="font-display font-bold text-lg leading-none tracking-tight block text-white">SKY'S THE LIMIT</span>\r\n` +
          '              <span className="eyebrow block mt-0.5 text-[#FF5A00]">PAINTING</span>\r\n' +
          '            </div>\r\n' +
          '          </Link>\r\n' +
          '\r\n' +
          '          {/* Desktop Nav */}\r\n' +
          '          <nav className="hidden lg:flex items-center gap-8">\r\n' +
          '            <div className="relative" onMouseLeave={() => setIsServicesOpen(false)}>\r\n' +
          '              <button \r\n' +
          '                className={cn(\r\n' +
          '                  "flex items-center gap-1 text-sm font-bold tracking-wider uppercase transition-colors py-2",\r\n' +
          `                  isServicesOpen || pathname.includes('/painting-services') ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]"\r\n` +
          '                )}\r\n' +
          '                onMouseEnter={() => setIsServicesOpen(true)}\r\n' +
          '                onClick={() => setIsServicesOpen(!isServicesOpen)}\r\n' +
          '              >\r\n' +
          '                Services <ChevronDown size={14} className={cn("transition-transform duration-200", isServicesOpen && "rotate-180")} />\r\n' +
          '              </button>\r\n' +
          '\r\n' +
          '              <AnimatePresence>\r\n' +
          '                {isServicesOpen && (\r\n' +
          '                  <motion.div \r\n' +
          '                    variants={containerVariants}\r\n' +
          '                    initial="hidden"\r\n' +
          '                    animate="show"\r\n' +
          '                    exit="exit"\r\n' +
          '                    className="absolute top-full left-1/2 -translate-x-1/2 mt-2 w-[480px] bg-[#0B0B0D] border border-white/10 shadow-2xl p-4 grid grid-cols-2 gap-2"\r\n' +
          '                  >\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/interior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Interior Painting</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Walls, trim, ceilings, & prep.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/exterior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><Brush size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Exterior Painting</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Siding, brick, stucco, & decks.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/painting-services/cabinet-refinishing" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Cabinet Refinishing</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">Factory-finish transformations.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '\r\n' +
          '                    <motion.div variants={itemVariants}>\r\n' +
          '                      <Link href="/commercial" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">\r\n' +
          '                        <div className="mt-0.5 text-[#FF5A00]"><Warehouse size={18} /></div>\r\n' +
          '                        <div>\r\n' +
          '                          <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Commercial</div>\r\n' +
          '                          <div className="text-xs text-gray-400 mt-1">HOA, property mgmt, retail.</div>\r\n' +
          '                        </div>\r\n' +
          '                      </Link>\r\n' +
          '                    </motion.div>\r\n' +
          '                  </motion.div>\r\n' +
          '                )}\r\n' +
          '              </AnimatePresence>\r\n' +
          '            </div>\r\n' +
          '\r\n' +
          '            <Link href="/projects" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/projects" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              Projects\r\n' +
          '            </Link>\r\n' +
          '            <Link href="/service-area" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/service-area" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              Service Area\r\n' +
          '            </Link>\r\n' +
          '            <Link href="/about" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/about" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>\r\n' +
          '              About Us\r\n' +
          '            </Link>\r\n' +
          '          </nav>\r\n' +
          '\r\n' +
          '          {/* CTA & Mobile Toggle */}\r\n' +
          '          <div className="flex items-center gap-4">\r\n' +
          '            <a href="tel:+16514104196" className="hidden lg:flex items-center gap-2 text-sm font-bold text-white hover:text-[#FF5A00] transition-colors">\n' +
          '              <Phone size={16} />\r\n' +
          '              (651) 410-4196\n' +
          '            </a>\r\n' +
          '            \r\n' +
          '            <Link \r\n' +
          '              href="/estimate" \r\n' +
          '              className="hidden bg-[#FF5A00] px-6 py-2.5 text-sm font-bold uppercase tracking-wider text-white transition-colors hover:bg-[#d94d00] md:inline-flex"\n' +
          '            >\r\n' +
          '              Get Estimate\r\n' +
          '            </Link>\r\n' +
          '\r\n' +
          '            {/* Mobile Menu */}\r\n' +
          '            <Sheet>\r\n' +
          '              <SheetTrigger>\r\n' +
          '                <button className="lg:hidden text-white p-2" aria-label="Open Menu">\r\n' +
          '                  <Menu size={24} />\r\n' +
          '                </button>\r\n' +
          '              </SheetTrigger>\r\n' +
          '              <SheetContent side="right" className="w-full sm:w-[400px] bg-[#050505] border-l border-white/10 p-0">\r\n' +
          '                <SheetHeader className="p-6 border-b border-white/10 text-left">\r\n' +
          '                  <SheetTitle className="text-white font-display text-xl uppercase">Menu</SheetTitle>\r\n' +
          `                  <SheetDescription className="sr-only">Navigate around the Sky's the Limit Painting website.</SheetDescription>\r\n` +
          '                </SheetHeader>\r\n' +
          '                \r\n' +
          '                <div className="p-6 overflow-y-auto h-[calc(100vh-80px)]">\r\n' +
          '                  <nav className="flex flex-col gap-6 text-xl">\r\n' +
          '                    <Link href="/">Home</Link>\r\n' +
          '                    <div className="pt-4 pb-2">\r\n' +
          '                      <p className="eyebrow mb-4">Painting Services</p>\r\n' +
          '                      <div className="flex flex-col gap-4 pl-4 border-l border-white/10">\r\n' +
          '                        <Link href="/painting-services/interior-painting" className="text-lg">Interior Painting</Link>\r\n' +
          '                        <Link href="/painting-services/exterior-painting" className="text-lg">Exterior Painting</Link>\r\n' +
          '                        <Link href="/painting-services/cabinet-refinishing" className="text-lg">Cabinet Refinishing</Link>\r\n' +
          '                        <Link href="/commercial" className="text-lg">Commercial & HOA</Link>\r\n' +
          '                      </div>\r\n' +
          '                    </div>\r\n' +
          '                    <Link href="/projects">Projects</Link>\r\n' +
          '                    <Link href="/service-area">Service Area</Link>\r\n' +
          '                    <Link href="/about">About Us</Link>\r\n' +
          '                    <Link href="/contact">Contact</Link>\r\n' +
          '   '... 755 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import React, { useState, useEffect } from 'react';
        import Link from 'next/link';
        import Image from 'next/image';
        import { usePathname } from 'next/navigation';
        import { Menu, X, Phone, PaintRoller, ChevronDown, Brush, Warehouse } from 'lucide-react';
        import { 
          Sheet, 
          SheetContent, 
          SheetTrigger, 
          SheetHeader,
          SheetTitle,
          SheetDescription
        } from '@/components/ui/sheet';
        import { motion, AnimatePresence } from 'motion/react';
        import { cn } from '@/lib/utils';
        
        export default function ConversionHeader() {
          const [isScrolled, setIsScrolled] = useState(false);
          const [isServicesOpen, setIsServicesOpen] = useState(false);
          const pathname = usePathname();
        
          useEffect(() => {
            const handleScroll = () => {
              setIsScrolled(window.scrollY > 20);
            };
            window.addEventListener('scroll', handleScroll);
            return () => window.removeEventListener('scroll', handleScroll);
          }, []);
        
          // Close dropdown on route change
          useEffect(() => {
            setIsServicesOpen(false);
          }, [pathname]);
        
          const navClass = cn(
            "fixed top-0 left-0 right-0 z-50 transition-all duration-300",
            isScrolled 
              ? "bg-[#0A0A0A]/95 backdrop-blur-xl border-b border-zinc-800 py-3" 
              : "bg-transparent py-5"
          );
        
          const containerVariants = {
            hidden: { opacity: 0, y: -5 },
            show: {
              opacity: 1,
              y: 0,
              transition: {
                staggerChildren: 0.05,
                duration: 0.2,
              }
            },
            exit: {
              opacity: 0,
              y: -5,
              transition: { duration: 0.15 }
            }
          };
        
          const itemVariants = {
            hidden: { opacity: 0, x: -10 },
            show: { opacity: 1, x: 0, transition: { type: "spring", stiffness: 300, damping: 24 } as any }
          };
        
          return (
            <header className={navClass}>
              <div className="container-page mx-auto">
                <div className="flex items-center justify-between">
                  
                  {/* Logo */}
                  <Link href="/" className="flex items-center gap-3 group">
                    <div className="flex h-10 w-10 shrink-0 items-center justify-center border border-zinc-700 bg-[#0A0A0A] u-transition group-hover:border-[#FF5A00]">
                      <Image src="/brand/SkyLLP_BrandLogo.svg" alt="Sky's the Limit Painting" width={24} height={24} priority />
                    </div>
                    <div className="hidden sm:block">
                      <span className="font-display font-bold text-lg leading-none tracking-tight block text-white">SKY'S THE LIMIT</span>
                      <span className="eyebrow block mt-0.5 text-[#FF5A00]">PAINTING</span>
                    </div>
                  </Link>
        
                  {/* Desktop Nav */}
                  <nav className="hidden lg:flex items-center gap-8">
                    <div className="relative" onMouseLeave={() => setIsServicesOpen(false)}>
                      <button 
                        className={cn(
                          "flex items-center gap-1 text-sm font-bold tracking-wider uppercase transition-colors py-2",
                          isServicesOpen || pathname.includes('/painting-services') ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]"
                        )}
                        onMouseEnter={() => setIsServicesOpen(true)}
                        onClick={() => setIsServicesOpen(!isServicesOpen)}
                      >
                        Services <ChevronDown size={14} className={cn("transition-transform duration-200", isServicesOpen && "rotate-180")} />
                      </button>
        
                      <AnimatePresence>
                        {isServicesOpen && (
                          <motion.div 
                            variants={containerVariants}
                            initial="hidden"
                            animate="show"
                            exit="exit"
                            className="absolute top-full left-1/2 -translate-x-1/2 mt-2 w-[480px] bg-[#0B0B0D] border border-white/10 shadow-2xl p-4 grid grid-cols-2 gap-2"
                          >
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/interior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Interior Painting</div>
                                  <div className="text-xs text-gray-400 mt-1">Walls, trim, ceilings, & prep.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/exterior-painting" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><Brush size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Exterior Painting</div>
                                  <div className="text-xs text-gray-400 mt-1">Siding, brick, stucco, & decks.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/painting-services/cabinet-refinishing" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><PaintRoller size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Cabinet Refinishing</div>
                                  <div className="text-xs text-gray-400 mt-1">Factory-finish transformations.</div>
                                </div>
                              </Link>
                            </motion.div>
        
                            <motion.div variants={itemVariants}>
                              <Link href="/commercial" className="flex items-start gap-3 p-3 hover:bg-white/5 transition-colors group">
                                <div className="mt-0.5 text-[#FF5A00]"><Warehouse size={18} /></div>
                                <div>
                                  <div className="text-white font-bold text-sm group-hover:text-[#FF5A00] transition-colors">Commercial</div>
                                  <div className="text-xs text-gray-400 mt-1">HOA, property mgmt, retail.</div>
                                </div>
                              </Link>
                            </motion.div>
                          </motion.div>
                        )}
                      </AnimatePresence>
                    </div>
        
                    <Link href="/projects" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/projects" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      Projects
                    </Link>
                    <Link href="/service-area" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/service-area" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      Service Area
                    </Link>
                    <Link href="/about" className={cn("text-sm font-bold tracking-wider uppercase transition-colors", pathname === "/about" ? "text-[#FF5A00]" : "text-white hover:text-[#FF5A00]")}>
                      About Us
                    </Link>
                  </nav>
        
                  {/* CTA & Mobile Toggle */}
                  <div className="flex items-center gap-4">
                    <a href="tel:+16514104196" className="hidden lg:flex items-center gap-2 text-sm font-bold text-white hover:text-[#FF5A00] transition-colors">
                      <Phone size={16} />
                      (651) 410-4196
                    </a>
                    
                    <Link 
                      href="/estimate" 
                      className="hidden bg-[#FF5A00] px-6 py-2.5 text-sm font-bold uppercase tracking-wider text-white transition-colors hover:bg-[#d94d00] md:inline-flex"
                    >
                      Get Estimate
                    </Link>
        
                    {/* Mobile Menu */}
                    <Sheet>
                      <SheetTrigger>
                        <button className="lg:hidden text-white p-2" aria-label="Open Menu">
                          <Menu size={24} />
                        </button>
                      </SheetTrigger>
                      <SheetContent side="right" className="w-full sm:w-[400px] bg-[#050505] border-l border-white/10 p-0">
                        <SheetHeader className="p-6 border-b border-white/10 text-left">
                          <SheetTitle className="text-white font-display text-xl uppercase">Menu</SheetTitle>
                          <SheetDescription className="sr-only">Navigate around the Sky's the Limit Painting website.</SheetDescription>
                        </SheetHeader>
                        
                        <div className="p-6 overflow-y-auto h-[calc(100vh-80px)]">
                          <nav className="flex flex-col gap-6 text-xl">
                            <Link href="/">Home</Link>
                            <div className="pt-4 pb-2">
                              <p className="eyebrow mb-4">Painting Services</p>
                              <div className="flex flex-col gap-4 pl-4 border-l border-white/10">
                                <Link href="/painting-services/interior-painting" className="text-lg">Interior Painting</Link>
                                <Link href="/painting-services/exterior-painting" className="text-lg">Exterior Painting</Link>
                                <Link href="/painting-services/cabinet-refinishing" className="text-lg">Cabinet Refinishing</Link>
                                <Link href="/commercial" className="text-lg">Commercial & HOA</Link>
                              </div>
                            </div>
                            <Link href="/projects">Projects</Link>
                            <Link href="/service-area">Service Area</Link>
                            <Link href="/about">About Us</Link>
                            <Link href="/contact">Contact</Link>
                          </nav>
                          
                          <div className="mt-12 flex flex-col gap-4">
                            <Link href="/estimate" className="bg-[#FF5A00] text-white text-center py-4 font-bold tracking-wider uppercase">
                              Get Your Free Estimate
                            </Link>
                            <a href="tel:+16514104196" className="border border-white/10 text-white text-center py-4 font-bold flex items-center justify-center gap-2">
                              <Phone size={18} />
                              (651) 410-4196
                            </a>
                          </div>
                        </div>
                      </SheetContent>
                    </Sheet>
                  </div>
                </div>
              </div>
            </header>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/e2e.test.mjs:559:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Test.start (node:internal/test_runner/test:944:17)
        node:internal/test_runner/test:1440:71
        node:internal/per_context/primordials:466:82
        new Promise (<anonymous>)
        new SafePromise (node:internal/per_context/primordials:435:3)
        node:internal/per_context/primordials:466:9
        Array.map (<anonymous>)
      ...
    # Subtest: T4.2 Intercepted Review - Negative rating intercepts unhappy client to private feedback
    ok 2 - T4.2 Intercepted Review - Negative rating intercepts unhappy client to private feedback
      ---
      duration_ms: 0.4621
      type: 'test'
      ...
    # Subtest: T4.3 Offline Contractor - Estimate costs, queue lead offline, and sync online
    ok 3 - T4.3 Offline Contractor - Estimate costs, queue lead offline, and sync online
      ---
      duration_ms: 0.4585
      type: 'test'
      ...
    # Subtest: T4.4 Security & Anti-Spam - Bot honeypot triggers quick success, and rate limit blocks flood
    ok 4 - T4.4 Security & Anti-Spam - Bot honeypot triggers quick success, and rate limit blocks flood
      ---
      duration_ms: 0.6184
      type: 'test'
      ...
    # Subtest: T4.5 Static SEO Crawler - Indexation, sitemaps validation, and schema markup checks
    ok 5 - T4.5 Static SEO Crawler - Indexation, sitemaps validation, and schema markup checks
      ---
      duration_ms: 0.5261
      type: 'test'
      ...
    1..5
not ok 52 - Tier 4: Real-World Scenarios
  ---
  duration_ms: 5.8032
  type: 'suite'
  location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\e2e.test.mjs:551:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: isAuthOkFromText fail-closed on not logged in / ambiguous / empty
ok 53 - isAuthOkFromText fail-closed on not logged in / ambiguous / empty
  ---
  duration_ms: 18.5426
  type: 'test'
  ...
# Subtest: deps bump subjects are one-offs
ok 54 - deps bump subjects are one-offs
  ---
  duration_ms: 1.2824
  type: 'test'
  ...
# Subtest: git-derived lesson shapes are always fake
ok 55 - git-derived lesson shapes are always fake
  ---
  duration_ms: 0.6028
  type: 'test'
  ...
# Subtest: extractLessons ignores git_log completely (unauthenticated)
ok 56 - extractLessons ignores git_log completely (unauthenticated)
  ---
  duration_ms: 0.8193
  type: 'test'
  ...
# Subtest: extractLessons ignores git_log even when authenticated but no real signals
ok 57 - extractLessons ignores git_log even when authenticated but no real signals
  ---
  duration_ms: 0.2655
  type: 'test'
  ...
# Subtest: extractLessons accepts real checkpoint with substance
ok 58 - extractLessons accepts real checkpoint with substance
  ---
  duration_ms: 1.3234
  type: 'test'
  ...
# Subtest: extractLessons accepts search error hits with body
ok 59 - extractLessons accepts search error hits with body
  ---
  duration_ms: 0.7398
  type: 'test'
  ...
# Subtest: assertNoFakeEntireLessons throws on git fakes
ok 60 - assertNoFakeEntireLessons throws on git fakes
  ---
  duration_ms: 1.3877
  type: 'test'
  ...
# Subtest: SKILL_WORTHY_KINDS never includes git
ok 61 - SKILL_WORTHY_KINDS never includes git
  ---
  duration_ms: 0.4294
  type: 'test'
  ...
# Subtest: short commit-stub body is fake even with checkpoint kind
ok 62 - short commit-stub body is fake even with checkpoint kind
  ---
  duration_ms: 0.7151
  type: 'test'
  ...
# Supabase client not initialized. Skipping database insertion.
# WARNING: RESEND_API_KEY is not set in the environment variables. Lead emails will not be sent!
# Supabase client not initialized. Skipping lead event insertion.
# Supabase client not initialized. Skipping lead event insertion.
# Supabase client not initialized. Skipping lead event insertion.
# Supabase client not initialized. Skipping lead event insertion.
# Subtest: api/leads Route Handler
    # Subtest: successfully processes valid lead payload with mocked webhook
    ok 1 - successfully processes valid lead payload with mocked webhook
      ---
      duration_ms: 640.7496
      type: 'test'
      ...
    # Subtest: returns 400 when missing required fields
    ok 2 - returns 400 when missing required fields
      ---
      duration_ms: 2.6987
      type: 'test'
      ...
    # Subtest: returns 400 for invalid email
    ok 3 - returns 400 for invalid email
      ---
      duration_ms: 52.8682
      type: 'test'
      ...
    1..3
ok 63 - api/leads Route Handler
  ---
  duration_ms: 700.5549
  type: 'suite'
  ...
# Subtest: normalizeErrorText strips paths and timestamps for stable fingerprints
ok 64 - normalizeErrorText strips paths and timestamps for stable fingerprints
  ---
  duration_ms: 2.5821
  type: 'test'
  ...
# Subtest: fingerprint is stable across duplicate OKF dumps with different paths
ok 65 - fingerprint is stable across duplicate OKF dumps with different paths
  ---
  duration_ms: 1.216
  type: 'test'
  ...
# Subtest: classifyFailure maps OKF wiki failures to healable okf-wiki
ok 66 - classifyFailure maps OKF wiki failures to healable okf-wiki
  ---
  duration_ms: 0.7245
  type: 'test'
  ...
# Subtest: classifyFailure maps PowerShell switch errors
ok 67 - classifyFailure maps PowerShell switch errors
  ---
  duration_ms: 0.4695
  type: 'test'
  ...
# Subtest: learning-loop CLI record dedupes second identical failure
ok 68 - learning-loop CLI record dedupes second identical failure
  ---
  duration_ms: 1411.3471
  type: 'test'
  ...
# Subtest: agent OS kernel quarantines without git checkout rollback
ok 69 - agent OS kernel quarantines without git checkout rollback
  ---
  duration_ms: 1.0772
  type: 'test'
  ...
# Subtest: lib/env - Environment variable loading
    # Subtest: ENV object exports expected keys
    ok 1 - ENV object exports expected keys
      ---
      duration_ms: 1.458
      type: 'test'
      ...
    # Subtest: getEnv returns correct value from process.env
    ok 2 - getEnv returns correct value from process.env
      ---
      duration_ms: 0.4989
      type: 'test'
      ...
    # Subtest: getEnv falls back to NEXT_PUBLIC_ prefix
    ok 3 - getEnv falls back to NEXT_PUBLIC_ prefix
      ---
      duration_ms: 0.5274
      type: 'test'
      ...
    # Subtest: getEnv returns undefined when key is not set
    ok 4 - getEnv returns undefined when key is not set
      ---
      duration_ms: 0.3039
      type: 'test'
      ...
    1..4
ok 70 - lib/env - Environment variable loading
  ---
  duration_ms: 4.6443
  type: 'suite'
  ...
# Subtest: lib/contact - Contact utilities
    # Subtest: businessEmail matches expected value
    ok 1 - businessEmail matches expected value
      ---
      duration_ms: 0.4729
      type: 'test'
      ...
    # Subtest: businessPhone matches expected value
    ok 2 - businessPhone matches expected value
      ---
      duration_ms: 3.4605
      type: 'test'
      ...
    # Subtest: smsPhone is E.164 format without dashes
    ok 3 - smsPhone is E.164 format without dashes
      ---
      duration_ms: 0.6665
      type: 'test'
      ...
    # Subtest: buildEstimateMailto generates valid mailto link
    ok 4 - buildEstimateMailto generates valid mailto link
      ---
      duration_ms: 1.1384
      type: 'test'
      ...
    # Subtest: buildEstimateMailto encodes body with field labels
    ok 5 - buildEstimateMailto encodes body with field labels
      ---
      duration_ms: 0.5929
      type: 'test'
      ...
    # Subtest: buildEstimateMailto filters empty fields
    ok 6 - buildEstimateMailto filters empty fields
      ---
      duration_ms: 0.5309
      type: 'test'
      ...
    1..6
ok 71 - lib/contact - Contact utilities
  ---
  duration_ms: 7.8059
  type: 'suite'
  ...
# Subtest: lib/seo - Schema.org JSON-LD generation
    # Subtest: serviceSchema returns valid Service schema
    ok 1 - serviceSchema returns valid Service schema
      ---
      duration_ms: 0.6441
      type: 'test'
      ...
    # Subtest: breadcrumbSchema returns valid BreadcrumbList
    ok 2 - breadcrumbSchema returns valid BreadcrumbList
      ---
      duration_ms: 1.1269
      type: 'test'
      ...
    # Subtest: breadcrumbSchema items have correct type and absolute URLs
    ok 3 - breadcrumbSchema items have correct type and absolute URLs
      ---
      duration_ms: 0.498
      type: 'test'
      ...
    # Subtest: localBusinessSchema returns valid schema for a city
    ok 4 - localBusinessSchema returns valid schema for a city
      ---
      duration_ms: 0.4552
      type: 'test'
      ...
    1..4
ok 72 - lib/seo - Schema.org JSON-LD generation
  ---
  duration_ms: 3.2958
  type: 'suite'
  ...
# Subtest: lib/settings - Company settings and defaults
    # Subtest: DEFAULT_SETTINGS company_name matches brand
    ok 1 - DEFAULT_SETTINGS company_name matches brand
      ---
      duration_ms: 0.2189
      type: 'test'
      ...
    # Subtest: DEFAULT_SETTINGS service_areas includes all six service areas
    ok 2 - DEFAULT_SETTINGS service_areas includes all six service areas
      ---
      duration_ms: 0.1982
      type: 'test'
      ...
    1..2
ok 73 - lib/settings - Company settings and defaults
  ---
  duration_ms: 0.6219
  type: 'suite'
  ...
# Subtest: gatePortalAccess denies missing session
ok 74 - gatePortalAccess denies missing session
  ---
  duration_ms: 2.2908
  type: 'test'
  ...
# Subtest: gatePortalAccess denies user without email
ok 75 - gatePortalAccess denies user without email
  ---
  duration_ms: 0.4109
  type: 'test'
  ...
# Subtest: gatePortalAccess accepts valid user
ok 76 - gatePortalAccess accepts valid user
  ---
  duration_ms: 0.2823
  type: 'test'
  ...
# Subtest: isProtectedPortalPath protects /portal but not login
ok 77 - isProtectedPortalPath protects /portal but not login
  ---
  duration_ms: 0.2727
  type: 'test'
  ...
# Subtest: portalLoginUrl encodes next path
ok 78 - portalLoginUrl encodes next path
  ---
  duration_ms: 0.2674
  type: 'test'
  ...
# Subtest: mapLeadsForPortal filters by email and strips empty ids
ok 79 - mapLeadsForPortal filters by email and strips empty ids
  ---
  duration_ms: 0.4954
  type: 'test'
  ...
# Subtest: buildPortalOAuthOptions builds callback redirect for Google
ok 80 - buildPortalOAuthOptions builds callback redirect for Google
  ---
  duration_ms: 0.5814
  type: 'test'
  ...
# [portal] leads fetch failed: permission denied for table leads
# Subtest: migration replaces using(false) with jwt email match
ok 81 - migration replaces using(false) with jwt email match
  ---
  duration_ms: 3.3211
  type: 'test'
  ...
# Subtest: queryLeadsForPortalEmail hits leads with email ilike + select list
ok 82 - queryLeadsForPortalEmail hits leads with email ilike + select list
  ---
  duration_ms: 1.2012
  type: 'test'
  ...
# Subtest: loadPortalDashboard returns only RLS-visible rows for session email
ok 83 - loadPortalDashboard returns only RLS-visible rows for session email
  ---
  duration_ms: 7.6051
  type: 'test'
  ...
# Subtest: loadPortalDashboard empty when RLS hides all (wrong email / using false simulation)
ok 84 - loadPortalDashboard empty when RLS hides all (wrong email / using false simulation)
  ---
  duration_ms: 8.3043
  type: 'test'
  ...
# Subtest: loadPortalDashboard surfaces query errors without leaking other data
ok 85 - loadPortalDashboard surfaces query errors without leaking other data
  ---
  duration_ms: 1.9223
  type: 'test'
  ...
# Subtest: applyLeadsSelectRls matches SQL policy semantics (case-insensitive email)
ok 86 - applyLeadsSelectRls matches SQL policy semantics (case-insensitive email)
  ---
  duration_ms: 0.2847
  type: 'test'
  ...
# Subtest: portal DTO never returns another emailΓÇÖs leads
ok 87 - portal DTO never returns another emailΓÇÖs leads
  ---
  duration_ms: 1.8089
  type: 'test'
  ...
# Subtest: null rows yield empty list
ok 88 - null rows yield empty list
  ---
  duration_ms: 1.1925
  type: 'test'
  ...
# Subtest: Vite config is deleted and package.json does not expose Gemini sdk
ok 89 - Vite config is deleted and package.json does not expose Gemini sdk
  ---
  duration_ms: 3.4317
  type: 'test'
  ...
# Subtest: lead email HTML escapes submitted keys and values
ok 90 - lead email HTML escapes submitted keys and values
  ---
  duration_ms: 3.6975
  type: 'test'
  ...
# Subtest: Vercel config has security headers and no blanket SPA rewrite
not ok 91 - Vercel config has security headers and no blanket SPA rewrite
  ---
  duration_ms: 4.1362
  type: 'test'
  location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\remediation.test.mjs:24:1'
  failureType: 'testCodeFailure'
  error: 'vercel.json must not coexist with vercel.ts'
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/remediation.test.mjs:27:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: build pipeline prerenders public routes and static 404 metadata
ok 92 - build pipeline prerenders public routes and static 404 metadata
  ---
  duration_ms: 3.431
  type: 'test'
  ...
# Subtest: before and after slider remains pointer-enabled and is keyboard accessible
ok 93 - before and after slider remains pointer-enabled and is keyboard accessible
  ---
  duration_ms: 0.5992
  type: 'test'
  ...
# Subtest: service area map is fast, routable, and accessible
ok 94 - service area map is fast, routable, and accessible
  ---
  duration_ms: 1.2218
  type: 'test'
  ...
# Subtest: lead form controls have accessible names and normalized funnel events
ok 95 - lead form controls have accessible names and normalized funnel events
  ---
  duration_ms: 1.6934
  type: 'test'
  ...
# Subtest: top-level routes use the three-market architecture in Next.js filesystem routing
ok 96 - top-level routes use the three-market architecture in Next.js filesystem routing
  ---
  duration_ms: 7.5108
  type: 'test'
  ...
# Subtest: primary navigation leads with residential, commercial, and public sector
not ok 97 - primary navigation leads with residential, commercial, and public sector
  ---
  duration_ms: 1.8568
  type: 'test'
  location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\site-architecture.test.mjs:20:1'
  failureType: 'testCodeFailure'
  error: 'Residential nav item is missing'
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/site-architecture.test.mjs:27:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: homepage states the approved positioning and avoids forbidden claims
not ok 98 - homepage states the approved positioning and avoids forbidden claims
  ---
  duration_ms: 4.6435
  type: 'test'
  location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\site-architecture.test.mjs:33:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Residential detail\. Commercial discipline\. Public-sector ready\./. Input:
    
    "import Link from 'next/link';\n" +
      "import { ArrowRight, Check, ClipboardCheck, HardHat, Ruler, ShieldCheck } from 'lucide-react';\n" +
      "import LeadForm from '../components/LeadForm';\n" +
      "import PrepProtocolStage from '../components/PrepProtocolStage';\n" +
      "import ResponsiveImage from '../components/ResponsiveImage';\n" +
      "import { faqSchema } from '../lib/seo';\n" +
      '\n' +
      'const scopeRows = [\n' +
      "  ['INTERIOR', 'Walls ┬╖ ceilings ┬╖ trim ┬╖ cabinets', 'Residential'],\n" +
      "  ['EXTERIOR', 'Siding ┬╖ trim ┬╖ decks ┬╖ fences', 'Residential'],\n" +
      "  ['PROPERTY', 'Repaints ┬╖ turnovers ┬╖ common areas', 'Commercial'],\n" +
      "  ['MARKING', 'Parking lots ┬╖ curbs ┬╖ safety striping', 'Public / commercial'],\n" +
      '];\n' +
      '\n' +
      'const faqItems = [\n' +
      '  {\n' +
      "    question: 'How is a painting project scoped?',\n" +
      "    answer: 'The scope begins with surfaces, condition, access, preparation requirements, photos, and timeline. The result is a written estimate before work is scheduled.',\n" +
      '  },\n' +
      '  {\n' +
      "    question: 'Who performs the walkthrough?',\n" +
      "    answer: 'Anthony Briseno handles the project conversation and walkthrough directly, so the scope is not passed between a sales team and a crew.',\n" +
      '  },\n' +
      '  {\n' +
      "    question: 'Can documentation be provided for property or public work?',\n" +
      "    answer: 'A certificate of insurance is available for qualified commercial and public-sector opportunities. Ask about documentation during the scope review.',\n" +
      '  },\n' +
      '];\n' +
      '\n' +
      'export default function HomeClient() {\n' +
      '  return (\n' +
      '    <article className="bg-[#0A0A0A] text-zinc-100">\n' +
      '      <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqSchema(faqItems)) }} />\n' +
      '\n' +
      '      <section aria-labelledby="home-title" className="border-b border-zinc-800">\n' +
      '        <div className="mx-auto grid min-h-[calc(100svh-117px)] max-w-7xl lg:grid-cols-[minmax(0,1.1fr)_minmax(24rem,0.9fr)]">\n' +
      '          <div className="flex flex-col justify-between px-6 py-10 sm:px-8 lg:px-12 lg:py-14">\n' +
      '            <div>\n' +
      '              <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">Twin Cities / Residential painting scope desk</p>\n' +
      '              <h1 id="home-title" className="mt-6 max-w-4xl text-5xl font-black uppercase leading-[0.9] tracking-[-0.06em] text-white sm:text-7xl lg:text-8xl">\n' +
      '                Paint work with the prep exposed.\n' +
      '              </h1>\n' +
      '              <p className="mt-8 max-w-2xl text-base leading-7 text-zinc-300 sm:text-lg">\n' +
      '                Sky&apos;s the Limit Painting LLC scopes the protection, correction, coating system, and final walkthrough before a project reaches your home.\n' +
      '              </p>\n' +
      '            </div>\n' +
      '\n' +
      '            <div className="mt-10">\n' +
      '              <Link href="/estimate" className="inline-flex min-h-14 items-center gap-4 bg-[#FF5A00] px-6 py-4 text-sm font-black uppercase tracking-[0.12em] text-white transition-colors hover:bg-[#d94d00] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-[#FF5A00]">\n' +
      '                Calculate room scope <ArrowRight aria-hidden="true" size={18} />\n' +
      '              </Link>\n' +
      '              <div className="mt-5 grid max-w-2xl border border-zinc-700 sm:grid-cols-3">\n' +
      `                {[['MN ID', 'IR816596'], ['WORKERS COMP', '176.041 EXEMPT'], ['COVERAGE', '100% INSURED']].map(([label, value]) => <div key={label} className="border-b border-zinc-800 px-4 py-3 last:border-b-0 sm:border-b-0 sm:border-r sm:last:border-r-0"><p className="font-mono text-[9px] uppercase tracking-[0.14em] text-zinc-500">{label}</p><p className="mt-1 font-mono text-[10px] font-bold uppercase tracking-[0.08em] text-white">{value}</p></div>)}\n` +
      '              </div>\n' +
      '              <p className="mt-4 font-mono text-[10px] uppercase tracking-[0.16em] text-zinc-500">Start with surfaces, condition, and dimensions. Contact details come last.</p>\n' +
      '            </div>\n' +
      '          </div>\n' +
      '\n' +
      '          <div className="relative min-h-[24rem] border-t border-zinc-800 lg:min-h-0 lg:border-l lg:border-t-0">\n' +
      '            <ResponsiveImage src="/images/site/marketing-hero-exterior-painting.webp" alt="Painter preparing an exterior trim surface with windows and landscaping protected" width={1600} height={900} sizes="(min-width: 1024px) 45vw, 100vw" priority fetchPriority="high" className="absolute inset-0 h-full w-full object-cover" />\n' +
      '            <div className="absolute inset-x-0 bottom-0 border-t border-zinc-700 bg-[#0A0A0A]/95 p-5 backdrop-blur-sm">\n' +
      '              <p className="font-mono text-[10px] uppercase tracking-[0.18em] text-zinc-400">Field image / exterior preparation</p>\n' +
      '              <p className="mt-2 text-sm font-semibold text-white">Protection and surface correction are part of the work orderΓÇönot an upgrade.</p>\n' +
      '            </div>\n' +
      '          </div>\n' +
      '        </div>\n' +
      '      </section>\n' +
      '\n' +
      '      <section aria-labelledby="scope-ledger" className="border-b border-zinc-800">\n' +
      '        <div className="mx-auto max-w-7xl px-6 py-16 sm:px-8 lg:px-12">\n' +
      '          <div className="grid gap-8 lg:grid-cols-[0.75fr_1.25fr] lg:gap-16">\n' +
      '            <div>\n' +
      '              <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">01 / Scope ledger</p>\n' +
      '              <h2 id="scope-ledger" className="mt-4 text-4xl font-black uppercase leading-none tracking-[-0.05em] text-white sm:text-5xl">What gets priced.</h2>\n' +
      '              <p className="mt-6 max-w-md text-sm leading-7 text-zinc-400">The estimate follows the actual site conditions. Each line item exists to make preparation and execution visible before scheduling.</p>\n' +
      '            </div>\n' +
      '            <dl className="border-t border-zinc-700">\n' +
      '              {scopeRows.map(([code, work, market]) => (\n' +
      '                <div key={code} className="grid grid-cols-[4rem_1fr] gap-4 border-b border-zinc-800 py-5 sm:grid-cols-[5rem_1fr_auto] sm:items-center">\n' +
      '                  <dt className="font-mono text-xs font-bold tracking-widest text-[#FF5A00]">{code}</dt>\n' +
      '                  <dd className="text-sm font-bold uppercase tracking-wide text-white">{work}</dd>\n' +
      '                  <dd className="col-start-2 font-mono text-[10px] uppercase tracking-[0.16em] text-zinc-500 sm:col-start-auto">{market}</dd>\n' +
      '                </div>\n' +
      '              ))}\n' +
      '            </dl>\n' +
      '          </div>\n' +
      '        </div>\n' +
      '      </section>\n' +
      '\n' +
      '      <PrepProtocolStage />\n' +
      '\n' +
      '      <section aria-labelledby="proof" className="border-b border-zinc-800">\n' +
      '        <div className="mx-auto grid max-w-7xl lg:grid-cols-2">\n' +
      '          <div className="relative aspect-[4/3] border-b border-zinc-800 lg:aspect-auto lg:border-b-0 lg:border-r">\n' +
      '            <ResponsiveImage src="/images/site/iphone-interior-painting-progress.webp" alt="Interior painting work in progress with room surfaces prepared for coating" width={1200} height={900} sizes="(min-width: 1024px) 50vw, 100vw" className="absolute inset-0 h-full w-full object-cover" />\n' +
      '          </div>\n' +
      '          <div className="px-6 py-16 sm:px-8 lg:px-12">\n' +
      '            <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">03 / Operational controls</p>\n' +
      '            <h2 id="proof" className="mt-4 text-4xl font-black uppercase leading-none tracking-[-0.05em] text-white sm:text-5xl">A contractor record you can inspect.</h2>\n' +
      '            <ul className="mt-10 divide-y divide-zinc-800 border-y border-zinc-800">\n' +
      '              {[\n' +
      "                [ShieldCheck, 'MN ID: IR816596', 'Minnesota specialty contractor registration'],\n" +
      "                [HardHat, '176.041 Workers Comp Exempt', 'Owner-operator exemption record'],\n" +
      "                [ClipboardCheck, '100% Insured', 'Certificate of insurance available for qualified opportunities'],\n" +
      "                [Ruler, 'Direct scope review', 'Owner-led walkthrough through final detail'],\n" +
      '              ].map(([Icon, title, detail]) => {\n' +
      '                const Mark = Icon as typeof ShieldCheck;\n' +
      '                return <li key={title as string} className="flex gap-4 py-5"><Mark aria-hidden="true" className="mt-1 shrink-0 text-[#FF5A00]" size={18} /><div><p className="text-sm font-bold uppercase tracking-wide text-white">{title as string}</p><p className="mt-1 font-mono text-[10px] uppercase tracking-[0.12em] text-zinc-500">{detail as string}</p></div></li>;\n' +
      '              })}\n' +
      '            </ul>\n' +
      '          </div>\n' +
      '        </div>\n' +
      '      </section>\n' +
      '\n' +
      '      <section aria-labelledby="request-scope" className="bg-[#0A0A0A]">\n' +
      '        <div className="mx-auto grid max-w-7xl gap-10 px-6 py-16 sm:px-8 lg:grid-cols-[0.8fr_1.2fr] lg:px-12 lg:py-20">\n' +
      '          <aside className="border-t-2 border-[#FF5A00] pt-6">\n' +
      '            <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">04 / Request a written scope</p>\n' +
      '            <h2 id="request-scope" className="mt-4 text-4xl font-black uppercase leading-none tracking-[-0.05em] text-white sm:text-5xl">Send the job data.</h2>\n' +
      '            <p className="mt-6 max-w-md text-sm leading-7 text-zinc-400">Tell us the property type, city, surfaces, timeline, and preparation needs. The final step asks how Anthony should reach you.</p>\n' +
      '            <p className="mt-8 font-mono text-[10px] uppercase tracking-[0.15em] text-zinc-500">Service area / Minneapolis ┬╖ St. Paul ┬╖ Twin Cities metro</p>\n' +
      '          </aside>\n' +
      '          <div className="border border-zinc-700 bg-[#111111] p-5 sm:p-8"><LeadForm source="homepage_scope_desk" defaultMarket="Residential" /></div>\n' +
      '        </div>\n' +
      '      </section>\n' +
      '    </article>\n' +
      '  );\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    import Link from 'next/link';
    import { ArrowRight, Check, ClipboardCheck, HardHat, Ruler, ShieldCheck } from 'lucide-react';
    import LeadForm from '../components/LeadForm';
    import PrepProtocolStage from '../components/PrepProtocolStage';
    import ResponsiveImage from '../components/ResponsiveImage';
    import { faqSchema } from '../lib/seo';
    
    const scopeRows = [
      ['INTERIOR', 'Walls ┬╖ ceilings ┬╖ trim ┬╖ cabinets', 'Residential'],
      ['EXTERIOR', 'Siding ┬╖ trim ┬╖ decks ┬╖ fences', 'Residential'],
      ['PROPERTY', 'Repaints ┬╖ turnovers ┬╖ common areas', 'Commercial'],
      ['MARKING', 'Parking lots ┬╖ curbs ┬╖ safety striping', 'Public / commercial'],
    ];
    
    const faqItems = [
      {
        question: 'How is a painting project scoped?',
        answer: 'The scope begins with surfaces, condition, access, preparation requirements, photos, and timeline. The result is a written estimate before work is scheduled.',
      },
      {
        question: 'Who performs the walkthrough?',
        answer: 'Anthony Briseno handles the project conversation and walkthrough directly, so the scope is not passed between a sales team and a crew.',
      },
      {
        question: 'Can documentation be provided for property or public work?',
        answer: 'A certificate of insurance is available for qualified commercial and public-sector opportunities. Ask about documentation during the scope review.',
      },
    ];
    
    export default function HomeClient() {
      return (
        <article className="bg-[#0A0A0A] text-zinc-100">
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqSchema(faqItems)) }} />
    
          <section aria-labelledby="home-title" className="border-b border-zinc-800">
            <div className="mx-auto grid min-h-[calc(100svh-117px)] max-w-7xl lg:grid-cols-[minmax(0,1.1fr)_minmax(24rem,0.9fr)]">
              <div className="flex flex-col justify-between px-6 py-10 sm:px-8 lg:px-12 lg:py-14">
                <div>
                  <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">Twin Cities / Residential painting scope desk</p>
                  <h1 id="home-title" className="mt-6 max-w-4xl text-5xl font-black uppercase leading-[0.9] tracking-[-0.06em] text-white sm:text-7xl lg:text-8xl">
                    Paint work with the prep exposed.
                  </h1>
                  <p className="mt-8 max-w-2xl text-base leading-7 text-zinc-300 sm:text-lg">
                    Sky&apos;s the Limit Painting LLC scopes the protection, correction, coating system, and final walkthrough before a project reaches your home.
                  </p>
                </div>
    
                <div className="mt-10">
                  <Link href="/estimate" className="inline-flex min-h-14 items-center gap-4 bg-[#FF5A00] px-6 py-4 text-sm font-black uppercase tracking-[0.12em] text-white transition-colors hover:bg-[#d94d00] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-[#FF5A00]">
                    Calculate room scope <ArrowRight aria-hidden="true" size={18} />
                  </Link>
                  <div className="mt-5 grid max-w-2xl border border-zinc-700 sm:grid-cols-3">
                    {[['MN ID', 'IR816596'], ['WORKERS COMP', '176.041 EXEMPT'], ['COVERAGE', '100% INSURED']].map(([label, value]) => <div key={label} className="border-b border-zinc-800 px-4 py-3 last:border-b-0 sm:border-b-0 sm:border-r sm:last:border-r-0"><p className="font-mono text-[9px] uppercase tracking-[0.14em] text-zinc-500">{label}</p><p className="mt-1 font-mono text-[10px] font-bold uppercase tracking-[0.08em] text-white">{value}</p></div>)}
                  </div>
                  <p className="mt-4 font-mono text-[10px] uppercase tracking-[0.16em] text-zinc-500">Start with surfaces, condition, and dimensions. Contact details come last.</p>
                </div>
              </div>
    
              <div className="relative min-h-[24rem] border-t border-zinc-800 lg:min-h-0 lg:border-l lg:border-t-0">
                <ResponsiveImage src="/images/site/marketing-hero-exterior-painting.webp" alt="Painter preparing an exterior trim surface with windows and landscaping protected" width={1600} height={900} sizes="(min-width: 1024px) 45vw, 100vw" priority fetchPriority="high" className="absolute inset-0 h-full w-full object-cover" />
                <div className="absolute inset-x-0 bottom-0 border-t border-zinc-700 bg-[#0A0A0A]/95 p-5 backdrop-blur-sm">
                  <p className="font-mono text-[10px] uppercase tracking-[0.18em] text-zinc-400">Field image / exterior preparation</p>
                  <p className="mt-2 text-sm font-semibold text-white">Protection and surface correction are part of the work orderΓÇönot an upgrade.</p>
                </div>
              </div>
            </div>
          </section>
    
          <section aria-labelledby="scope-ledger" className="border-b border-zinc-800">
            <div className="mx-auto max-w-7xl px-6 py-16 sm:px-8 lg:px-12">
              <div className="grid gap-8 lg:grid-cols-[0.75fr_1.25fr] lg:gap-16">
                <div>
                  <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">01 / Scope ledger</p>
                  <h2 id="scope-ledger" className="mt-4 text-4xl font-black uppercase leading-none tracking-[-0.05em] text-white sm:text-5xl">What gets priced.</h2>
                  <p className="mt-6 max-w-md text-sm leading-7 text-zinc-400">The estimate follows the actual site conditions. Each line item exists to make preparation and execution visible before scheduling.</p>
                </div>
                <dl className="border-t border-zinc-700">
                  {scopeRows.map(([code, work, market]) => (
                    <div key={code} className="grid grid-cols-[4rem_1fr] gap-4 border-b border-zinc-800 py-5 sm:grid-cols-[5rem_1fr_auto] sm:items-center">
                      <dt className="font-mono text-xs font-bold tracking-widest text-[#FF5A00]">{code}</dt>
                      <dd className="text-sm font-bold uppercase tracking-wide text-white">{work}</dd>
                      <dd className="col-start-2 font-mono text-[10px] uppercase tracking-[0.16em] text-zinc-500 sm:col-start-auto">{market}</dd>
                    </div>
                  ))}
                </dl>
              </div>
            </div>
          </section>
    
          <PrepProtocolStage />
    
          <section aria-labelledby="proof" className="border-b border-zinc-800">
            <div className="mx-auto grid max-w-7xl lg:grid-cols-2">
              <div className="relative aspect-[4/3] border-b border-zinc-800 lg:aspect-auto lg:border-b-0 lg:border-r">
                <ResponsiveImage src="/images/site/iphone-interior-painting-progress.webp" alt="Interior painting work in progress with room surfaces prepared for coating" width={1200} height={900} sizes="(min-width: 1024px) 50vw, 100vw" className="absolute inset-0 h-full w-full object-cover" />
              </div>
              <div className="px-6 py-16 sm:px-8 lg:px-12">
                <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">03 / Operational controls</p>
                <h2 id="proof" className="mt-4 text-4xl font-black uppercase leading-none tracking-[-0.05em] text-white sm:text-5xl">A contractor record you can inspect.</h2>
                <ul className="mt-10 divide-y divide-zinc-800 border-y border-zinc-800">
                  {[
                    [ShieldCheck, 'MN ID: IR816596', 'Minnesota specialty contractor registration'],
                    [HardHat, '176.041 Workers Comp Exempt', 'Owner-operator exemption record'],
                    [ClipboardCheck, '100% Insured', 'Certificate of insurance available for qualified opportunities'],
                    [Ruler, 'Direct scope review', 'Owner-led walkthrough through final detail'],
                  ].map(([Icon, title, detail]) => {
                    const Mark = Icon as typeof ShieldCheck;
                    return <li key={title as string} className="flex gap-4 py-5"><Mark aria-hidden="true" className="mt-1 shrink-0 text-[#FF5A00]" size={18} /><div><p className="text-sm font-bold uppercase tracking-wide text-white">{title as string}</p><p className="mt-1 font-mono text-[10px] uppercase tracking-[0.12em] text-zinc-500">{detail as string}</p></div></li>;
                  })}
                </ul>
              </div>
            </div>
          </section>
    
          <section aria-labelledby="request-scope" className="bg-[#0A0A0A]">
            <div className="mx-auto grid max-w-7xl gap-10 px-6 py-16 sm:px-8 lg:grid-cols-[0.8fr_1.2fr] lg:px-12 lg:py-20">
              <aside className="border-t-2 border-[#FF5A00] pt-6">
                <p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-[#FF5A00]">04 / Request a written scope</p>
                <h2 id="request-scope" className="mt-4 text-4xl font-black uppercase leading-none tracking-[-0.05em] text-white sm:text-5xl">Send the job data.</h2>
                <p className="mt-6 max-w-md text-sm leading-7 text-zinc-400">Tell us the property type, city, surfaces, timeline, and preparation needs. The final step asks how Anthony should reach you.</p>
                <p className="mt-8 font-mono text-[10px] uppercase tracking-[0.15em] text-zinc-500">Service area / Minneapolis ┬╖ St. Paul ┬╖ Twin Cities metro</p>
              </aside>
              <div className="border border-zinc-700 bg-[#111111] p-5 sm:p-8"><LeadForm source="homepage_scope_desk" defaultMarket="Residential" /></div>
            </div>
          </section>
        </article>
      );
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/site-architecture.test.mjs:36:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: remediation guardrails cover secrets, headers, App Router SEO, and accessible interactions
not ok 99 - remediation guardrails cover secrets, headers, App Router SEO, and accessible interactions
  ---
  duration_ms: 5.5383
  type: 'test'
  location: 'C:\\Users\\Johnny Cage\\DEV\\skysthelimit-collab\\tests\\site-architecture.test.mjs:42:1'
  failureType: 'testCodeFailure'
  error: 'vercel.json must not coexist with vercel.ts'
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///C:/Users/Johnny%20Cage/DEV/skysthelimit-collab/tests/site-architecture.test.mjs:48:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: local SEO and service landing pages are routable and listed in the sitemap
ok 100 - local SEO and service landing pages are routable and listed in the sitemap
  ---
  duration_ms: 2.0261
  type: 'test'
  ...
# Subtest: M2 compliance and contractor registration statements are correctly set
ok 101 - M2 compliance and contractor registration statements are correctly set
  ---
  duration_ms: 5.4687
  type: 'test'
  ...
1..101
# tests 299
# suites 22
# pass 280
# fail 19
# cancelled 0
# skipped 0
# todo 0
# duration_ms 9359.601
