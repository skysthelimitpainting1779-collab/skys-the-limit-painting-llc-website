node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.vercel
tmp/
.next/
tsconfig.tsbuildinfo
*.tsbuildinfo
graphify-out/

# Claude Code runtime state only (NOT agent definitions — those are tracked)
.claude/*
!.claude/agents/
!.claude/agents/*.md

# Ignore generated skill script outputs only
.agents/skills/*/lib/
.agents/skills/*/*/lib/

# Ignore runtime/operational databases and caches (not knowledge files)
.agents/hub_db.json
.agents/coordination.db
.agents/coordination.db-journal
.agents/dashboard.html

# Ignore transient agent plan/task mirrors (these are IDE scratch, not repo artifacts)
plan.md
task.md

# Foglamp codebase scan (public unlisted map + secret edit token)
.foglamp/
