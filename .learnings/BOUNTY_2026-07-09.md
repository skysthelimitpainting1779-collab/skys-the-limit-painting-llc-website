---
type: report
title: Ontology + multi-domain upgrade bounty
tags: [bounty, ontology, seo, performance, devops]
---

# Bounty results — 2026-07-09

## Ontology (absolute best setup)

| Piece | Status |
|-------|--------|
| Kernel `.agents/AGENTS.md` | slim, domain-aware |
| Map `ONTOLOGY.md` v2.1 | layers + domain agents + Turso |
| 8 domain packages | state, errors, successes, SYSTEM_PROMPT |
| Turso `domain_agent_state` + `domain_events` | synced |
| Orchestrator protocol | `domains/ORCHESTRATOR.md` |
| No archives | hard-delete purge |
| Enforce jurisdiction | `domain:enforce` |
| Health includes domains | `agentos:health` |

## Product upgrades shipped

### SEO (`seo` agent)
- `src/app/sitemap.ts` — dynamic routes + landing pages from data
- `src/app/robots.ts` — AI crawlers + block admin/api/review
- `public/robots.txt` aligned
- JSON-LD `hasCredential` MN IR816596
- Richer metadata (title template, robots, OG/twitter)

### Performance (`frontend-vercel`)
- Removed **blocking multi-family Google Fonts CSS** (major LCP/request savings)
- `next/font` Geist only (`display: swap`)
- `CustomCursor` + `HeatmapOverlay` **dynamic `ssr: false`** — off critical path

### Design (`ui-ux`)
- CSS font stacks use Geist variable + system industrial fallbacks
- Radius 0 already enforced globally

### DevOps / security (`ci-devops`)
- `poweredByHeader: false`
- Security headers: nosniff, referrer, frame, permissions-policy
- Image formats avif/webp
- Long-cache `/brand/*`

### Agent OS
- Domain sync step in learn-pipeline
- Domain health in ontology health

## Commands for next agent

```bash
npm run agentos:health
npm run domain:list
npm run domain:sync -- all
```
