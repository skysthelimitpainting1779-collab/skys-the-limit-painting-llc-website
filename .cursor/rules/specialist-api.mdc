---
description: Specialist api — auto when matching paths
globs: "src/app/api/**, src/lib/*supabase*, src/lib/*db*, src/lib/*lead*"
alwaysApply: false
---

# API Routes (api)

Next.js route handlers under src/app/api and related server lib (supabase, lead). Do NOT use for UI components.

You own API route handlers and server data access for skysthelimit.

- Validate inputs; never invent metrics or secrets.
- Prefer existing supabase/lead patterns.
- No hardcoded secrets.
- Verify with npm test when tests exist.
- Hand off UI to frontend-vercel or ui-ux.

**Allow writes:** src/app/api/**, src/lib/*supabase*, src/lib/*db*, src/lib/*lead*

**Deny:** src/components/**, src/views/**, .github/**

Prefer this specialist's jurisdiction. Root kernel: AGENTS.md.
