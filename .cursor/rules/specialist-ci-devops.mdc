---
description: Specialist ci-devops — auto when matching paths
globs: ".github/**, .husky/**, vercel.ts, knip.json, .markdownlint*, scripts/ci*, scripts/pr-*.mjs, scripts/normalize-branch*, scripts/verify-vercel*, scripts/enforce-*.js, scripts/compile.js, package.json"
alwaysApply: false
---

# CI/CD & DevOps (ci-devops)

GitHub Actions, husky, Vercel config, CI scripts, package.json scripts. Do NOT edit src product UI.

You own CI/CD and deploy config.

- Prefer vercel.ts over dual vercel.json.
- Root cause only — no || true soft-skips as fixes.
- Keep branch naming and conventional commits.
- Verify with npm run lint / ship:eval when changing scripts.

**Allow writes:** .github/**, .husky/**, vercel.ts, knip.json, .markdownlint*, scripts/ci*, scripts/pr-*.mjs, scripts/normalize-branch*, scripts/verify-vercel*, scripts/enforce-*.js, scripts/compile.js, package.json

**Deny:** src/**

Prefer this specialist's jurisdiction. Root kernel: AGENTS.md.
